---
phase: 12.6-self-healing-executor
task: multi-fault-external-test
total_tasks: 1
status: in_progress
last_updated: 2026-03-15T03:08:16Z
---

<current_state>
Phase 12.6 (Self-Healing Executor) code is complete and verified (11/11 must-haves, 579 tests).
We are in an external validation loop — testing against a 5-service multi-fault Docker demo built by an external tester. The demo has 5 simultaneous faults across 3 domains (DB auth, network isolation, Redis binding, worker IP mismatch, spool permissions).

InfraBrain autonomously fixed 3/5 faults in iterative debug→execute cycles. Remaining 2 faults (Redis config path + worker IP via network connect) need the system improvements below.

RunPod was recharged with a new pod URL: https://0rctqo6dtrhe5c-11434.proxy.runpod.net
Config updated. The 122B model may need more RAM — check with a simple test first.
</current_state>

<completed_work>

### Phase 12.6 Code (all done)
- Self-healing module: correction prompt builder, LLM response parser, safety pipeline validation
- Dynamic error-driven loop: error-type progress detection, bonus attempts, correction history
- Skill context enrichment: tool list with privilege info, discovery data, domain knowledge, rolling context
- Error-specific correction hints: already-exists→ALTER, permission→-u 0, TTY→strip -it, connection→network connect
- Executor integration: self-healing replaces CircuitBreaker, fixKnownCommandErrors deleted
- Route wiring: debug→CLI→execute→executor→self-healer pipeline complete
- Self-healer uses strategic model for corrections (fallback to default)
- Executor sanitizes docker exec -it flags before execution
- Soft routing enforcement: warns instead of 503 when models match (agnostic single-model support)

### Multi-Fault Test Progress
- Runde 1: docker network connect multi-fault-demo_dataplane app-core-01 ✓ (network isolation fixed)
- Runde 2: Redis CONFIG SET bind 0.0.0.0 ✓ (runtime fix, but restart reverted because config FILE wasn't edited)
- Runde 3: ALTER USER svcuser WITH PASSWORD 's3cretpw' ✓ (DB auth fixed!)
- Manual fixes needed for: Redis config file path (/usr/local/etc/redis/ not /etc/redis/), Worker IP (docker network connect --ip), spool permissions (chown), Redis protected-mode

### Documentation
- README updated: self-healing, enriched discovery, Qwen3.5 models, 579 tests
- ADR-014: Self-Healing Executor with Error-Driven Correction
- ADR-015: Model Upgrade Strategy — Qwen3.5 MoE Family
- Memory files: multi_fault_learnings.md, next_session_plan.md
</completed_work>

<remaining_work>

### Immediate: Clean Multi-Fault Test (the goal)
1. Verify RunPod models are accessible (test 35B and 122B separately)
2. If 122B doesn't load (RAM), use 35B for everything
3. Reset multi-fault demo: `cd demo/multi-fault-demo/multi-fault-demo && ./reset.sh`
4. Single debug→execute cycle: `/infra:debug` → `/infra:execute` → `curl localhost:9000` = 200 OK
5. If not 200, iterative debug→execute until all 5 faults are fixed

### System Improvements (if single-cycle doesn't work)
- Self-healer: increase maxAttempts from 3 to 5-7 for complex scenarios
- Audit history: show correctedCommand, stderr, errorTypeChanged in `/infra:history` output
- Discovery: config file path finder already added but LLM may still guess wrong paths
- Consider: plan-validation-loop (check for known-bad patterns before executing)
</remaining_work>

<decisions_made>

- Soft routing enforcement instead of hard 503 — agnostic single-model support
- Self-healer uses strategic model (122B) for corrections, fallback to default (35B)
- Error-specific correction hints derived from stderr patterns, not hardcoded scenarios
- Network-expert skill enriched with topology, env vars, error logs, config file discovery
- Planning skill: FORBIDDEN COMMANDS (no container recreation, no CREATE USER)
- Model upgrade: Qwen3.5 MoE family (35B default, 122B strategic, deepseek-r1 forensic)
- Skills should contain domain knowledge hints, not scripted rules
</decisions_made>

<blockers>

- RunPod 122B model needs ~51.5 GB system RAM — previous pod only had 9.4 GB free. Need larger pod or fewer loaded models.
- Cloudflare proxy timeout (100s) kills self-healer when using RunPod. SSH tunnel eliminates this but requires pod SSH access.
- LLM still guesses Redis config path wrong (/etc/redis/ vs /usr/local/etc/redis/) despite config file discovery in skill
</blockers>

<context>
The multi-fault demo is the killer POC. We went from "completely wrong containers" (ddev-* instead of multi-fault-*) to "correct diagnosis, 3/5 faults fixed autonomously" in one session. The remaining gap is:
1. Redis config file path (discovery finds it, LLM ignores discovery)
2. Worker IP (LLM tries to edit .env instead of docker network connect --ip)
3. Spool permissions (would need linux-expert skill, but network-expert is selected)

These are solvable with: better model (122B loaded properly), more self-healer attempts, or multi-skill chaining.

The user wants this POC to work end-to-end before moving on. No pause until 200 OK.
</context>

<next_action>
1. Test RunPod connectivity: `curl -s https://0rctqo6dtrhe5c-11434.proxy.runpod.net/api/tags | python3 -c "import sys,json; [print(m['name']) for m in json.loads(sys.stdin.read())['models']]"`
2. Test 122B model: `curl -s https://0rctqo6dtrhe5c-11434.proxy.runpod.net/api/generate -d '{"model":"qwen3.5:122b-a10b","prompt":"Say ok","stream":false}' --max-time 120`
3. If 122B works: reset multi-fault demo + npm run dev + test
4. If 122B doesn't load: use 35B for everything (soft routing handles this)
5. Goal: curl -s http://localhost:9000 | jq . → all "ok"
</next_action>
