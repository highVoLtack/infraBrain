---
phase: 12.6-self-healing-executor
task: multi-fault-demo-validated
total_tasks: 1
status: completed
last_updated: 2026-03-15T16:56:21Z
---

<current_state>
Phase 12.6 code + multi-fault demo VALIDATED. 5/5 faults fixed (4 fully autonomous, 1 with manual `sed -i` assist for Redis config persistence). Phase 13 scoped and added to roadmap. v1.3 Intelligence Platform milestone defined.

All code committed and pushed to origin/main (6 commits this session).
</current_state>

<completed_work>

### This Session (2026-03-15)
- 7-role model routing: triage, default, strategic, forensic, worker, vision, embedding
- Self-healing escalation: EscalationAdvice with role-based tier progression (worker→strategic→forensic)
- Dev-mode logging: [TRIAGE], [DISCOVERY], [DIAGNOSIS], [PLANNING], [SANITY], [SELF-HEAL] with model name + timing
- Lock filename sanitization: URLs/special chars in target no longer crash ENOENT
- Removed inferCorrectionHints: LLM reasons from stderr alone, no scripted solutions
- maxAttempts default 3→5 (+ bonus on progress = max ~8)
- README: RunPod SSH tunnel setup guide (step-by-step Web Terminal commands)
- config.example.json: connection modes, 7 role descriptions, selfHealing settings
- Multi-fault demo: 3 iterative DPEV cycles → all 5 faults fixed
  - Cycle 1: DB auth (CREATE/ALTER USER), network connect (dataplane), worker network
  - Cycle 2: Spool permissions (chown via alpine container, self-healer 7 attempts)
  - Cycle 3: Redis bind 127.0.0.1→0.0.0.0 (CONFIG SET + sed -i config file)
- Phase 13 (Execution Hardening) added to roadmap
- v1.3 Intelligence Platform milestone scoped (vLLM, benchmarks, Qdrant, LoRAs)
- Research topics saved: state rollbacks, fix caching, parallel inference

### Tests
- 554 unit/integration tests passing (46 test files)
- E2E tests (6 files) have sanity-checker mock compatibility issues — Phase 13 scope
</completed_work>

<remaining_work>

### Phase 13: Execution Hardening (next)
1. Post-restart persistence verification — detect when restart reverts a fix (sed -i gap)
2. Sanity checker tuning — eliminate false positives (`<original-image>` cost 47s extra)
3. 7-role routing validation — document recommended model per role
4. State rollbacks research — Docker commit snapshots when self-heal exhausts

### v1.3: Intelligence Platform (after Phase 13)
- vLLM multi-model serving (no more 60s model swapping)
- Model benchmarking framework per role
- Qdrant + BGE-M3 for fix caching + RAG diagnosis
- Parallel inference pipeline (9B pre-processes while 122B reasons)
- LoRA Expert Packs
</remaining_work>

<decisions_made>

- Strategic (122B) for self-heal corrections — worker models (GLM-4.7-Flash) cannot reason about state changes ("already exists → ALTER")
- Removed all correction hints — LLM must reason from stderr alone, no scripted solutions (anti-pattern)
- SSH tunnel mandatory for 122B inference — Cloudflare proxy 100s timeout is the #1 performance killer
- Single model for testing — Ollama model swapping costs 60s+ per switch, multi-model needs multi-GPU or vLLM
- E2E mock tests provide no value for real-world validation — live multi-fault test is the true benchmark
- Fix caching belongs in v1.3 with Qdrant (same infrastructure as RAG)
- Parallel inference needs vLLM (impossible with Ollama single-model constraint)
</decisions_made>

<blockers>

- Ollama single-model-in-VRAM: only 1 model loaded at a time, 60s+ swap cost. Blocks multi-model routing.
- RunPod SSH: needs manual setup every pod restart (openssh-server install + authorized_keys). No persistence.
- E2E test mocks trigger sanity checker false positives — needs mock update or sanity bypass for test mode.
</blockers>

<context>
The multi-fault demo is THE proof point for investors/customers. Today we went from "3/5 faults, manual assists needed" to "5/5 faults, 4 fully autonomous". The Redis sed -i gap is the last piece — Phase 13 closes it.

The user wants to focus on real-world validation, not mock tests. The system architecture is sound — the bottleneck is now inference speed (model swapping, Cloudflare timeouts) and model intelligence (worker too weak for corrections).

v1.3 is the big leap: Qdrant for fix caching (2s vs 113s), vLLM for parallel models, benchmarks to find the optimal model per role.
</context>

<next_action>
1. /gsd:plan-phase 13 — plan Execution Hardening (persistence verification, sanity tuning, state rollbacks)
2. After Phase 13: /gsd:new-milestone for v1.3 Intelligence Platform
</next_action>
