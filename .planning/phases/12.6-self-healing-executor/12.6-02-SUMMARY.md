---
phase: 12.6-self-healing-executor
plan: 02
subsystem: execution
tags: [self-healing, executor-integration, circuit-breaker-replacement, rolling-context, backwards-compatible]

requires:
  - phase: 12.6-self-healing-executor
    provides: selfHealStep() correction loop, SelfHealContext/SelfHealResult types, selfHealing config
provides:
  - Self-healing wired into executor step loop replacing blind CircuitBreaker retries
  - ExecutionDeps extended with correctionModel, skill, rewriteRules, containers
  - Rolling context tracks actual corrected command after self-healing
  - fixKnownCommandErrors regex band-aid fully removed from planner.ts
  - Backwards-compatible fallback to CircuitBreaker when self-healing deps absent
affects: [12.6-03, executor, planner, e2e-tests]

tech-stack:
  added: []
  patterns: [self-healing-first-retry, backwards-compatible-deps, corrected-command-tracking]

key-files:
  modified:
    - src/execution/executor.ts
    - src/execution/types.ts
    - src/orchestrator/planner.ts
    - tests/execution/executor.test.ts
    - tests/orchestrator/planner.test.ts

key-decisions:
  - "Self-healing deps optional on ExecutionDeps for backwards compatibility with existing callers"
  - "First attempt runs directly (no CircuitBreaker), self-healing triggered only on failure"
  - "Shell mode detection via needsShell() applied to first attempt too (not just corrections)"
  - "fixKnownCommandErrors deleted entirely -- self-healing handles errors at execution time"

patterns-established:
  - "Optional enhancement deps: correctionModel+skill presence gates self-healing activation"
  - "Corrected command propagation: rolling context receives actual command used, not original"

requirements-completed: [ENGN-08]

duration: 4min
completed: 2026-03-14
---

# Phase 12.6 Plan 02: Executor Integration Summary

**Self-healing wired into executor step loop with CircuitBreaker fallback, fixKnownCommandErrors deleted, rolling context tracks corrected commands**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-14T17:52:55Z
- **Completed:** 2026-03-14T17:57:07Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- Executor step loop now routes failed commands to selfHealStep when correctionModel+skill deps present
- CircuitBreaker fallback preserved for callers without self-healing deps (zero breaking changes)
- fixKnownCommandErrors function deleted (50 lines of regex patches) -- self-healing handles errors dynamically
- Rolling context updated with actual corrected command so downstream LLM calls see what really ran
- 556 tests passing (4 new executor tests, 8 removed fixKnownCommandErrors tests, 1 new removal confirmation test)

## Task Commits

Each task was committed atomically:

1. **Task 1: Wire self-healing into executor.ts + extend ExecutionDeps** - `2c27cbe` (feat)
2. **Task 2: Remove fixKnownCommandErrors + verify full suite** - `dfc0187` (fix)

## Files Created/Modified
- `src/execution/executor.ts` - Self-healing integration in step loop, shell mode first attempt, corrected command tracking
- `src/execution/types.ts` - ExecutionDeps extended with correctionModel, skill, rewriteRules, containers
- `src/orchestrator/planner.ts` - fixKnownCommandErrors function and post-process .map() removed
- `tests/execution/executor.test.ts` - 4 new self-healing integration tests (success, CB fallback, exhausted, rolling context)
- `tests/orchestrator/planner.test.ts` - fixKnownCommandErrors tests replaced with removal confirmation test

## Decisions Made
- Self-healing deps (correctionModel, skill, rewriteRules, containers) are optional on ExecutionDeps so all existing callers work without changes
- First attempt runs command directly before CircuitBreaker or self-healing -- avoids unnecessary retry overhead on success
- needsShell() check applied to first attempt too, using runShellCommand for piped/redirected commands
- fixKnownCommandErrors deleted entirely rather than deprecated -- self-healing is the replacement

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Executor integration complete, ready for Plan 03 (E2E integration testing)
- ExecutionDeps interface documented for callers to provide self-healing deps
- Plan 03 will test the full DPEV chain with self-healing correction in live scenarios

---
*Phase: 12.6-self-healing-executor*
*Completed: 2026-03-14*
