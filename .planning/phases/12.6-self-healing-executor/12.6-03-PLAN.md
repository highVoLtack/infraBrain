---
phase: 12.6-self-healing-executor
plan: 03
type: execute
wave: 2
depends_on: ["12.6-01"]
files_modified:
  - tests/execution/self-healer-integration.test.ts
  - src/routes/debug.ts
autonomous: false
requirements: [ENGN-08]

must_haves:
  truths:
    - "Self-healing context (correctionModel, skill, rewriteRules, containers) passed from debug route to executor"
    - "End-to-end self-healing: bad command -> LLM correction -> safety validation -> successful execution"
    - "Permission Trap live DPEV passes without regex command patches"
  artifacts:
    - path: "tests/execution/self-healer-integration.test.ts"
      provides: "Integration test simulating full self-healing pipeline with mocked LLM"
      min_lines: 80
    - path: "src/routes/debug.ts"
      provides: "Wired self-healing deps into executePlan call"
      contains: "correctionModel"
  key_links:
    - from: "src/routes/debug.ts"
      to: "src/execution/executor.ts"
      via: "ExecutionDeps with correctionModel + skill + rewriteRules + containers"
      pattern: "correctionModel"
---

<objective>
Wire self-healing dependencies from the debug route into the executor, write an integration test proving the full pipeline, and verify with a human checkpoint that the Permission Trap live DPEV works without regex patches.

Purpose: Plan 01 built the self-healer module, Plan 02 wired it into the executor. This plan ensures the debug route (the actual entry point) passes the required self-healing context (model, skill, rewrite rules, containers) through to the executor, and provides an integration test plus live verification.

Output: Debug route wired, integration test, live Permission Trap verification
</objective>

<execution_context>
@/Users/nikolasretkovic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nikolasretkovic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.6-self-healing-executor/12.6-01-SUMMARY.md
@.planning/phases/12.6-self-healing-executor/12.6-RESEARCH.md

<interfaces>
<!-- From Plans 01 + 02 outputs -->

From src/execution/types.ts (extended in Plans 01+02):
```typescript
export interface ExecutionDeps {
  // ... existing fields ...
  correctionModel?: LanguageModel;
  skill?: SkillFile;
  rewriteRules?: RewriteRule[];
  containers?: string[];
}
```

From src/execution/self-healer.ts (Plan 01):
```typescript
export async function selfHealStep(step, failedResult, context): Promise<SelfHealResult>;
```

From src/routes/debug.ts (current -- to be modified):
```typescript
// The debug route calls executePlan() with ExecutionDeps
// It already has access to: selectedSkill, containers, rewriteRules from the DPEV flow
// Needs to: pass correctionModel, skill, rewriteRules, containers into ExecutionDeps
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire self-healing deps in debug route + integration test</name>
  <files>src/routes/debug.ts, tests/execution/self-healer-integration.test.ts</files>
  <action>
1. **Modify debug.ts** where `executePlan()` is called:
   - The debug route already has `selectedSkill` (SkillFile), `containers` (string[]), and `rewriteRules` (RewriteRule[]) from the DPEV flow
   - Add `correctionModel` to the ExecutionDeps object: use `registry.get('default')` for corrections (default model = general-purpose, good for simple syntax fixes)
   - Add `skill: selectedSkill`, `rewriteRules`, `containers` to the ExecutionDeps
   - This enables self-healing for all commands executed through the debug route
   - Import LanguageModel type if needed

2. **Create integration test** (tests/execution/self-healer-integration.test.ts):
   - Test the full pipeline: selfHealStep with mocked LLM + real safety functions
   - Scenario: `docker exec container chown -u 0 1000:1000 /app/data` fails with "invalid option -- 'u'" stderr
   - Mock LLM returns corrected: `chown 1000:1000 /app/data`
   - Safety pipeline allows it (mock skill with chown in tools)
   - Dynamic rewriter wraps it: `docker exec -u 0 container chown 1000:1000 /app/data`
   - Runner executes corrected command successfully (mock exit 0)
   - Assert: result.status === 'success', result.attempts.length === 1, audit logged
   - Second scenario: LLM returns blocked command, then corrects on retry
   - Third scenario: all 3 attempts fail, returns 'exhausted'

3. **Run tests**:
   - `npx vitest run tests/execution/self-healer-integration.test.ts --reporter=verbose`
   - `npx vitest run --reporter=verbose` (full suite)
  </action>
  <verify>
    <automated>npx vitest run tests/execution/self-healer-integration.test.ts --reporter=verbose && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>
  - Debug route passes correctionModel, skill, rewriteRules, containers to executePlan
  - Integration test proves full self-healing pipeline with realistic scenario
  - All tests pass (full suite green)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Permission Trap live DPEV with self-healing</name>
  <files>n/a</files>
  <action>
Human verifies the Permission Trap live DPEV passes end-to-end with the self-healing executor. No code changes -- this is a verification-only checkpoint.

Self-healing executor has been built and wired: when LLMs generate wrong command syntax (e.g., bad chown flags, missing -u 0), the executor captures the error, asks the LLM for a corrected command, validates it through the safety pipeline, and retries -- up to 3 times. fixKnownCommandErrors() has been completely removed.
  </action>
  <verify>
1. Start the Permission Trap demo:
   ```bash
   cd demo/permission-trap && docker compose up -d && cd ../..
   ```
2. Run InfraBrain debug:
   ```bash
   npm run dev
   # Then in the CLI: /infra:debug "the permission-trap container is crashing with a permission error"
   ```
3. Watch for self-healing in action:
   - If the LLM generates a wrong chown command, you should see a `[SELF-HEAL]` log showing the error and corrected command
   - The fix should apply successfully after correction
   - The app should recover (PID file written, container stays running)
4. Check that no `[PLANNER] Fixed command:` logs appear (fixKnownCommandErrors removed)
5. Verify the audit trail includes correction attempts if self-healing was triggered

Expected: Permission Trap DPEV passes end-to-end. Self-healing may or may not trigger depending on whether the LLM gets the command right on first try -- that's fine. The important thing is that there are no regex patches and the system handles errors gracefully.
  </verify>
  <done>
  - Permission Trap live DPEV passes without regex command patches
  - Self-healing triggers and succeeds if LLM generates wrong syntax (or LLM gets it right first try)
  - No [PLANNER] Fixed command: logs in output
  </done>
</task>

</tasks>

<verification>
npx vitest run --reporter=verbose
# Full suite green, integration test passes
# Live Permission Trap DPEV verified by human
</verification>

<success_criteria>
- Debug route wires all self-healing deps to executePlan
- Integration test proves full self-healing pipeline (error -> LLM correction -> safety -> retry -> success)
- Permission Trap live DPEV passes without regex command patches
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/12.6-self-healing-executor/12.6-03-SUMMARY.md`
</output>
