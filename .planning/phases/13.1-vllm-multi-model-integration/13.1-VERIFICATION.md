---
phase: 13.1-vllm-multi-model-integration
verified: 2026-03-17T21:56:00Z
status: gaps_found
score: 12/13 must-haves verified
gaps:
  - truth: "Full test suite passes with zero regressions"
    status: failed
    reason: "tests/cli/commands.test.ts still asserts old Ollama-specific message 'Ollama not detected' in the health-disconnected test, but the CLI health command now emits 'No backends detected. Check your defaultBaseUrl config.' following the new multi-backend response format."
    artifacts:
      - path: "tests/cli/commands.test.ts"
        issue: "Line 120: expect(logCalls).toContain('Ollama not detected') — assertion was not updated when CLI health display was migrated to multi-backend format in Plan 03 fix commit 2f49a98"
    missing:
      - "Update tests/cli/commands.test.ts line 120 to assert 'No backends detected' (or the actual new output) instead of 'Ollama not detected'"
      - "Optionally: update the mock response at lines 105-111 to use the new multi-backend format { status: 'ok', backends: [], summary: 'all_disconnected' } instead of the old single-backend format { status: 'ok', ollama: 'disconnected', error: '...' }"
human_verification:
  - test: "End-to-end vLLM multi-model serving"
    expected: "Permission Trap DPEV runs with triage on vLLM port 8000 (9B) and diagnosis on Ollama port 11434 (122B), no 60s+ swap delay between roles"
    why_human: "Requires live RunPod vLLM instance — automated tests use mocks only. ENGN-10i is flagged manual-only in RESEARCH.md."
---

# Phase 13.1: vLLM Multi-Model Integration Verification Report

**Phase Goal:** Replace Ollama's single-model-in-VRAM constraint with vLLM's OpenAI-compatible multi-model serving. All 7 roles (triage/default/strategic/forensic/worker/vision/embedding) can run simultaneously without 60s+ model swapping. This unblocks the live demo and enables true multi-role routing.

**Verified:** 2026-03-17T21:56:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | ModelMap accepts both string entries (legacy) and `{ model, baseUrl }` object entries | VERIFIED | `ModelMapEntrySchema` is `z.union([z.string(), z.object({model, baseUrl})])` in `src/config/types.ts:6-12`. 10 tests pass in `tests/config/types.test.ts`. |
| 2 | Config field renamed from `ollamaBaseUrl` to `defaultBaseUrl` with backwards compat | VERIFIED | `InfraBrainConfigSchema` uses `z.preprocess` to map legacy `ollamaBaseUrl` to `defaultBaseUrl`. Test "maps legacy ollamaBaseUrl to defaultBaseUrl" passes. |
| 3 | `createModelRegistry` creates per-role models with role-specific baseURLs | VERIFIED | `src/llm/openai-compat.ts:31-64` loops all 7 ROLES, resolves `baseUrl` from entry type (string → `defaultBaseUrl`, object → `entry.baseUrl`). 12 tests pass. |
| 4 | Provider cache reuses the same provider for identical baseURLs | VERIFIED | `providerCache = new Map<string, provider>()` at `openai-compat.ts:33`. Test "provider cache reuses same baseURL" confirms `createOpenAICompatible` called once for 7 same-URL roles. |
| 5 | `supportsStructuredOutputs` flag is set for `generateObject` compatibility | VERIFIED | `createCompatProvider` passes `supportsStructuredOutputs: true` at `openai-compat.ts:22`. Test "sets supportsStructuredOutputs to true" passes. |
| 6 | All callers use openai-compat provider instead of ollama provider | VERIFIED | `src/index.ts:8` imports from `./llm/openai-compat.js`. Zero matches for `ollamaBaseUrl`, `createOllamaModel`, `ai-sdk-ollama`, `from.*ollama` in `src/` (excluding the backwards-compat handler). |
| 7 | Health check probes all unique backends via OpenAI `/v1/models` endpoint | VERIFIED | `src/api/routes/health.ts:48` fetches `${baseUrl}/models`. `extractUniqueBackendUrls()` deduplicates. 7 health tests pass including multi-backend, dedup, and timeout cases. |
| 8 | Status route uses `defaultBaseUrl` not `ollamaBaseUrl` | VERIFIED | `StatusRouteDeps` interface has `defaultBaseUrl: string` at `status.ts:10`. Fetches `${deps.defaultBaseUrl}/models` at `status.ts:36`. |
| 9 | `ollama.ts` is deleted — no Ollama-specific code remains | VERIFIED | `ls src/llm/` shows `openai-compat.ts`, `provider.ts`, `token-budget.ts`, `toon-encoder.ts`, `types.ts` — no `ollama.ts`. Zero Ollama-specific imports in `src/`. |
| 10 | Full test suite passes with zero regressions | FAILED | 626/636 non-skipped tests pass. 1 non-E2E failure: `tests/cli/commands.test.ts` — "health command shows disconnected status when Ollama is down" asserts `'Ollama not detected'` but CLI now outputs `'No backends detected. Check your defaultBaseUrl config.'`. |
| 11 | `config.example.json` shows both legacy and new hybrid ModelMap formats | VERIFIED | `config.example.json` contains `defaultBaseUrl`, `triage: { model, baseUrl }` hybrid entry, plus embedded `_example_legacy` and `_example_full_vllm` sections. |
| 12 | vLLM setup documentation explains hybrid deployment | VERIFIED | `docs/VLLM-SETUP.md` exists (200 lines), covers RunPod SSH tunnel, vLLM launch command, Ollama fallback, and troubleshooting. |
| 13 | `MODEL-ROUTING.md` updated with per-role baseURL routing | VERIFIED | `docs/MODEL-ROUTING.md` line 44 confirms "Per-Role Backend Routing" section explaining string vs object ModelMap entries. |

**Score:** 12/13 truths verified

---

## Required Artifacts

### Plan 01 Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/llm/openai-compat.ts` | Unified OpenAI-compatible provider + model registry | VERIFIED | 65 lines. Exports `createCompatModel` and `createModelRegistry`. Uses `@ai-sdk/openai-compatible`. |
| `src/config/types.ts` | Updated config schema with union ModelMap + `defaultBaseUrl` | VERIFIED | Contains `ModelMapEntrySchema`, `defaultBaseUrl` default `'http://localhost:11434/v1'`, `z.preprocess` backwards compat. |
| `tests/llm/openai-compat.test.ts` | Unit tests for provider creation and registry | VERIFIED | 12 tests, all passing. Covers model creation, registry, per-role routing, provider cache, structured output flag. |
| `tests/config/types.test.ts` | Unit tests for config schema migration | VERIFIED | 10 tests, all passing. Covers union entries, defaults, legacy migration, precedence. |

### Plan 02 Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/index.ts` | Updated imports from openai-compat | VERIFIED | Line 8: `from './llm/openai-compat.js'`. Line 22: re-exports `createCompatModel, createModelRegistry`. Line 35: `config.defaultBaseUrl` used. |
| `src/api/routes/health.ts` | Multi-backend health check via `/v1/models` | VERIFIED | `createHealthRoute(config, registry?)` signature. `extractUniqueBackendUrls()` exported. Fetches `/models` on all unique backends. |
| `src/api/routes/status.ts` | Provider-agnostic status route | VERIFIED | `StatusRouteDeps.defaultBaseUrl: string`. Fetches `${deps.defaultBaseUrl}/models`. |
| `tests/api/health.test.ts` | Unit tests for multi-backend health check (ENGN-10f) | VERIFIED | 7 tests, all passing: URL extraction, single connected, mixed status, all down, timeout, registry availability, deduplication. 60+ lines (241 total). |

### Plan 03 Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `config.example.json` | Reference config with hybrid vLLM+Ollama deployment example | VERIFIED | Contains `defaultBaseUrl`, hybrid triage entry as object, plus legacy and full-vLLM examples as doc sections. |
| `docs/VLLM-SETUP.md` | RunPod vLLM deployment guide | VERIFIED | 200 lines. Covers why vLLM, SSH tunnel setup with autossh, vLLM serve command, Ollama fallback, InfraBrain config, health verification, troubleshooting. |

---

## Key Link Verification

### Plan 01 Key Links

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/llm/openai-compat.ts` | `@ai-sdk/openai-compatible` | `createOpenAICompatible` import | WIRED | Line 1: `import { createOpenAICompatible } from '@ai-sdk/openai-compatible'`. Package in `package.json:16`. |
| `src/config/types.ts` | `src/llm/openai-compat.ts` | `ModelMap` type consumed by `createModelRegistry` | WIRED | `openai-compat.ts:3` imports `ModelMap` from `../config/types.js`. `createModelRegistry` parameter typed as `ModelMap`. |

### Plan 02 Key Links

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/index.ts` | `src/llm/openai-compat.ts` | `import createModelRegistry` | WIRED | `src/index.ts:8`: `import { createCompatModel, createModelRegistry } from './llm/openai-compat.js'`. Used at line 35. |
| `src/api/routes/health.ts` | OpenAI `/v1/models` | `fetch` to `/models` endpoint | WIRED | `health.ts:48`: `fetch(\`${baseUrl}/models\`, ...)`. 3s AbortController timeout. |
| `src/api/server.ts` | `src/api/routes/health.ts` | `createHealthRoute` with config | WIRED | `server.ts:46`: `app.use('/health', createHealthRoute(deps.config!, deps.modelRegistry))`. |
| `tests/api/health.test.ts` | `src/api/routes/health.ts` | `import createHealthRoute` | WIRED | `health.test.ts:4`: `import { createHealthRoute, extractUniqueBackendUrls } from '../../src/api/routes/health.js'`. |

---

## Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| ENGN-10 | 13.1-01, 13.1-02, 13.1-03 | Multi-model parallel serving via vLLM OpenAI-compatible API, eliminating Ollama single-model-in-VRAM bottleneck | SUBSTANTIALLY SATISFIED (1 test gap) | Unified `@ai-sdk/openai-compatible` provider operational. Per-role baseURL routing implemented and tested. `ollama.ts` deleted. `ai-sdk-ollama` removed. Health multi-backend probing working. User verified with RunPod Ollama (5 models). One test not updated for new CLI output format. |

**Sub-requirements from RESEARCH.md:**

| Sub-req | Description | Status | Evidence |
|---------|-------------|--------|---------|
| ENGN-10a | `createOpenAICompatProvider` creates valid LanguageModel | VERIFIED | `openai-compat.test.ts` "creates model with default baseURL" passes. |
| ENGN-10b | Per-role baseURL routing in registry | VERIFIED | "object entry uses its own baseUrl" passes. |
| ENGN-10c | Backwards-compatible string modelMap entries | VERIFIED | "string entry uses defaultBaseUrl" passes. |
| ENGN-10d | Object modelMap entries with baseUrl | VERIFIED | "per-role baseUrl routing uses correct model IDs" passes. |
| ENGN-10e | Config schema migration (ollamaBaseUrl → defaultBaseUrl) | VERIFIED | `types.test.ts` "maps legacy ollamaBaseUrl to defaultBaseUrl" passes. |
| ENGN-10f | Health check probes all unique backends | VERIFIED | 7 health tests pass including deduplication and multi-backend. |
| ENGN-10g | `generateObject` works with structured output flag | VERIFIED | "sets supportsStructuredOutputs to true" passes. |
| ENGN-10h | Provider cache reuses same baseURL | VERIFIED | "provider cache reuses same baseURL" — `createOpenAICompatible` called once. |
| ENGN-10i | Full DPEV with vLLM backend | NEEDS HUMAN | Requires live RunPod + vLLM. User verified with Ollama backend (5 models); vLLM-specific path not yet tested. |

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `tests/cli/commands.test.ts` | 120 | Stale assertion `'Ollama not detected'` — not updated when CLI health display was rewritten | Blocker | 1 test failing in non-E2E suite. Assertion matches old single-backend response shape; CLI now uses multi-backend format. |

**E2E failures (pre-existing, not caused by this phase):**

- `tests/e2e/poc-postgres-connleak.test.ts` — 1 failure (pre-existing, requires live infrastructure)
- `tests/e2e/poc-docker-storage.test.ts` — 3 failures (pre-existing)
- `tests/e2e/poc-nginx-502.test.ts` — 3 failures (pre-existing)
- `tests/e2e/poc-permission-trap.test.ts` — 3 failures (pre-existing)

The E2E failures are documented pre-existing conditions from Plan 01 SUMMARY.md ("Pre-existing E2E failures (4 files, 9 tests) are unrelated to this plan"). They require live Docker + infrastructure. Confirmed pre-existing: Plan 02 SUMMARY says "627 tests passing" which matches the current 626 passing + 1 new CLI test failing.

---

## Human Verification Required

### 1. Live vLLM Multi-Model Demo (ENGN-10i)

**Test:** Deploy vLLM on RunPod with a small model (e.g., `Qwen/Qwen3.5-7B`) on port 8000, set up SSH tunnel, configure InfraBrain with `triage: { model: "qwen3.5-9b", baseUrl: "http://localhost:8000/v1" }`, and run the Permission Trap DPEV.

**Expected:** Triage role completes in under 3 seconds using vLLM; strategic/forensic roles use Ollama on port 11434 without 60s+ model swapping. Health check at `/health` shows two backends: vLLM connected and Ollama connected.

**Why human:** Requires live RunPod GPU instance with vLLM running. Automated tests mock all HTTP calls. This is the primary goal of Phase 13.1 and can only be confirmed with real hardware.

---

## Gaps Summary

One gap blocks the "full test suite passes with zero regressions" truth:

**`tests/cli/commands.test.ts` line 120** — The test for "health command shows disconnected status when Ollama is down" was not updated when the CLI health display was rewritten in Plan 03 fix commit `2f49a98`. The test mocks the health API response using the old single-backend format (`{ ollama: 'disconnected', error: 'Ollama not detected...' }`) and asserts the CLI prints `'Ollama not detected'`. The CLI now renders the new multi-backend format and prints `'No backends detected. Check your defaultBaseUrl config.'` when the backends array is empty or all disconnected.

**Fix required:** Update `tests/cli/commands.test.ts` to use the new multi-backend response format mock and assert the correct new output string.

This is a trivial one-line test fix. All implementation is correct — the CLI behavior itself is working as intended (confirmed by user verification in Plan 03 checkpoint). This is a test-maintenance gap only.

---

_Verified: 2026-03-17T21:56:00Z_
_Verifier: Claude (gsd-verifier)_
