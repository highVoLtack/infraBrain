---
phase: 13.1-vllm-multi-model-integration
plan: 03
type: execute
wave: 3
depends_on: ["13.1-02"]
files_modified:
  - config.example.json
  - docs/MODEL-ROUTING.md
  - docs/VLLM-SETUP.md
autonomous: false
requirements: [ENGN-10]

must_haves:
  truths:
    - "config.example.json shows both legacy (string) and new (object with baseUrl) ModelMap formats"
    - "vLLM setup documentation explains hybrid deployment (vLLM + Ollama)"
    - "MODEL-ROUTING.md updated with per-role baseURL routing"
  artifacts:
    - path: "config.example.json"
      provides: "Reference config with hybrid vLLM+Ollama deployment example"
      contains: "defaultBaseUrl"
    - path: "docs/VLLM-SETUP.md"
      provides: "RunPod vLLM deployment guide"
  key_links: []
---

<objective>
Update configuration examples and documentation for the new provider-agnostic architecture, then verify the full integration with a checkpoint.

Purpose: Ensure the user can configure hybrid vLLM+Ollama deployments and understand the new config format. The checkpoint verifies everything works together before live RunPod testing.

Output: Updated config example, vLLM setup guide, and user verification of the integration.
</objective>

<execution_context>
@/Users/nikolasretkovic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nikolasretkovic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13.1-vllm-multi-model-integration/13.1-CONTEXT.md
@.planning/phases/13.1-vllm-multi-model-integration/13.1-01-SUMMARY.md
@.planning/phases/13.1-vllm-multi-model-integration/13.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update config example and documentation</name>
  <files>config.example.json, docs/MODEL-ROUTING.md, docs/VLLM-SETUP.md</files>
  <action>
    **Step 1: Update config.example.json**
    Show the new config format with hybrid deployment example:
    ```json
    {
      "defaultBaseUrl": "http://localhost:11434/v1",
      "modelMap": {
        "triage": { "model": "qwen3.5:9b", "baseUrl": "http://localhost:8000/v1" },
        "default": "qwen3.5:35b-a3b",
        "strategic": "qwen3.5:122b-a10b",
        "forensic": "qwen3.5:35b-a3b",
        "worker": "qwen3.5:9b",
        "vision": "qwen3.5:122b-a10b",
        "embedding": "bge-m3"
      }
    }
    ```
    Include comments (via a separate `config.example.md` or inline JSON comments if the project uses JSONC). Show:
    - Legacy format (all strings, single defaultBaseUrl) still works
    - Hybrid format (some roles point to vLLM, rest use Ollama via defaultBaseUrl)
    - Full vLLM format (all roles with explicit baseUrl)

    **Step 2: Update docs/MODEL-ROUTING.md**
    - Add "Per-Role Backend Routing" section explaining the new config format
    - Explain that `defaultBaseUrl` replaces `ollamaBaseUrl` (backwards compatible)
    - Show how string entries use `defaultBaseUrl` and object entries use their own `baseUrl`
    - Add hybrid deployment example (vLLM for triage, Ollama for heavy models)
    - Keep existing 7-role definitions and latency evidence

    **Step 3: Create docs/VLLM-SETUP.md**
    - RunPod SSH tunnel setup (autossh recommended, reference STATE.md notes)
    - vLLM launch command for small model (port 8000):
      ```bash
      vllm serve Qwen/Qwen3.5-7B --dtype bfloat16 --gpu-memory-utilization 0.3 --max-model-len 4096 --served-model-name qwen3.5-9b --port 8000
      ```
    - Ollama serving heavy model on default port 11434
    - InfraBrain config pointing triage/worker to vLLM, rest to Ollama
    - Health check verification: `curl http://localhost:3000/health`
    - Troubleshooting: model name mismatches (vLLM uses HF paths, Ollama uses short names), /v1 suffix requirement, timeout differences
  </action>
  <verify>
    <automated>cat config.example.json | python3 -m json.tool > /dev/null 2>&1 && echo "Valid JSON" || echo "Invalid JSON"</automated>
  </verify>
  <done>
    - config.example.json shows hybrid vLLM+Ollama deployment with per-role baseUrl
    - docs/MODEL-ROUTING.md has per-role backend routing section
    - docs/VLLM-SETUP.md covers RunPod deployment and troubleshooting
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify provider swap works end-to-end</name>
  <files>N/A</files>
  <action>
    User verifies the complete provider swap works with their Ollama setup. All automated work is done in Plans 01-02 and Task 1 above. This checkpoint confirms integration.
  </action>
  <verify>User confirms health check and startup work</verify>
  <done>User types "approved" after verifying InfraBrain starts and health check responds</done>
  <what-built>
    Complete provider swap from Ollama-specific to unified OpenAI-compatible provider:
    1. New `@ai-sdk/openai-compatible` provider with per-role baseURL routing
    2. Config schema with backwards-compatible `defaultBaseUrl` (was `ollamaBaseUrl`)
    3. ModelMap accepts both string and `{ model, baseUrl }` object entries
    4. Multi-backend health check via OpenAI `/v1/models` endpoint
    5. `ai-sdk-ollama` dependency removed, `src/llm/ollama.ts` deleted
    6. Full test suite passing
  </what-built>
  <how-to-verify>
    1. Start InfraBrain with Ollama only (legacy config): `npx tsx src/index.ts`
       - Expected: starts normally, `/health` shows connected
    2. Check health endpoint: `curl http://localhost:3000/health`
       - Expected: `{ "status": "ok", "backends": [...] }` with connected Ollama
    3. (Optional) If vLLM available: update `.infrabrain/config.json` with hybrid config from config.example.json, restart, verify both backends show in health check
    4. Run Permission Trap DPEV if RunPod tunnel available -- verify triage+diagnosis work through new provider
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- config.example.json is valid JSON with hybrid deployment example
- docs/MODEL-ROUTING.md and docs/VLLM-SETUP.md exist with relevant content
- User confirms InfraBrain starts and health check works with Ollama backend
</verification>

<success_criteria>
- Documentation enables user to configure hybrid vLLM+Ollama deployment
- User verifies the provider swap works end-to-end with their Ollama setup
- Ready for live RunPod testing with vLLM
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-vllm-multi-model-integration/13.1-03-SUMMARY.md`
</output>
