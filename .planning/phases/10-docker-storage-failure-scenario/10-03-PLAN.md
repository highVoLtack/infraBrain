---
phase: 10-docker-storage-failure-scenario
plan: 03
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - tests/e2e/poc-docker-storage.test.ts
autonomous: true
requirements: [E2E-02]

must_haves:
  truths:
    - "E2E test starts broken Docker storage environment via reset script"
    - "Test verifies broken state: tmpfs full and Redis refusing writes"
    - "Test exercises full DPEV loop: diagnose via /debug -> execute fix via /execute -> verify recovery"
    - "Test confirms audit trail contains skill_selection, decision, execution events"
    - "Test uses mocked LLM responses — deterministic, no GPU dependency"
  artifacts:
    - path: "tests/e2e/poc-docker-storage.test.ts"
      provides: "Full DPEV loop E2E test for Docker storage scenario"
      min_lines: 150
  key_links:
    - from: "tests/e2e/poc-docker-storage.test.ts"
      to: "demo/docker-storage/reset-docker-storage.sh"
      via: "execSync in beforeAll"
      pattern: "reset-docker-storage"
    - from: "tests/e2e/poc-docker-storage.test.ts"
      to: "skills/docker-storage.md"
      via: "SkillRegistry.populate + selectSkill mock"
      pattern: "docker-storage"
    - from: "tests/e2e/poc-docker-storage.test.ts"
      to: "src/api/routes/debug.ts"
      via: "POST /debug with mocked LLM"
      pattern: "post.*debug"
    - from: "tests/e2e/poc-docker-storage.test.ts"
      to: "src/api/routes/execute.ts"
      via: "POST /execute with canned fix plan"
      pattern: "post.*execute"
---

<objective>
Create the automated E2E test proving the full DPEV loop for the Docker storage failure scenario: broken state -> diagnosis with Causal Deduplication -> risk-tiered fix plan -> execution -> recovery verification -> audit trail completeness.

Purpose: Proves InfraBrain can autonomously diagnose and fix Docker volume-full failures end-to-end.
Output: tests/e2e/poc-docker-storage.test.ts with full DPEV loop validation.
</objective>

<execution_context>
@/Users/nikolasretkovic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nikolasretkovic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-docker-storage-failure-scenario/10-CONTEXT.md
@.planning/phases/10-docker-storage-failure-scenario/10-RESEARCH.md

# Direct template — follow this test's structure exactly
@tests/e2e/poc-postgres-connleak.test.ts

# Plan 01 and 02 summaries (will exist by wave 2)
@.planning/phases/10-docker-storage-failure-scenario/10-01-SUMMARY.md
@.planning/phases/10-docker-storage-failure-scenario/10-02-SUMMARY.md

<interfaces>
<!-- From tests/e2e/poc-postgres-connleak.test.ts — exact pattern to follow: -->

<!-- Mock setup (vi.mock BEFORE imports): -->
<!-- vi.mock('../../src/orchestrator/router.js') — selectSkill -->
<!-- vi.mock('../../src/orchestrator/planner.js') — generateFixPlan (keep real generatePlanMarkdown, formatPlanTable) -->
<!-- vi.mock('../../src/orchestrator/context.js') — buildMessages -->

<!-- Server setup: -->
<!-- createServer({provider, auditLogger, validator, ollamaBaseUrl, registry, config, sessionId, sessionDir, store, lockDir}) -->

<!-- LLMProvider mock interface: -->
<!-- { model: {} as any, registry: { get: () => ({} as any), entries: () => [] } as any, streamDiagnosis, generateCommand } -->

<!-- FixPlan type (from src/orchestrator/types.ts): -->
<!-- { summary: string, complexity: string, steps: { command: string, description: string, risk: string, rollback: string }[] } -->

<!-- WriteThrough.queryAuditLog({ sessionId, limit }): AuditEntry[] -->
<!-- AuditEntry.eventType: string -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Docker storage E2E test with full DPEV loop</name>
  <files>tests/e2e/poc-docker-storage.test.ts</files>
  <action>
Create `tests/e2e/poc-docker-storage.test.ts` following the EXACT structure of `tests/e2e/poc-postgres-connleak.test.ts`. Same imports, same mock setup, same server initialization pattern. The only differences are the scenario-specific content.

**Mock setup (BEFORE imports, same as postgres test):**
- `vi.mock('../../src/orchestrator/router.js')` — selectSkill
- `vi.mock('../../src/orchestrator/planner.js')` — generateFixPlan (keep real generatePlanMarkdown, formatPlanTable)
- `vi.mock('../../src/orchestrator/context.js')` — buildMessages

**Canned fix plan (4 steps per CONTEXT.md):**
```typescript
const cannedFixPlan: FixPlan = {
  summary: 'Truncate bloated log file and restart crashed Redis',
  complexity: 'moderate',
  steps: [
    {
      command: 'docker exec storage-logger du -sh /shared/*',
      description: 'Identify bloat source files on shared volume',
      risk: 'read',
      rollback: 'N/A',
    },
    {
      command: 'docker exec storage-logger truncate -s 0 /shared/bloat.log',
      description: 'Truncate bloated log file (preserves inode)',
      risk: 'write',
      rollback: 'File will be re-created by logger if still running',
    },
    {
      command: 'docker restart storage-redis',
      description: 'Restart Redis to recover from crash state',
      risk: 'write',
      rollback: 'docker stop storage-redis',
    },
    {
      command: 'docker exec storage-redis redis-cli PING',
      description: 'Verify Redis recovered and responding',
      risk: 'read',
      rollback: 'N/A',
    },
  ],
};
```

**beforeAll (120s timeout on describe):**
1. Run `bash demo/docker-storage/reset-docker-storage.sh` via execSync (cwd: PROJECT_ROOT, timeout: 120_000)
2. Verify broken state: poll up to 15 attempts (2s apart):
   - Check `docker exec storage-logger df /shared --output=pcent` >= 95%
   - Check `docker exec storage-redis redis-cli SET infratest 1` fails
   - Both must be true to confirm broken state
3. Create temp directory, session, SQLite DB, WriteThrough store (same as postgres test)
4. Create mock LLM provider with `generateCommand` returning diagnosis text that mentions:
   - Container discovery found storage-logger and storage-redis
   - Capacity check shows /shared at 95%+ usage
   - Ownership analysis shows bloat.log (9MB) owned by logger, dump.rdb by Redis
   - Causal Deduplication: bloat.log = LOG BLOAT (safe to truncate), dump.rdb = STATE DATA (preserve)
   - Risk-tiered fix: truncate log, restart Redis, dual verify
5. Set up SkillRegistry, get docker-storage skill, mock selectSkill and generateFixPlan
6. Create server with all deps (same pattern as postgres test)

**Test 1: "broken environment shows full volume and crashed redis"**
- Verify `docker exec storage-logger df -h /shared` shows high usage
- Verify `docker exec storage-redis redis-cli SET test 1` fails (MISCONF or connection refused)

**Test 2: "diagnoses storage bloat and generates fix plan"**
- POST /debug with prompt about Docker storage / disk full / Redis crashing
- Assert: status 200, diagnosis mentions bloat/storage/full, fixPlan defined with >= 3 steps
- Assert: skillMessage contains "docker-storage"
- Store sessionId and fixPlan for test 3

**Test 3: "executes fix plan and verifies recovery"**
- POST /execute with sessionId, fixPlan, target: "docker-storage", adminName: "e2e-test"
- Assert: status 200, status "completed", stepResults has entries
- Verify recovery: `docker exec storage-redis redis-cli PING` returns PONG
- Verify disk freed: `docker exec storage-logger df /shared --output=pcent` shows < 50%

**Test 4: "audit trail contains full DPEV evidence"**
- Query audit log via store.queryAuditLog({ sessionId: testSessionId, limit: 100 })
- Assert eventTypes contain: skill_selection, decision, execution_start, step_complete, execution_complete
- Assert at least one entry has reasoning content

**afterAll:**
- `docker compose -f demo/docker-storage/docker-compose.yml down --remove-orphans --volumes` (best-effort)
- `rmSync(tmpDir, { recursive: true, force: true })` (best-effort)

**Key difference from postgres test:** 4-step fix plan (not 3), broken state check uses df + redis-cli SET (not psql), recovery check uses redis-cli PING (not psql SELECT).
  </action>
  <verify>
    <automated>cd /Users/nikolasretkovic/Desktop/InfraBrain && npx vitest run tests/e2e/poc-docker-storage.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All 4 E2E tests pass: broken environment confirmed, diagnosis with Causal Deduplication generated, fix plan executed with recovery, audit trail complete with DPEV events</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/e2e/poc-docker-storage.test.ts` — all 4 tests pass
- `npx vitest run` — full test suite passes (no regressions)
- Test exercises real Docker commands against real containers
- Test uses mocked LLM responses (deterministic, no GPU)
- Audit trail verification confirms skill_selection + decision + execution events
</verification>

<success_criteria>
- E2E test proves full DPEV loop: diagnose identifies volume bloat with Causal Deduplication, plan proposes truncate + restart, execute runs it, verify confirms recovery
- Recovery verified with dual check: disk space freed AND redis-cli PING returns PONG
- Audit trail contains all DPEV event types
- Test is deterministic (mocked LLM) and completes within 120s
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/10-docker-storage-failure-scenario/10-03-SUMMARY.md`
</output>
