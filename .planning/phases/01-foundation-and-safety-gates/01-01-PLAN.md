---
phase: 01-foundation-and-safety-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - src/index.ts
  - src/llm/provider.ts
  - src/llm/ollama.ts
  - src/llm/token-budget.ts
  - src/llm/types.ts
  - src/config/defaults.ts
  - src/config/types.ts
  - tests/llm/provider.test.ts
  - tests/llm/token-budget.test.ts
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-09

must_haves:
  truths:
    - "LLM provider interface accepts a prompt and returns a response via Ollama"
    - "Provider can be swapped without changing business logic code"
    - "Token budget is tracked and enforced before LLM calls"
  artifacts:
    - path: "src/llm/provider.ts"
      provides: "LLM provider abstraction with streamDiagnosis and generateCommand"
      exports: ["LLMProvider", "createProvider"]
    - path: "src/llm/ollama.ts"
      provides: "Ollama-specific model factory"
      exports: ["createOllamaModel"]
    - path: "src/llm/token-budget.ts"
      provides: "Token budget tracking and enforcement"
      exports: ["TokenBudget", "estimateTokens", "checkBudget"]
    - path: "src/llm/types.ts"
      provides: "Shared LLM type definitions"
      exports: ["LLMProvider", "TokenUsage", "TaskBudget"]
  key_links:
    - from: "src/llm/provider.ts"
      to: "ai (Vercel AI SDK)"
      via: "streamText and generateText imports"
      pattern: "import.*from 'ai'"
    - from: "src/llm/ollama.ts"
      to: "ai-sdk-ollama"
      via: "ollama provider import"
      pattern: "import.*from 'ai-sdk-ollama'"
    - from: "src/llm/provider.ts"
      to: "src/llm/token-budget.ts"
      via: "pre-flight token check before LLM call"
      pattern: "checkBudget|estimateTokens"
---

<objective>
Project scaffolding and LLM provider abstraction with token budget enforcement.

Purpose: Establish the TypeScript project foundation and the LLM communication layer that all other features build on. The provider abstraction ensures CORE-02 pluggability from day one.

Output: Working Node.js/TypeScript project with ESM, Vitest, and an LLM provider that can stream diagnostic text and generate commands through Ollama with token budget enforcement.
</objective>

<execution_context>
@/Users/nikolasretkovic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nikolasretkovic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-safety-gates/01-CONTEXT.md
@.planning/phases/01-foundation-and-safety-gates/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Initialize project and create LLM provider abstraction</name>
  <files>package.json, tsconfig.json, vitest.config.ts, src/index.ts, src/llm/types.ts, src/llm/provider.ts, src/llm/ollama.ts, src/config/defaults.ts, src/config/types.ts, tests/llm/provider.test.ts</files>
  <behavior>
    - Test: createProvider(mockModel) returns object with streamDiagnosis and generateCommand methods
    - Test: streamDiagnosis yields string chunks from the model stream
    - Test: generateCommand returns a string response from the model
    - Test: createOllamaModel returns a LanguageModel-compatible object (mock ai-sdk-ollama)
    - Test: provider abstraction does not import ollama directly -- only through factory
  </behavior>
  <action>
    1. Initialize the project:
       - `npm init -y` then update package.json: name "infrabrain", type "module", scripts (dev: "tsx src/index.ts", build: "tsup src/index.ts --format esm --dts", test: "vitest run", test:watch: "vitest")
       - Install production deps: `npm install ai ai-sdk-ollama express better-sqlite3 commander zod uuid chalk`
       - Install dev deps: `npm install -D typescript tsx tsup vitest @vitest/coverage-v8 @types/node @types/express @types/better-sqlite3 @types/uuid`
       - Create tsconfig.json: target ES2022, module NodeNext, moduleResolution NodeNext, outDir dist, rootDir src, strict true, esModuleInterop true, declaration true, sourceMap true, include ["src/**/*"]
       - Create vitest.config.ts: import defineConfig from vitest/config, test.globals false, test.include ["tests/**/*.test.ts"]

    2. Create src/config/types.ts:
       - Zod schema for InfraBrainConfig: ollamaBaseUrl (string, default "http://localhost:11434"), modelName (string, default "llama3.3:70b"), apiPort (number, default 3000), sessionDir (string, default ".infrabrain"), tokenBudgets object with diagnosis (number, default 4096) and command (number, default 2048)
       - Export inferred TypeScript type from Zod schema

    3. Create src/config/defaults.ts:
       - Export DEFAULT_CONFIG parsed from the Zod schema defaults
       - Export OLLAMA_HEALTH_URL derived from config baseUrl + "/api/tags"

    4. Create src/llm/types.ts:
       - Export LLMProvider interface: model (LanguageModel from 'ai'), streamDiagnosis(prompt: string, systemPrompt: string): AsyncIterable<string>, generateCommand(prompt: string, systemPrompt: string): Promise<string>
       - Export TokenUsage type: { promptTokens: number, completionTokens: number, totalTokens: number }
       - Export TaskBudget type: { maxTokens: number, taskType: 'diagnosis' | 'command' }

    5. Create src/llm/provider.ts:
       - Import streamText, generateText from 'ai'
       - Import LLMProvider, TaskBudget from './types.js'
       - Export createProvider(model: LanguageModel): LLMProvider
       - streamDiagnosis: uses streamText with model, system prompt, prompt, maxTokens from diagnosis budget (4096). Yields chunks from result.textStream.
       - generateCommand: uses generateText with model, system prompt, prompt, maxTokens from command budget (2048). Returns text.
       - Both methods log token usage after completion via console.debug

    6. Create src/llm/ollama.ts:
       - Import ollama from 'ai-sdk-ollama'
       - Export createOllamaModel(modelName?: string) that returns ollama(modelName ?? 'llama3.3:70b')
       - This is the ONLY file that imports from 'ai-sdk-ollama' -- provider abstraction per CORE-02

    7. Create src/index.ts as minimal entry point: import and re-export from llm modules (placeholder, will be expanded in Plan 04)

    Write tests FIRST (RED), then implement (GREEN). Mock the AI SDK's streamText and generateText using vi.mock('ai').
  </action>
  <verify>
    <automated>cd /Users/nikolasretkovic/Desktop/InfraBrain && npx vitest run tests/llm/provider.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - package.json has all dependencies, type "module", and scripts
    - tsconfig.json configured for ESM with NodeNext resolution
    - vitest.config.ts exists and vitest runs
    - LLMProvider interface exported from src/llm/types.ts
    - createProvider in src/llm/provider.ts wraps AI SDK streamText/generateText
    - createOllamaModel in src/llm/ollama.ts is the sole Ollama import point
    - All provider tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement token budget tracking and enforcement</name>
  <files>src/llm/token-budget.ts, tests/llm/token-budget.test.ts</files>
  <behavior>
    - Test: estimateTokens("hello world") returns approximately 2-3 (using 4-chars-per-token heuristic)
    - Test: estimateTokens("") returns 0
    - Test: checkBudget(prompt, systemPrompt, { maxTokens: 100, taskType: 'diagnosis' }) returns { allowed: true, estimated, budget } when under budget
    - Test: checkBudget with a very long prompt exceeding budget returns { allowed: false, estimated, budget, overflow }
    - Test: trackUsage records actual token usage from AI SDK response and logs warning when actual exceeds 80% of budget
    - Test: estimateTokens handles non-ASCII text (multiply factor for CJK, etc.)
  </behavior>
  <action>
    Create src/llm/token-budget.ts:

    1. Export estimateTokens(text: string): number
       - Use 4-chars-per-token heuristic for ASCII
       - For non-ASCII, use 2-chars-per-token (rough CJK adjustment)
       - Return Math.ceil of the estimate

    2. Export interface BudgetCheck { allowed: boolean; estimatedTokens: number; budgetTokens: number; overflow?: number }

    3. Export checkBudget(prompt: string, systemPrompt: string, budget: TaskBudget): BudgetCheck
       - Estimate tokens for prompt + systemPrompt combined
       - Compare against budget.maxTokens
       - Return allowed: true/false with details
       - If not allowed, include overflow count

    4. Export trackUsage(actual: TokenUsage, budget: TaskBudget): void
       - Log warning to console.warn if actual.promptTokens > budget.maxTokens * 0.8
       - Log debug info with actual vs budget comparison

    Write tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>cd /Users/nikolasretkovic/Desktop/InfraBrain && npx vitest run tests/llm/token-budget.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - estimateTokens returns reasonable approximation using 4-char heuristic
    - checkBudget blocks prompts exceeding budget with clear overflow info
    - trackUsage logs warnings at 80% threshold
    - All token budget tests pass
  </done>
</task>

</tasks>

<verification>
npx vitest run --reporter=verbose
All tests in tests/llm/ pass. TypeScript compiles without errors (npx tsc --noEmit).
</verification>

<success_criteria>
- Project builds and tests run with `npm test`
- LLM provider abstraction exists with pluggable design (CORE-01, CORE-02)
- Token budget enforcement prevents oversized prompts (CORE-09)
- Ollama is isolated behind a factory function (CORE-02 pluggability)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-safety-gates/01-01-SUMMARY.md`
</output>
