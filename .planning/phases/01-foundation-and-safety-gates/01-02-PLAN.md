---
phase: 01-foundation-and-safety-gates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/state/db.ts
  - src/state/session.ts
  - src/state/store.ts
  - src/state/types.ts
  - src/audit/logger.ts
  - src/audit/types.ts
  - tests/state/db.test.ts
  - tests/state/store.test.ts
  - tests/audit/logger.test.ts
autonomous: true
requirements:
  - SAFE-09
  - SAFE-10
  - INTF-06

must_haves:
  truths:
    - "Every decision is logged as structured JSON to both audit.jsonl file and SQLite"
    - "Before/after state diffs are captured for every state change"
    - "Fix plan state persists to both human-readable files and SQLite"
    - "Files are source of truth -- if file and SQLite diverge, file wins"
  artifacts:
    - path: "src/state/db.ts"
      provides: "SQLite schema initialization with sessions and audit_log tables"
      exports: ["initDatabase"]
    - path: "src/state/store.ts"
      provides: "Write-through dual storage (file + SQLite)"
      exports: ["WriteThrough"]
    - path: "src/state/session.ts"
      provides: "Session lifecycle management"
      exports: ["createSession", "loadSession"]
    - path: "src/audit/logger.ts"
      provides: "Structured JSON audit logging with state diffs"
      exports: ["AuditLogger"]
    - path: "src/audit/types.ts"
      provides: "Audit entry type definitions"
      exports: ["AuditEntry", "AuditEventType", "StateDiff"]
  key_links:
    - from: "src/state/store.ts"
      to: "src/state/db.ts"
      via: "SQLite write on every state change"
      pattern: "db\\.prepare|db\\.run"
    - from: "src/state/store.ts"
      to: "filesystem"
      via: "writeFileSync to session directory"
      pattern: "writeFileSync|appendFileSync"
    - from: "src/audit/logger.ts"
      to: "src/state/store.ts"
      via: "audit entries persisted through store"
      pattern: "store\\.persistAudit|appendFileSync"
---

<objective>
State storage, session management, and structured audit logging with state diffs.

Purpose: Build the persistence layer that every other feature depends on -- sessions for tracking fix plans, write-through storage for durability, and audit logging for trust and accountability. This is the "memory" of InfraBrain.

Output: Working dual-write storage (file + SQLite), session lifecycle, and audit logger that captures structured JSON entries with before/after diffs.
</objective>

<execution_context>
@/Users/nikolasretkovic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nikolasretkovic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-safety-gates/01-CONTEXT.md
@.planning/phases/01-foundation-and-safety-gates/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SQLite schema, session management, and write-through store</name>
  <files>src/state/types.ts, src/state/db.ts, src/state/session.ts, src/state/store.ts, tests/state/db.test.ts, tests/state/store.test.ts</files>
  <behavior>
    - Test: initDatabase creates sessions and audit_log tables with correct columns
    - Test: initDatabase sets WAL journal mode and NORMAL synchronous
    - Test: initDatabase is idempotent (calling twice does not error)
    - Test: createSession generates a UUID session ID and creates session directory with state.json
    - Test: loadSession reads state.json from session directory
    - Test: WriteThrough.persistState writes to both file and SQLite atomically
    - Test: WriteThrough.persistState file content matches SQLite content
    - Test: WriteThrough.appendAudit appends JSONL to audit.jsonl and inserts into audit_log table
    - Test: Session directory structure matches .infrabrain/sessions/{session-id}/state.json, audit.jsonl, diffs/
  </behavior>
  <action>
    1. Create src/state/types.ts:
       - Export SessionState interface: { sessionId: string, createdAt: string, updatedAt: string, status: 'active' | 'completed' | 'failed', currentPlan?: FixPlanState }
       - Export FixPlanState interface: { id: string, description: string, steps: FixStep[], currentStep: number, status: 'pending' | 'in_progress' | 'completed' | 'failed' }
       - Export FixStep interface: { id: number, command: string, description: string, status: 'pending' | 'approved' | 'rejected' | 'executed' | 'failed', riskLevel?: string }

    2. Create src/state/db.ts:
       - Import Database from 'better-sqlite3'
       - Export initDatabase(dbPath: string): Database.Database
       - Set pragma journal_mode = WAL, synchronous = NORMAL
       - CREATE TABLE IF NOT EXISTS sessions (id TEXT PRIMARY KEY, state TEXT NOT NULL, created_at TEXT DEFAULT datetime('now'), updated_at TEXT DEFAULT datetime('now'))
       - CREATE TABLE IF NOT EXISTS audit_log (id INTEGER PRIMARY KEY AUTOINCREMENT, session_id TEXT NOT NULL, timestamp TEXT DEFAULT datetime('now'), event_type TEXT NOT NULL, risk_level TEXT, command TEXT, decision TEXT, reasoning TEXT, diff_before TEXT, diff_after TEXT, FOREIGN KEY session_id REFERENCES sessions(id))
       - CREATE INDEX IF NOT EXISTS idx_audit_session, idx_audit_timestamp, idx_audit_event_type
       - Use the exact schema from RESEARCH.md

    3. Create src/state/session.ts:
       - Import { v7 as uuidv7 } from 'uuid' for time-ordered session IDs
       - Export createSession(baseDir: string): SessionState -- creates .infrabrain/sessions/{id}/ with state.json, empty audit.jsonl, diffs/ directory
       - Export loadSession(sessionDir: string): SessionState -- reads and parses state.json

    4. Create src/state/store.ts:
       - Export class WriteThrough with constructor(db: Database.Database)
       - persistState(sessionDir: string, state: SessionState): void -- writeFileSync state.json FIRST (source of truth per user decision), then INSERT OR REPLACE into sessions table
       - appendAudit(sessionDir: string, entry: AuditEntry): void -- appendFileSync to audit.jsonl (one JSON line), then INSERT into audit_log table
       - Both operations are synchronous (better-sqlite3 sync API) for atomicity within same tick

    Write tests FIRST using a temp directory (vitest beforeEach creates tmp dir, afterEach cleans up). Use :memory: or temp file for SQLite in tests.
  </action>
  <verify>
    <automated>cd /Users/nikolasretkovic/Desktop/InfraBrain && npx vitest run tests/state/ --reporter=verbose</automated>
  </verify>
  <done>
    - SQLite database initializes with correct schema (sessions + audit_log tables, indexes)
    - Sessions create directory structure at .infrabrain/sessions/{id}/
    - WriteThrough writes to both file and SQLite on every state change
    - File is always written first (source of truth)
    - All state tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Structured audit logger with state diffs</name>
  <files>src/audit/types.ts, src/audit/logger.ts, tests/audit/logger.test.ts</files>
  <behavior>
    - Test: AuditLogger.logDecision creates entry with event_type "decision", reasoning, options considered
    - Test: AuditLogger.logCommandValidation creates entry with command, risk_level, decision (allowed/blocked)
    - Test: AuditLogger.logApproval creates entry with command, decision (approved/rejected), approval_type
    - Test: AuditLogger.logStateDiff captures before and after JSON snapshots as diff_before and diff_after
    - Test: Each log method writes to both file (via store.appendAudit) and includes timestamp
    - Test: Audit entries are valid JSON when read back from audit.jsonl
    - Test: logStateDiff with identical before/after still logs but marks diff as "no_change"
  </behavior>
  <action>
    1. Create src/audit/types.ts:
       - Export AuditEventType enum: 'decision' | 'command_validation' | 'approval' | 'state_change' | 'error' | 'session_start' | 'session_end'
       - Export AuditEntry interface: { timestamp: string, sessionId: string, eventType: AuditEventType, riskLevel?: string, command?: string, decision?: string, reasoning?: string, optionsConsidered?: string[], diffBefore?: string, diffAfter?: string, metadata?: Record<string, unknown> }
       - Export StateDiff interface: { before: unknown, after: unknown, changed: boolean }

    2. Create src/audit/logger.ts:
       - Export class AuditLogger with constructor(store: WriteThrough, sessionId: string, sessionDir: string)
       - logDecision(reasoning: string, optionsConsidered: string[], chosen: string): void -- builds AuditEntry with eventType 'decision', calls store.appendAudit
       - logCommandValidation(command: string, riskLevel: string, allowed: boolean, reason?: string): void -- eventType 'command_validation'
       - logApproval(command: string, approved: boolean, approvalType: 'auto' | 'y_n' | 'typed_confirmation'): void -- eventType 'approval'
       - logStateDiff(before: unknown, after: unknown): void -- eventType 'state_change', serializes before/after to JSON strings, sets changed flag by deep comparison (JSON.stringify comparison is sufficient for Phase 1)
       - logError(error: string, context?: Record<string, unknown>): void -- eventType 'error'
       - All methods create timestamp via new Date().toISOString()

    Write tests FIRST. Mock WriteThrough.appendAudit to capture entries and verify structure.
  </action>
  <verify>
    <automated>cd /Users/nikolasretkovic/Desktop/InfraBrain && npx vitest run tests/audit/logger.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - AuditLogger writes structured JSON entries for decisions, validations, approvals, state changes
    - State diffs capture before/after snapshots as JSON
    - All audit entries include timestamp, sessionId, eventType
    - All audit logger tests pass
  </done>
</task>

</tasks>

<verification>
cd /Users/nikolasretkovic/Desktop/InfraBrain && npx vitest run tests/state/ tests/audit/ --reporter=verbose
All state and audit tests pass. SQLite schema is correct. Dual-write works.
</verification>

<success_criteria>
- State persists to both .infrabrain/sessions/{id}/state.json and SQLite (INTF-06)
- Every decision logged as structured JSON with reasoning (SAFE-09)
- Before/after state diffs captured for every change (SAFE-10)
- Files are source of truth over SQLite
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-safety-gates/01-02-SUMMARY.md`
</output>
