---
phase: 01-foundation-and-safety-gates
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - src/api/server.ts
  - src/api/routes/debug.ts
  - src/api/routes/health.ts
  - src/cli/repl.ts
  - src/cli/commands.ts
  - src/cli/formatter.ts
  - src/index.ts
  - tests/api/routes.test.ts
  - tests/cli/commands.test.ts
autonomous: false
requirements:
  - INTF-01
  - INTF-05

must_haves:
  truths:
    - "Admin can start InfraBrain and it connects to a running Ollama instance"
    - "Admin can issue /infra:debug command and see it accepted and routed through the system"
    - "REST API serves /debug and /health endpoints"
    - "CLI calls the API internally -- API is the single execution path"
    - "REPL feels like a dedicated tool (psql-style), not a chatbot"
  artifacts:
    - path: "src/api/server.ts"
      provides: "Express 5 server setup with middleware"
      exports: ["createServer"]
    - path: "src/api/routes/debug.ts"
      provides: "POST /debug endpoint"
      exports: ["debugRoute"]
    - path: "src/api/routes/health.ts"
      provides: "GET /health endpoint with Ollama connectivity check"
      exports: ["healthRoute"]
    - path: "src/cli/repl.ts"
      provides: "Interactive REPL loop"
      exports: ["startRepl"]
    - path: "src/cli/commands.ts"
      provides: "Commander.js command definitions"
      exports: ["program", "registerCommands"]
    - path: "src/index.ts"
      provides: "Application entry point wiring everything together"
  key_links:
    - from: "src/cli/commands.ts"
      to: "src/api/server.ts"
      via: "CLI commands call REST API endpoints internally"
      pattern: "fetch.*localhost.*debug|fetch.*localhost.*health"
    - from: "src/cli/repl.ts"
      to: "src/cli/commands.ts"
      via: "REPL dispatches input to Commander commands"
      pattern: "program\\.parse|registerCommands"
    - from: "src/api/routes/debug.ts"
      to: "src/llm/provider.ts"
      via: "debug route calls LLM provider for diagnosis"
      pattern: "provider\\.streamDiagnosis|provider\\.generateCommand"
    - from: "src/api/routes/debug.ts"
      to: "src/safety/validator.ts"
      via: "validates any generated commands before returning"
      pattern: "validateCommand"
    - from: "src/api/routes/health.ts"
      to: "Ollama /api/tags"
      via: "health check verifies Ollama connectivity"
      pattern: "fetch.*api/tags|ollamaBaseUrl"
    - from: "src/index.ts"
      to: "src/api/server.ts"
      via: "starts Express server"
      pattern: "createServer"
    - from: "src/index.ts"
      to: "src/cli/repl.ts"
      via: "starts REPL after server is ready"
      pattern: "startRepl"
---

<objective>
Express REST API, CLI REPL, and full application wiring.

Purpose: Wire all foundation components into a working application. The admin starts InfraBrain, sees a REPL prompt, issues `/infra:debug "test"`, and the command flows through CLI -> API -> LLM provider -> safety validation -> audit log. This is the final integration that proves Phase 1's success criteria.

Output: Working CLI application with REPL, Express API (/debug, /health), and complete command flow from user input to safety-gated LLM response with audit trail.
</objective>

<execution_context>
@/Users/nikolasretkovic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nikolasretkovic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-safety-gates/01-CONTEXT.md
@.planning/phases/01-foundation-and-safety-gates/01-RESEARCH.md
@.planning/phases/01-foundation-and-safety-gates/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-safety-gates/01-02-SUMMARY.md
@.planning/phases/01-foundation-and-safety-gates/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Express API server with /debug and /health routes</name>
  <files>src/api/server.ts, src/api/routes/debug.ts, src/api/routes/health.ts, tests/api/routes.test.ts</files>
  <behavior>
    - Test: GET /health returns 200 with { status: "ok", ollama: "connected"|"disconnected" }
    - Test: GET /health returns ollama: "disconnected" when Ollama is not reachable (mock fetch)
    - Test: POST /debug with { prompt: "test" } returns 200 with response body
    - Test: POST /debug without prompt returns 400 with { error: "prompt is required" }
    - Test: POST /debug validates any commands in LLM response through safety validator
    - Test: Express error handler catches async errors and returns 500 with { error: message }
  </behavior>
  <action>
    1. Create src/api/routes/health.ts:
       - Export healthRoute as Express Router
       - GET / handler: fetch Ollama /api/tags endpoint (from config.ollamaBaseUrl). If success, return { status: "ok", ollama: "connected", models: [...] }. If ECONNREFUSED, return { status: "ok", ollama: "disconnected", error: "Ollama not detected at {url}. Run `ollama serve` first." }

    2. Create src/api/routes/debug.ts:
       - Export debugRoute as Express Router
       - Accept dependencies via factory pattern: createDebugRoute(provider: LLMProvider, auditLogger: AuditLogger, validator: typeof validateCommand)
       - POST / handler: validate request body has prompt (string), call provider.streamDiagnosis or provider.generateCommand with the prompt, validate any extracted commands via safety validator, log decisions to audit, return { sessionId, diagnosis, commands: [{ command, riskLevel, allowed }] }
       - For Phase 1: use generateCommand (non-streaming) for API responses. Streaming is for REPL display only.

    3. Create src/api/server.ts:
       - Export createServer(deps: { provider, store, auditLogger, config }): { app, server }
       - Use Express 5 (import express from 'express')
       - app.use(express.json())
       - Mount /health and /debug routes
       - Add async error handler middleware (Express 5 catches async throws natively)
       - Return both app (for testing) and server (for shutdown)

    Write tests FIRST. Use supertest (install as dev dep: `npm install -D supertest @types/supertest`) to test routes. Mock LLM provider and audit logger.
  </action>
  <verify>
    <automated>cd /Users/nikolasretkovic/Desktop/InfraBrain && npx vitest run tests/api/routes.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - GET /health returns Ollama connectivity status (INTF-05)
    - POST /debug accepts prompt, returns diagnosis with safety-validated commands (INTF-05)
    - Missing prompt returns 400
    - Server errors return 500 with message
    - All API route tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: CLI REPL, command routing, and application entry point</name>
  <files>src/cli/repl.ts, src/cli/commands.ts, src/cli/formatter.ts, src/index.ts, tests/cli/commands.test.ts</files>
  <action>
    1. Create src/cli/formatter.ts:
       - Export formatDiagnosis(text: string): string -- format LLM diagnostic output with chalk styling
       - Export formatCommand(command: string, riskLevel: RiskLevel, allowed: boolean): string -- color-coded command display (green=read, yellow=write, red=destructive, gray+strikethrough=blocked)
       - Export formatApprovalResult(result: ApprovalResult): string -- human-readable approval outcome
       - Export formatError(error: string): string -- chalk.red formatted error
       - Export REPL_PROMPT: string -- "infrabrain> " with chalk styling (tool-like, not chatbot)

    2. Create src/cli/commands.ts:
       - Import { Command } from 'commander'
       - Export registerCommands(config: { apiBaseUrl: string }): Command
       - Register `/infra:debug <prompt>` command: sends POST to internal API /debug endpoint, displays formatted response
       - Register `/infra:health` command: sends GET to internal API /health, displays status
       - All commands call the REST API internally (per user decision: API is the single execution path)
       - Commands use fetch() to call localhost API

    3. Create src/cli/repl.ts:
       - Import readline from 'node:readline/promises'
       - Export startRepl(config: { apiBaseUrl: string, program: Command }): Promise<void>
       - Create readline interface with input: process.stdin, output: process.stdout
       - Display welcome banner: InfraBrain version, Ollama status (call /health on startup)
       - Print clear error if Ollama not running: "Ollama not detected at {url}. Run `ollama serve` first."
       - Prompt loop with REPL_PROMPT
       - If input starts with /infra: -- parse as Commander command
       - If input is "exit" or "quit" -- close gracefully
       - Handle Ctrl+C gracefully (close readline, shutdown server)
       - Use rl.pause() before LLM output streaming and rl.resume() after (per pitfall #5)

    4. Update src/index.ts:
       - Parse CLI args: if args present, run one-shot mode (Commander parse + exit). If no args, start REPL mode.
       - Initialize: loadConfig -> initDatabase -> createOllamaModel -> createProvider -> createSession -> WriteThrough -> AuditLogger -> createServer -> startRepl (or one-shot)
       - Wire all dependencies together
       - Handle startup errors gracefully (Ollama not running, DB permission issues)

    Write tests for command parsing (unit test Commander commands with mock fetch). REPL is manual-only verification.
  </action>
  <verify>
    <automated>cd /Users/nikolasretkovic/Desktop/InfraBrain && npx vitest run tests/cli/commands.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - REPL starts with welcome banner and Ollama status check
    - /infra:debug "test" routes through API and returns formatted response
    - /infra:health shows Ollama connectivity
    - CLI calls API internally (not directly calling LLM provider)
    - One-shot mode works for scripting
    - Entry point wires all components together
    - All CLI command tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 1 end-to-end flow</name>
  <files>none</files>
  <action>Human verifies the complete integrated application works end-to-end with live Ollama.</action>
  <what-built>
    Complete Phase 1 application: InfraBrain CLI with REPL, Express API, LLM provider (Ollama), command validation with risk-tiered approval gates, and audit logging.
  </what-built>
  <how-to-verify>
    Prerequisites: Ollama must be running (`ollama serve`) with llama3.3:70b pulled (or adjust model name in config).

    1. Start InfraBrain: `npm run dev`
       - Verify: Welcome banner appears, Ollama status shows "connected"
       - Verify: REPL prompt "infrabrain>" appears (tool-like, not chatbot)

    2. Test health: type `/infra:health`
       - Verify: Shows Ollama connected status with model list

    3. Test debug command: type `/infra:debug "What processes are using port 80?"`
       - Verify: LLM diagnostic reasoning appears
       - Verify: Any suggested commands show risk level classification
       - Verify: Commands go through approval gate (read auto-approved, write asks Y/n)

    4. Check audit trail: inspect `.infrabrain/sessions/` directory
       - Verify: Session directory exists with state.json and audit.jsonl
       - Verify: audit.jsonl contains structured JSON entries for the interaction

    5. Test safety gate: If LLM suggests a destructive command, verify typed confirmation is required

    6. Test API directly: `curl http://localhost:3000/health`
       - Verify: Returns JSON with ollama status
  </how-to-verify>
  <verify>Manual verification by human</verify>
  <done>Human confirms end-to-end flow works: REPL -> API -> LLM -> safety gates -> audit trail</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
cd /Users/nikolasretkovic/Desktop/InfraBrain && npx vitest run --reporter=verbose
Full test suite passes. Application starts with `npm run dev` and responds to commands.
</verification>

<success_criteria>
- Admin can start InfraBrain and see it connect to Ollama
- /infra:debug command routes through API -> LLM -> safety validation -> audit log
- REST API serves /debug and /health endpoints (INTF-05)
- CLI is the primary interface, API is the execution path (INTF-01)
- Human verifies the complete flow works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-safety-gates/01-04-SUMMARY.md`
</output>
