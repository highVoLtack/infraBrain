---
phase: 12.2-skill-driven-discovery
plan: 01
subsystem: engine
tags: [zod, yaml, discovery, skills, frontmatter]

requires:
  - phase: 12.1-dynamic-command-rewriter
    provides: RewriteRuleSchema pattern for skill frontmatter extensions
provides:
  - DiscoveryCommandSchema for validating command+label pairs
  - discovery field on SkillFrontmatterSchema with default []
  - 16 discovery commands declared in 4 skill YAML files
affects: [12.2-02, debug.ts discovery consumer]

tech-stack:
  added: []
  patterns: [skill-declared discovery commands in YAML frontmatter]

key-files:
  created: []
  modified:
    - src/skills/types.ts
    - skills/nginx-troubleshoot.md
    - skills/postgres-troubleshoot.md
    - skills/docker-storage.md
    - skills/linux-filesystem-troubleshoot.md
    - tests/skills/types.test.ts

key-decisions:
  - "YAML single-quoted strings for Go template syntax and embedded quotes"
  - "discovery field placed after rewrite_rules for consistency"
  - "Doubled single quotes (''idle'') for YAML escaping of embedded SQL quotes"

patterns-established:
  - "Discovery commands declared in skill YAML frontmatter, not hardcoded in TypeScript"

requirements-completed: [ENGN-04]

duration: 2min
completed: 2026-03-14
---

# Phase 12.2 Plan 01: Discovery Schema and YAML Migration Summary

**DiscoveryCommandSchema added to SkillFrontmatterSchema with Zod validation, 16 discovery commands migrated from hardcoded debug.ts into 4 skill YAML files**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-14T11:21:34Z
- **Completed:** 2026-03-14T11:23:44Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- DiscoveryCommandSchema validates command+label pairs via Zod with min-length constraints
- discovery field on SkillFrontmatterSchema defaults to [] for backwards compatibility
- All 16 discovery commands from DISCOVERY_COMMANDS constant migrated into skill YAML
- YAML quoting correctly handles Go template syntax ({{.Names}}) and embedded SQL quotes ('idle')

## Task Commits

Each task was committed atomically:

1. **Task 1: Add DiscoveryCommandSchema to types.ts with unit tests** - `8147033` (feat, TDD)
2. **Task 2: Migrate discovery commands into all 4 skill YAML files** - `eba2a6e` (feat)

## Files Created/Modified
- `src/skills/types.ts` - Added DiscoveryCommandSchema, DiscoveryCommand type, discovery field on SkillFrontmatterSchema
- `tests/skills/types.test.ts` - 5 new tests for discovery schema validation and defaults
- `skills/nginx-troubleshoot.md` - 2 discovery commands (containers, networks)
- `skills/postgres-troubleshoot.md` - 5 discovery commands (containers, IPs, connections, limits, idle detail)
- `skills/docker-storage.md` - 5 discovery commands (containers, IPs, capacity, ownership, system df)
- `skills/linux-filesystem-troubleshoot.md` - 4 discovery commands (status, logs, permissions, identity)

## Decisions Made
- Used YAML single-quoted strings for all discovery commands to handle Go template syntax without escaping
- Placed discovery field after rewrite_rules in SkillFrontmatterSchema for consistency with Phase 12.1 pattern
- Used doubled single quotes (''idle'') in YAML for the postgres idle connections command -- verified round-trip produces correct SQL

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All 16 discovery commands are now in skill YAML, ready for Plan 02 to wire debug.ts to read from skills
- DiscoveryCommandSchema provides typed validation for the consumer side
- Skills without discovery (planning, verification, log-analysis) load with empty array -- no breakage

---
*Phase: 12.2-skill-driven-discovery*
*Completed: 2026-03-14*
