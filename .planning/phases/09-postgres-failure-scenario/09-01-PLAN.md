---
phase: 09-postgres-failure-scenario
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - demo/nginx/docker-compose.yml
  - demo/nginx/nginx.conf
  - demo/nginx/reset.sh
  - demo/postgres/docker-compose.yml
  - demo/postgres/leaky-app/Dockerfile
  - demo/postgres/leaky-app/leak.py
  - demo/postgres/leaky-app/init.sql
  - demo/postgres/reset-postgres.sh
  - tests/e2e/poc-nginx-502.test.ts
autonomous: true
requirements:
  - SCEN-01
  - SCEN-03

must_haves:
  truths:
    - "Running docker compose up in demo/postgres/ starts Postgres and leaky-app containers"
    - "Leaky-app opens 18 connections saturating Postgres max_connections=20"
    - "Running reset-postgres.sh idempotently restores broken state (confirms 'too many connections')"
    - "Existing nginx E2E test still passes after demo directory restructure"
  artifacts:
    - path: "demo/postgres/docker-compose.yml"
      provides: "Postgres + leaky-app compose environment"
      contains: "max_connections=20"
    - path: "demo/postgres/leaky-app/leak.py"
      provides: "Python connection leak simulator"
      contains: "psycopg2"
    - path: "demo/postgres/leaky-app/Dockerfile"
      provides: "Leaky-app container image"
      contains: "psycopg2-binary"
    - path: "demo/postgres/leaky-app/init.sql"
      provides: "Non-superuser leaky user creation"
      contains: "CREATE USER leaky"
    - path: "demo/postgres/reset-postgres.sh"
      provides: "Idempotent broken state reset"
      contains: "too many"
    - path: "demo/nginx/docker-compose.yml"
      provides: "Restructured nginx demo"
    - path: "demo/nginx/nginx.conf"
      provides: "Restructured nginx config"
    - path: "demo/nginx/reset.sh"
      provides: "Restructured nginx reset script"
  key_links:
    - from: "demo/postgres/docker-compose.yml"
      to: "demo/postgres/leaky-app/Dockerfile"
      via: "build context"
      pattern: "build:.*leaky-app"
    - from: "demo/postgres/docker-compose.yml"
      to: "demo/postgres/leaky-app/init.sql"
      via: "volume mount to docker-entrypoint-initdb.d"
      pattern: "init\\.sql:/docker-entrypoint-initdb\\.d"
    - from: "demo/postgres/reset-postgres.sh"
      to: "demo/postgres/docker-compose.yml"
      via: "docker compose -f"
      pattern: "docker compose.*docker-compose\\.yml"
    - from: "tests/e2e/poc-nginx-502.test.ts"
      to: "demo/nginx/"
      via: "updated path references"
      pattern: "demo/nginx"
---

<objective>
Create the Postgres failure scenario Docker environment and restructure demo directory into per-scenario subdirectories.

Purpose: Establishes the "broken infrastructure" that the diagnostic skill will analyze. Also restructures demo/ into the Chaos Library pattern (demo/<scenario>/) so future scenarios follow the same layout.

Output: Working Docker Compose environment where Postgres is saturated with leaked connections, an idempotent reset script, and restructured nginx demo with passing tests.
</objective>

<execution_context>
@/Users/nikolasretkovic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nikolasretkovic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-postgres-failure-scenario/09-CONTEXT.md
@.planning/phases/09-postgres-failure-scenario/09-RESEARCH.md

@demo/docker-compose.yml
@demo/nginx.conf
@demo/reset.sh
@tests/e2e/poc-nginx-502.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure demo directory into per-scenario subdirectories</name>
  <files>demo/nginx/docker-compose.yml, demo/nginx/nginx.conf, demo/nginx/reset.sh, tests/e2e/poc-nginx-502.test.ts</files>
  <action>
Move existing nginx demo files into `demo/nginx/` subdirectory:
1. Create `demo/nginx/` directory
2. Move `demo/docker-compose.yml` -> `demo/nginx/docker-compose.yml`
3. Move `demo/nginx.conf` -> `demo/nginx/nginx.conf`
4. Move `demo/reset.sh` -> `demo/nginx/reset.sh`
5. Update `tests/e2e/poc-nginx-502.test.ts` to reference new paths:
   - Line 84: `demo/reset.sh` -> `demo/nginx/reset.sh`
   - Line 182: `demo/docker-compose.yml` -> `demo/nginx/docker-compose.yml`
   - Any other path references to demo/ root files
6. Leave `demo/start-api.ts` at the demo/ root (shared utility, not scenario-specific)
7. Verify no other files reference the old paths (grep for `demo/docker-compose.yml`, `demo/reset.sh`, `demo/nginx.conf` across the codebase)
  </action>
  <verify>
    <automated>npx vitest run tests/e2e/poc-nginx-502.test.ts --reporter=verbose 2>&1 | head -5 || echo "Test file compiles (Docker may not be running for full E2E)"</automated>
  </verify>
  <done>Nginx demo files live under demo/nginx/, poc-nginx-502.test.ts references updated paths, no stale references to old locations remain in codebase</done>
</task>

<task type="auto">
  <name>Task 2: Create Postgres Docker environment and reset script</name>
  <files>demo/postgres/docker-compose.yml, demo/postgres/leaky-app/Dockerfile, demo/postgres/leaky-app/leak.py, demo/postgres/leaky-app/init.sql, demo/postgres/reset-postgres.sh</files>
  <action>
Create `demo/postgres/` directory with the complete broken Postgres environment:

**docker-compose.yml** (per CONTEXT.md locked decisions):
- `postgres:16` image, container_name `postgres-demo`, host port `5433:5432`
- Command: `postgres -c max_connections=20 -c superuser_reserved_connections=2` (18 regular + 2 superuser slots)
- Environment: POSTGRES_USER=postgres, POSTGRES_PASSWORD=postgres, POSTGRES_DB=postgres
- Mount `./leaky-app/init.sql` to `/docker-entrypoint-initdb.d/init.sql` for leaky user creation
- Network: `pgnet` (bridge driver)
- `leaky-app` service: build from `./leaky-app`, container_name `leaky-app`, depends_on postgres, same `pgnet` network

**leaky-app/init.sql:**
- `CREATE USER leaky WITH PASSWORD 'leaky'; GRANT CONNECT ON DATABASE postgres TO leaky;`

**leaky-app/Dockerfile:**
- FROM python:3-slim
- RUN pip install psycopg2-binary
- COPY leak.py /app/leak.py
- CMD ["python", "/app/leak.py"]

**leaky-app/leak.py** (per CONTEXT.md):
- Connect to `host=postgres dbname=postgres user=leaky password=leaky`
- Open 18 connections in a loop with 2s backoff retry on OperationalError
- Print clear messages: "Connection N/18 established" and "Holding 18 connections open..."
- Hold connections forever with `while True: time.sleep(60)`
- Use `conn.autocommit = True` for each connection

**reset-postgres.sh** (per CONTEXT.md/RESEARCH.md):
- `#!/usr/bin/env bash` with `set -euo pipefail`
- Resolve SCRIPT_DIR and COMPOSE_FILE
- `docker compose -f "$COMPOSE_FILE" down --remove-orphans 2>/dev/null || true`
- `docker compose -f "$COMPOSE_FILE" up -d --build`
- Poll loop (max 30 iterations, 2s sleep = 60s timeout): try `docker exec postgres-demo psql -U leaky -d postgres -c "SELECT 1"` and check stderr for "too many" -- break on match
- Exit 0 on success with message "SUCCESS: Postgres connection limit reached"
- Exit 1 on timeout with message "ERROR: Broken state not reached within 60 seconds"
- Make executable: `chmod +x demo/postgres/reset-postgres.sh`
  </action>
  <verify>
    <automated>test -f demo/postgres/docker-compose.yml && test -f demo/postgres/leaky-app/leak.py && test -f demo/postgres/leaky-app/Dockerfile && test -f demo/postgres/leaky-app/init.sql && test -x demo/postgres/reset-postgres.sh && echo "All Postgres demo files exist and reset script is executable"</automated>
  </verify>
  <done>demo/postgres/ contains complete Docker environment: compose file with postgres:16 (max_connections=20, superuser_reserved_connections=2) + leaky-app (18 connections), init.sql creating leaky user, and executable reset-postgres.sh with 60s polling timeout</done>
</task>

</tasks>

<verification>
- All nginx demo files moved to demo/nginx/ with no stale references
- demo/postgres/ contains docker-compose.yml, leaky-app/{Dockerfile,leak.py,init.sql}, reset-postgres.sh
- reset-postgres.sh is executable and uses correct compose file path
- Postgres compose uses max_connections=20, superuser_reserved_connections=2, host port 5433
- Leaky-app opens 18 connections as non-superuser (leaky) with 2s backoff retry
</verification>

<success_criteria>
- demo/ follows Chaos Library pattern: demo/nginx/ and demo/postgres/ as separate scenario directories
- No broken references to old demo/ file locations
- Postgres Docker environment is fully defined and ready for manual or automated testing
- reset-postgres.sh can be run to create/recreate the broken state
</success_criteria>

<output>
After completion, create `.planning/phases/09-postgres-failure-scenario/09-01-SUMMARY.md`
</output>
