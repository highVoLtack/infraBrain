---
phase: 06-resume-wiring-and-audit-completeness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/execution/executor.ts
  - tests/execution/executor.test.ts
autonomous: true
requirements:
  - SAFE-09

must_haves:
  truths:
    - "Lock acquire emits lock_acquired audit event with target"
    - "Lock release emits lock_released audit event with target"
    - "Lock conflict returns rejected and emits lock_conflict audit event"
    - "Lock force-override emits lock_override audit event"
    - "Audit events only emitted after successful lock operations (not before)"
  artifacts:
    - path: "src/execution/executor.ts"
      provides: "Lock audit event emission in executor"
      contains: "lock_acquired"
    - path: "tests/execution/executor.test.ts"
      provides: "Tests for lock audit events"
      contains: "lock_acquired"
  key_links:
    - from: "src/execution/executor.ts"
      to: "deps.auditLogger.logExecution"
      via: "lock event calls"
      pattern: "logExecution\\('lock_(acquired|released|conflict|override)'"
---

<objective>
Add audit event emission for lock operations in the executor.

Purpose: Lock acquire, release, conflict, and override operations are currently silent in the audit trail. The event types and display formatters already exist (`lock_acquired`, `lock_released`, `lock_conflict`, `lock_override` in audit/types.ts, display logic in formatter.ts). The executor just needs to call `deps.auditLogger.logExecution()` at the right points.

Output: Executor emits 4 lock-related audit events; all existing + new tests pass.
</objective>

<execution_context>
@/Users/nikolasretkovic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nikolasretkovic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-resume-wiring-and-audit-completeness/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/audit/types.ts:
```typescript
export type AuditEventType =
  | 'lock_acquired'
  | 'lock_released'
  | 'lock_conflict'
  | 'lock_override'
  // ... other types
```

From src/execution/types.ts:
```typescript
export interface ExecutionDeps {
  auditLogger: {
    logExecution: (eventType: AuditEventType, details: Record<string, unknown>) => void;
  };
  // ... other fields
}
```

From src/execution/executor.ts (existing pattern):
```typescript
deps.auditLogger.logExecution('circuit_breaker_triggered', {
  stepIndex: i,
  command: step.command,
  maxRetries: deps.config.circuitBreaker.maxRetries,
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add lock audit event tests</name>
  <files>tests/execution/executor.test.ts</files>
  <behavior>
    - Test: lock_acquired event emitted after successful lock acquisition (logExecution called with 'lock_acquired' and { target })
    - Test: lock_released event emitted after lock release in finally block (logExecution called with 'lock_released' and { target })
    - Test: lock_conflict event emitted when lock is held and no readline for override (logExecution called with 'lock_conflict')
    - Test: lock_override event emitted when force-override succeeds (logExecution called with 'lock_override')
    - Test: lock_acquired NOT emitted when lock acquisition fails (rejected path)
  </behavior>
  <action>
    Add new test cases to the existing executor test file. Use the existing mock pattern for deps (the test file already mocks auditLogger.logExecution as vi.fn()). For each test:
    1. Set up lock state (acquireLock mock returns 'acquired' or 'locked')
    2. Call executePlan
    3. Assert deps.auditLogger.logExecution was called with the expected event type and details

    For lock_conflict test: mock acquireLock to return { status: 'locked', existing: {...} } and do NOT provide readline on deps (so override prompt is skipped, returns rejected).

    For lock_override test: mock acquireLock to return { status: 'locked', existing: {...} }, provide readline mock that answers 'yes' to promptLockOverride, then assert lock_override event emitted.
  </action>
  <verify>
    <automated>cd /Users/nikolasretkovic/Desktop/InfraBrain && npx vitest run tests/execution/executor.test.ts -x 2>&1 | tail -20</automated>
  </verify>
  <done>New lock audit event tests exist and FAIL (red phase) because executor.ts does not yet emit these events</done>
</task>

<task type="auto">
  <name>Task 2: Emit lock audit events in executor</name>
  <files>src/execution/executor.ts</files>
  <action>
    Add 4 audit event emissions to executor.ts at the correct locations. Follow the research guidance: emit AFTER successful operations, not before.

    1. After `lockAcquired = true` (line 61): Add `deps.auditLogger.logExecution('lock_acquired', { target });`

    2. In the lock conflict rejection paths (line 46 return and line 57 return): Add `deps.auditLogger.logExecution('lock_conflict', { target, existingSession: lockResult.existing?.sessionId });` before each return.

    3. After the force-override re-acquire succeeds (after line 55): Add `deps.auditLogger.logExecution('lock_override', { target, overriddenSession: lockResult.existing?.sessionId });`

    4. In the finally block, after `releaseLock(target, lockDir)` (line 229): Add `deps.auditLogger.logExecution('lock_released', { target });`

    Do NOT change the lock manager itself. Audit is a cross-cutting concern handled at the executor level per research guidance.
  </action>
  <verify>
    <automated>cd /Users/nikolasretkovic/Desktop/InfraBrain && npx vitest run tests/execution/executor.test.ts -x 2>&1 | tail -20</automated>
  </verify>
  <done>All executor tests pass including the new lock audit event tests. lock_acquired, lock_released, lock_conflict, and lock_override events are emitted at correct points.</done>
</task>

</tasks>

<verification>
Run full test suite to confirm no regressions:
```bash
npx vitest run
```
All 322+ tests pass.
</verification>

<success_criteria>
- lock_acquired emitted after successful lock acquisition
- lock_released emitted after lock release in finally block
- lock_conflict emitted on rejected lock
- lock_override emitted on force-override
- All existing tests continue to pass (no regressions)
- New tests verify each lock audit event
</success_criteria>

<output>
After completion, create `.planning/phases/06-resume-wiring-and-audit-completeness/06-01-SUMMARY.md`
</output>
