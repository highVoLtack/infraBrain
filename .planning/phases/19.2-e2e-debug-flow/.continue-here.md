---
phase: 19.2-e2e-debug-flow
task: 0
total_tasks: 0
status: planning
last_updated: "2026-04-16T16:56:35.112Z"
---

<current_state>
Phase 19.2 registered in ROADMAP + REQUIREMENTS with 6 requirements (TERM-E01 through TERM-E06).
Phase directory created but no plans yet. Next: /gsd:plan-phase 19.2
</current_state>

<completed_work>

- Phase 19 (Ink Terminal UI): 6/6 plans, verified 8/8
- Phase 19.1 (Ink UI Polish): 2/2 plans, verified 7/7
- Gemini Cloud integration: fully agnostic provider routing with apiKey, .env support
- Lazy LanceDB table creation: no more hardcoded vector dimensions
- Auto-port fallback: EADDRINUSE -> free port
- MODEL-ROUTING.md: 7 provider recipes documented
- Live UAT with nginx 502 demo: Discovery + Diagnosis + Plan works end-to-end via Ink UI
- Phase 19.2 registered with 6 requirements
</completed_work>

<remaining_work>

- Plan Phase 19.2 (/gsd:plan-phase 19.2)
- Execute Phase 19.2
- Verify Phase 19.2
</remaining_work>

<decisions_made>

- Phase 19.2 scope covers the full DPEV+E+V flow in Ink UI:
  1. Discovery spinner (TERM-E01) -- blank screen during discovery is bad UX
  2. Structured plan approval (TERM-E02) -- Y/N with risk badges, not raw Markdown
  3. Execution step streaming (TERM-E03) -- SSE for each step with live stdout/stderr
  4. Verification display (TERM-E04) -- show if fix actually worked
  5. Plan rendering (TERM-E05) -- numbered steps with risk badges, not **Markdown**
  6. Session outcome (TERM-E06) -- "completed" only when fix verified, not after diagnosis
- Additional bugs found during UAT to fix in 19.2:
  - Replay timer keeps counting (should show stored duration, not live timer)
  - Duplicate DIAGNOSIS phase in replay (structured fail -> free-text shows as two phases)
- Gemini paid API key available (no more 503 rate limiting)
</decisions_made>

<blockers>
- None. All infrastructure in place.
</blockers>

<context>
The SSE streaming pipeline (/stream/debug) runs the full DPEV pipeline but only emits
Discovery + Diagnosis + Plan events. It does NOT trigger execution (POST /execute) or
verification afterward. The Ink UI shows "Session complete" after the plan phase, but
the nginx is still returning 502.

The /stream/execute SSE endpoint already exists (built in Phase 19 Plan 02) with step
streaming and approval flows. It just needs to be wired into the Ink UI flow after plan
approval.

Key files:
- src/api/routes/stream-debug.ts -- emits DPEV events, needs to chain to execution
- src/api/routes/stream-execute.ts -- already has step streaming + approval
- src/ui/panels/DPEVPanel.tsx -- needs plan rendering + approval UI + execution display
- src/ui/components/StepCard.tsx -- already built, needs wiring
- src/ui/components/ApprovalWrite.tsx -- already built, needs wiring
- src/ui/components/DPEVPhaseHeader.tsx -- timer bug in replay mode
</context>

<next_action>
Start with: /gsd:plan-phase 19.2
</next_action>
