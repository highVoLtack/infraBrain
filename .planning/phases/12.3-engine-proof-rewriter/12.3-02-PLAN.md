---
phase: 12.3-engine-proof-rewriter
plan: 02
type: execute
wave: 2
depends_on:
  - "12.3-01"
files_modified:
  - skills/linux-filesystem-troubleshoot.md
  - skills/docker-storage.md
  - tests/e2e/poc-permission-trap.test.ts
  - tests/e2e/poc-docker-storage.test.ts
autonomous: true
requirements:
  - ENGN-05

must_haves:
  truths:
    - "linux-filesystem-troubleshoot.md contains COMMAND-ONLY instructions and zero docker exec in examples"
    - "docker-storage.md contains COMMAND-ONLY instructions and zero docker exec in examples"
    - "postgres-troubleshoot.md is unchanged (already SQL-ONLY)"
    - "nginx-troubleshoot.md is unchanged (no privilege escalation needed)"
    - "All E2E tests pass with the new rewriter behavior"
    - "Canned fix plans in E2E tests work correctly with docker-exec-aware rewriter"
  artifacts:
    - path: "skills/linux-filesystem-troubleshoot.md"
      provides: "COMMAND-ONLY mode skill prompt"
      contains: "COMMAND-ONLY"
    - path: "skills/docker-storage.md"
      provides: "COMMAND-ONLY mode skill prompt"
      contains: "COMMAND-ONLY"
  key_links:
    - from: "skills/linux-filesystem-troubleshoot.md"
      to: "src/execution/dynamic-rewriter.ts"
      via: "rewrite_rules frontmatter consumed by dynamicRewrite()"
      pattern: "rewrite_rules"
    - from: "tests/e2e/poc-permission-trap.test.ts"
      to: "src/execution/dynamic-rewriter.ts"
      via: "canned fix plan commands processed through dynamicRewrite()"
      pattern: "cannedFixPlan"
---

<objective>
Refactor skill prompts to COMMAND-ONLY mode and ensure E2E tests pass with the new docker-exec-aware rewriter from Plan 01.

Purpose: Skills should instruct the LLM to write bare commands (like postgres uses SQL-ONLY). The rewriter handles container targeting and privilege escalation. E2E canned fix plans that already contain `docker exec` must work correctly through the new parser (no double-wrapping, correct flag injection).

Output: Updated skill files with COMMAND-ONLY instructions, E2E tests verified green.
</objective>

<execution_context>
@/Users/nikolasretkovic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nikolasretkovic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.3-engine-proof-rewriter/12.3-RESEARCH.md
@.planning/phases/12.3-engine-proof-rewriter/12.3-01-SUMMARY.md

@skills/linux-filesystem-troubleshoot.md
@skills/docker-storage.md
@skills/postgres-troubleshoot.md
@tests/e2e/poc-permission-trap.test.ts
@tests/e2e/poc-docker-storage.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add COMMAND-ONLY mode to linux-filesystem-troubleshoot.md and docker-storage.md</name>
  <files>skills/linux-filesystem-troubleshoot.md, skills/docker-storage.md</files>
  <action>
    **linux-filesystem-troubleshoot.md:**
    1. Add a `## COMMAND-ONLY MODE` section after `## STRICT RULES`, modeled on postgres-troubleshoot.md's `## SQL-ONLY MODE`:
       ```
       ## COMMAND-ONLY MODE

       You write ONLY bare filesystem commands. The execution engine automatically wraps your commands in `docker exec [-u 0] <container> ...`, targeting the container identified from GROUND TRUTH.

       DO NOT write `docker exec`. DO NOT write `docker logs`. DO NOT specify container names in commands. Write ONLY the bare command as if you were logged into the system directly.

       Example of what you output in the `command` field:
       - CORRECT: `chown 1000:1000 /app/data`
       - CORRECT: `ls -ld /app/data`
       - CORRECT: `id`
       - WRONG: `docker exec permission-app chown 1000:1000 /app/data`
       - WRONG: `docker exec -u 0 permission-app chown 1000:1000 /app/data`
       ```
    2. Update the Diagnostic Ladder examples to remove `docker exec` from command examples in Steps 2, 3, and 4. Change Step 2 from `docker exec <container> ls -ld <path>` to just `ls -ld <path>`. Step 3: `id` instead of `docker exec <container> id`. Step 4 fix: `chown 1000:1000 <path>` instead of `docker exec -u 0 <container> chown 1000:1000 <path>`.
    3. Update the Example section at the bottom: remove all `docker exec` from command fields. Keep `docker ps -a`, `docker logs`, and `docker restart` as-is in discovery/restart steps (those are infrastructure commands, not inside-container commands).
    4. Keep the discovery frontmatter as-is (discovery commands like `docker logs permission-app` are run by the engine, not the LLM).

    **docker-storage.md:**
    1. Add the same `## COMMAND-ONLY MODE` section after `## STRICT RULES`:
       ```
       ## COMMAND-ONLY MODE

       You write ONLY bare system commands. The execution engine automatically wraps your commands in `docker exec <container> ...`, targeting the container identified from GROUND TRUTH.

       DO NOT write `docker exec`. DO NOT specify container names in commands. Write ONLY the bare command as if you were logged into the system directly.

       Example of what you output in the `command` field:
       - CORRECT: `truncate -s 0 /shared/bloat.log`
       - CORRECT: `df -h /shared`
       - CORRECT: `du -sh /shared/*`
       - WRONG: `docker exec storage-logger truncate -s 0 /shared/bloat.log`
       - WRONG: `docker exec storage-logger df -h /shared`
       ```
    2. Update Diagnostic Ladder step examples: remove `docker exec <container>` prefixes from commands that run inside containers. Keep `docker ps`, `docker network inspect`, `docker system df`, `docker restart` as-is (infrastructure commands).
    3. Update the Important Rules section: remove references to `docker exec` in command examples.

    **DO NOT modify:** postgres-troubleshoot.md (already SQL-ONLY), nginx-troubleshoot.md (per user decision: keep as-is, no privilege escalation needed).
  </action>
  <verify>
    <automated>grep -c "docker exec" skills/linux-filesystem-troubleshoot.md | xargs -I{} test {} -le 5 && echo "PASS: limited docker exec references" || echo "FAIL"</automated>
  </verify>
  <done>
    - linux-filesystem-troubleshoot.md has COMMAND-ONLY MODE section
    - docker-storage.md has COMMAND-ONLY MODE section
    - Skill examples show bare commands, not docker exec wrapped
    - Discovery frontmatter unchanged
    - Rewrite rules frontmatter unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify E2E tests pass with docker-exec-aware rewriter</name>
  <files>tests/e2e/poc-permission-trap.test.ts, tests/e2e/poc-docker-storage.test.ts</files>
  <action>
    The E2E tests use canned fix plans with pre-built docker exec commands. With the Plan 01 rewriter change, these commands will now be PARSED instead of passed through. Analyze each canned fix plan to determine if the new behavior causes issues:

    **poc-permission-trap.test.ts canned fix plan:**
    - `docker exec permission-app ls -ld /app/data` -- parser extracts inner `ls -ld /app/data`, matches `simpleContainerRule` (^(ls|id|stat|cat|whoami)), no user injection needed, reassembles to same command. SHOULD PASS.
    - `docker exec -u 0 permission-app chown 1000:1000 /app/data` -- parser extracts inner `chown 1000:1000 /app/data`, matches `privilegeRule` (^(chown|chmod)), rule has `user: "0"`, but `-u 0` already in flags -- no double injection. Reassembles to same command. SHOULD PASS.
    - `docker restart permission-app` -- NOT a docker exec command, passes to bare command path. No matching rule, passthrough. SHOULD PASS.
    - `docker logs permission-app --tail 5` -- NOT a docker exec command, passthrough. SHOULD PASS.

    **poc-docker-storage.test.ts canned fix plan:**
    - `docker exec storage-logger du -sh /shared/` -- parser extracts inner `du -sh /shared/`, matches `^(df|du)` rule, no user, reassembles to same. SHOULD PASS.
    - `docker exec storage-logger truncate -s 0 /shared/bloat.log` -- parser extracts inner `truncate -s 0 /shared/bloat.log`, matches `^truncate` rule, no user, reassembles to same. SHOULD PASS.
    - `docker restart storage-redis` -- NOT docker exec, passthrough. SHOULD PASS.
    - `docker exec storage-redis redis-cli PING` -- parser extracts inner `redis-cli PING`. Does docker-storage skill have a rule matching `redis-cli`? No -- only `^truncate` and `^(df|du)`. No rule matches -> passthrough with -it stripped (no -it present). Returns same command. SHOULD PASS.

    **Action:** Run the full E2E test suite. If any test fails due to the rewriter change, update the canned fix plan commands to match the new expected output. Based on analysis above, no changes should be needed -- but verify.

    Run: `npx vitest run tests/e2e/poc-permission-trap.test.ts tests/e2e/poc-docker-storage.test.ts -x`

    If tests fail, diagnose whether it's a rewriter issue or a test expectation issue and fix accordingly. The canned fix plan commands should produce identical output through the new parser for all current cases.

    Also run the full unit test suite to catch any regressions: `npx vitest run -x`
  </action>
  <verify>
    <automated>npx vitest run -x</automated>
  </verify>
  <done>
    - All E2E tests pass (poc-permission-trap, poc-docker-storage, poc-nginx-502)
    - All unit tests pass (477+ existing + new docker-exec tests from Plan 01)
    - Full test suite green
  </done>
</task>

</tasks>

<verification>
npx vitest run -x
</verification>

<success_criteria>
- linux-filesystem-troubleshoot.md and docker-storage.md have COMMAND-ONLY MODE sections
- Skill examples use bare commands, not docker exec wrapped
- postgres-troubleshoot.md and nginx-troubleshoot.md unchanged
- Full test suite passes (unit + E2E)
- No canned fix plan changes needed (or minimal adjustments if the parser produces different whitespace)
</success_criteria>

<output>
After completion, create `.planning/phases/12.3-engine-proof-rewriter/12.3-02-SUMMARY.md`
</output>
