---
phase: 12.3-engine-proof-rewriter
plan: 02
subsystem: execution
tags: [skill-prompts, command-only, docker-exec, e2e-validation]

# Dependency graph
requires:
  - phase: 12.3-engine-proof-rewriter
    plan: 01
    provides: "parseDockerExec() + docker-exec-aware rewrite branch in dynamicRewrite()"
  - phase: 12.1-dynamic-rewriter
    provides: "dynamicRewrite() pure function with RewriteRuleSchema"
provides:
  - "COMMAND-ONLY mode skill prompts for linux-filesystem-troubleshoot and docker-storage"
  - "E2E validation that docker-exec-aware rewriter handles canned fix plans correctly"
affects: [skill-authoring, future-skills]

# Tech tracking
tech-stack:
  added: []
  patterns: ["COMMAND-ONLY mode skill prompt pattern (modeled on SQL-ONLY from postgres skill)"]

key-files:
  created: []
  modified:
    - skills/linux-filesystem-troubleshoot.md
    - skills/docker-storage.md

key-decisions:
  - "Discovery frontmatter commands kept as-is -- they are engine-executed, not LLM-generated"
  - "Infrastructure commands (docker restart, docker logs, docker ps) left in skill text -- they are not inside-container commands"
  - "No E2E test changes needed -- docker exec parser correctly handles existing canned fix plans"

patterns-established:
  - "COMMAND-ONLY mode section placement: after STRICT RULES, before Diagnostic Ladder"
  - "Three-skill-mode taxonomy: SQL-ONLY (postgres), COMMAND-ONLY (linux-fs, docker-storage), as-is (nginx)"

requirements-completed: [ENGN-05]

# Metrics
duration: 4min
completed: 2026-03-14
---

# Phase 12.3 Plan 02: Skill Prompt COMMAND-ONLY Mode Summary

**COMMAND-ONLY mode added to linux-filesystem and docker-storage skills with full E2E validation of docker-exec-aware rewriter**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-14T12:08:19Z
- **Completed:** 2026-03-14T12:12:06Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Added COMMAND-ONLY MODE section to linux-filesystem-troubleshoot.md and docker-storage.md
- Removed docker exec from Diagnostic Ladder examples -- bare commands only (ls, id, chown, df, du, truncate)
- Verified all 8 E2E tests pass (permission-trap + docker-storage) with docker-exec-aware rewriter
- Confirmed no canned fix plan changes needed -- parser correctly handles existing docker exec commands
- Full test suite: 176 unit tests passed, 12 E2E tests passed (2 pre-existing failures unrelated)

## Task Commits

Each task was committed atomically:

1. **Task 1: Add COMMAND-ONLY mode to skills** - `26c0c66` (feat)
2. **Task 2: Verify E2E tests pass** - verification only, no code changes needed

## Files Created/Modified
- `skills/linux-filesystem-troubleshoot.md` - Added COMMAND-ONLY MODE section, updated Diagnostic Ladder and Examples to use bare commands
- `skills/docker-storage.md` - Added COMMAND-ONLY MODE section, updated Diagnostic Ladder steps and Tools section

## Decisions Made
- Discovery frontmatter commands (docker exec for ls -ld, id, df, du) kept as-is -- these are engine-executed discovery commands, not LLM output
- Infrastructure commands (docker restart, docker logs, docker ps) left in skill text where appropriate -- they are infrastructure-level, not inside-container commands
- No E2E test modifications needed -- the docker exec parser in dynamicRewrite() correctly parses existing canned fix plan commands and reassembles them identically

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All skills now use appropriate command modes: SQL-ONLY (postgres), COMMAND-ONLY (linux-fs, docker-storage), as-is (nginx)
- The engine-first architecture is fully proven: skills declare intent, engine handles wrapping
- Phase 12.3 complete -- rewriter engine proven with docker exec awareness and skill prompt alignment

---
*Phase: 12.3-engine-proof-rewriter*
*Completed: 2026-03-14*

## Self-Check: PASSED
