---
phase: 12.3-engine-proof-rewriter
plan: 01
subsystem: execution
tags: [docker-exec, rewriter, command-parsing, tdd]

# Dependency graph
requires:
  - phase: 12.1-dynamic-rewriter
    provides: "dynamicRewrite() pure function with RewriteRuleSchema and 21 tests"
provides:
  - "parseDockerExec() helper for docker exec flag/container/inner extraction"
  - "Docker-exec-aware rewrite branch in dynamicRewrite() with user injection"
affects: [12.3-engine-proof-rewriter, debug.ts integration]

# Tech tracking
tech-stack:
  added: []
  patterns: ["token-walk parsing for docker exec flags", "belt-and-suspenders -it stripping"]

key-files:
  created: []
  modified:
    - src/execution/dynamic-rewriter.ts
    - tests/execution/dynamic-rewriter.test.ts

key-decisions:
  - "Token-walk parser over regex for docker exec flag parsing -- more robust for value-flags like -u 0"
  - "parseDockerExec not exported -- internal helper, not part of public API"
  - "User injection prepended to flag list for consistent docker exec formatting"

patterns-established:
  - "Docker exec value-flag set (DOCKER_EXEC_VALUE_FLAGS) for token consumption"
  - "Parse-match-reassemble pattern for docker exec commands"

requirements-completed: [ENGN-05]

# Metrics
duration: 2min
completed: 2026-03-14
---

# Phase 12.3 Plan 01: Docker-Exec-Aware Rewriter Summary

**parseDockerExec token-walk parser + docker exec rewrite branch with -u injection, -it stripping, and inner command rule matching**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-14T12:03:24Z
- **Completed:** 2026-03-14T12:05:53Z
- **Tasks:** 2 (TDD RED + GREEN)
- **Files modified:** 2

## Accomplishments
- Replaced blind docker exec passthrough with parse-match-reassemble pipeline
- Inner commands now matched against rewrite rules with strip_flags, wrapper, and user injection
- No double-injection: existing -u/--user flags detected and preserved
- Belt-and-suspenders -it/-ti/-i/-t stripping on all docker exec commands
- 29 tests passing (21 existing regression + 8 new docker-exec-aware)
- 488 tests passing across full suite (2 pre-existing nginx E2E failures out of scope)

## Task Commits

Each task was committed atomically:

1. **RED: Failing docker exec tests** - `82405c7` (test)
2. **GREEN: Docker-exec-aware rewriting** - `4d6e907` (feat)

_TDD cycle: RED wrote 8 failing tests, GREEN implemented parseDockerExec + modified dynamicRewrite._

## Files Created/Modified
- `src/execution/dynamic-rewriter.ts` - Added DockerExecParts interface, DOCKER_EXEC_VALUE_FLAGS constant, parseDockerExec() helper, replaced passthrough with parse-match-reassemble branch
- `tests/execution/dynamic-rewriter.test.ts` - 8 new test cases: user injection, double-inject prevention, -it stripping, strip_flags on inner, passthrough for unparseable/no-match

## Decisions Made
- Token-walk parser over regex for docker exec flag parsing -- more robust for value-flags like `-u 0`
- `parseDockerExec` not exported -- internal helper only, not part of public API
- User injection prepended to flag list (`['-u', rule.user, ...cleanFlags]`) for consistent formatting
- Bare command path (lines 104-136 in original) left completely unchanged -- only docker exec branch modified

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Docker exec rewriting proven with 8 new test cases
- Next plans can address skill prompt COMMAND-ONLY updates and E2E validation
- dynamicRewrite() signature unchanged -- no integration changes needed

---
*Phase: 12.3-engine-proof-rewriter*
*Completed: 2026-03-14*

## Self-Check: PASSED
