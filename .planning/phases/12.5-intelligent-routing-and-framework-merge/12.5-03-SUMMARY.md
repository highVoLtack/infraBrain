---
phase: 12.5-intelligent-routing-and-framework-merge
plan: 03
subsystem: testing
tags: [e2e, agnosticism, docker, remix, dynamic-discovery, dpev]

requires:
  - phase: 12.5-01
    provides: "EnrichedSkillSummary, negative_triggers, enriched registry.list()"
  - phase: 12.5-02
    provides: "Two-tier routing (preFilterSkills + enriched LLM), DPEV enforcement"
provides:
  - "Parameterized permission trap demo with DATA_DIR environment variable"
  - "Remix scenario with vault-processor-99 container and /var/lib/internal/secrets path"
  - "Dynamic E2E test proving engine agnosticism (zero hardcoded container names or paths)"
  - "Full DPEV chain proof: routing -> discovery -> diagnosis -> chown -u 0 -> recovery"
affects: [phase-12.5-complete, agnosticism-proof]

tech-stack:
  added: []
  patterns:
    - "Parameterized Docker demos via ARG/ENV for scenario remixing"
    - "Dynamic container discovery in E2E tests via docker compose ps + printenv"

key-files:
  created:
    - demo/permission-trap/docker-compose.remix.yml
    - demo/permission-trap/reset-permission-trap-remix.sh
  modified:
    - demo/permission-trap/app/app.py
    - demo/permission-trap/app/Dockerfile
    - tests/e2e/poc-permission-trap.test.ts

key-decisions:
  - "DATA_DIR defaults to /app/data for full backwards compatibility with original demo"
  - "Container name discovered via docker compose ps, data path via docker exec printenv"
  - "Canned fix plan built inside beforeAll after dynamic discovery (not at module scope)"

patterns-established:
  - "Parameterized Docker demo: ARG/ENV in Dockerfile + environment in compose for scenario variants"
  - "Dynamic E2E testing: discover container names and paths at runtime, never hardcode"

requirements-completed: [ENGN-07]

duration: 4min
completed: 2026-03-14
---

# Phase 12.5 Plan 03: Permission Trap Remix -- Agnosticism Proof Summary

**Parameterized permission trap demo with vault-processor-99 remix scenario and fully dynamic E2E test proving engine handles any container name and any path without code changes**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-14T14:35:38Z
- **Completed:** 2026-03-14T14:39:38Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- Permission trap demo parameterized via DATA_DIR environment variable (backwards-compatible default)
- Remix scenario: vault-processor-99 container with /var/lib/internal/secrets path shows Permission Denied
- E2E test discovers container name and data path dynamically -- zero hardcoded strings
- Full DPEV chain completes: linux-expert selected, chown -u 0 applied, app recovers with PID written
- 523 tests passing (2 pre-existing nginx E2E failures remain out of scope)

## Task Commits

Each task was committed atomically:

1. **Task 1: Parameterize Permission Trap demo + create remix scenario** - `b3e4120` (feat)
2. **Task 2: Rewrite E2E test for dynamic remix scenario** - `540a1ec` (feat)

## Files Created/Modified
- `demo/permission-trap/app/app.py` - Reads DATA_DIR from environment, defaults to /app/data
- `demo/permission-trap/app/Dockerfile` - Uses ARG/ENV for DATA_DIR parameterization
- `demo/permission-trap/docker-compose.remix.yml` - Remix compose with vault-processor-99 and /var/lib/internal/secrets
- `demo/permission-trap/reset-permission-trap-remix.sh` - Starts the remix environment
- `tests/e2e/poc-permission-trap.test.ts` - Dynamic E2E test with container/path discovery

## Decisions Made
- DATA_DIR defaults to /app/data in both app.py and Dockerfile for full backwards compatibility
- Container name discovered via `docker compose ps --format "{{.Names}}"` (not hardcoded)
- Data path discovered via `docker exec printenv DATA_DIR` (not hardcoded)
- Canned fix plan built inside beforeAll after dynamic discovery, not at module scope

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 12.5 complete: skill schema, two-tier routing, and agnosticism proof all delivered
- Engine proven truly agnostic: vault-processor-99 with /var/lib/internal/secrets works without any engine code changes
- 2 pre-existing nginx E2E failures remain out of scope (Docker networking environment issue)

## Self-Check: PASSED

All 6 files verified present. Both commits verified in git log.

---
*Phase: 12.5-intelligent-routing-and-framework-merge*
*Completed: 2026-03-14*
