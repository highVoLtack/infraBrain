---
phase: 12.5-intelligent-routing-and-framework-merge
plan: 02
subsystem: orchestrator
tags: [routing, pre-filter, negative-selection, dpev, triggers]

requires:
  - phase: 12.5-01
    provides: "EnrichedSkillSummary interface, negative_triggers/when_not_to_use in skill frontmatter, enriched registry.list()"
provides:
  - "preFilterSkills() Level 0 trigger-based routing with negative trigger exclusion"
  - "Enriched selectSkill() with two-tier routing (pre-filter -> LLM)"
  - "ROUTING_CONSTITUTION with Negative Selection protocol for LLM system prompt"
  - "buildRoutingPrompt() accepting EnrichedSkillSummary[] with triggers/when_not_to_use/priority"
  - "enforceDPEVSequence() for ordered phase transition validation"
affects: [12.5-03, debug-route, skill-routing]

tech-stack:
  added: []
  patterns:
    - "Two-tier routing: Level 0 pre-filter (deterministic) + Level 1 LLM (enriched)"
    - "Negative Selection: exclude first, then select among remaining candidates"
    - "DPEV sequence enforcement via phase tracking array"

key-files:
  created:
    - tests/api/debug-dpev.test.ts
  modified:
    - src/orchestrator/router.ts
    - src/orchestrator/context.ts
    - src/api/routes/debug.ts
    - tests/orchestrator/router.test.ts

key-decisions:
  - "preFilterSkills returns ALL skills on empty result (safety net for unrecognized queries)"
  - "Single-candidate shortcut skips LLM entirely for deterministic routing"
  - "negative_triggers are Level 0 only -- never sent to LLM prompt (when_not_to_use goes to LLM)"
  - "DPEV enforcement is additive -- validates at transitions but does not restructure handler"
  - "Discovery auto-completes immediately (empty or not) to unblock diagnosis phase"

patterns-established:
  - "Two-tier routing: trigger pre-filter narrows candidates, LLM resolves ambiguity"
  - "DPEV completedPhases tracking at route handler level"

requirements-completed: [ENGN-07]

duration: 6min
completed: 2026-03-14
---

# Phase 12.5 Plan 02: Two-Tier Routing Engine Summary

**Two-tier routing (preFilterSkills + enriched LLM) with Negative Selection constitution and DPEV sequence enforcement**

## Performance

- **Duration:** 6 min
- **Started:** 2026-03-14T14:22:34Z
- **Completed:** 2026-03-14T14:28:37Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- "permission error" now routes deterministically to linux-expert (not log-analysis) via negative trigger exclusion
- Single-candidate pre-filter shortcut eliminates unnecessary LLM calls for clear-cut prompts
- DPEV sequence enforcement prevents out-of-order phase transitions in debug route
- 523 tests passing (up from 512), 2 pre-existing nginx E2E failures remain out of scope

## Task Commits

Each task was committed atomically:

1. **Task 1: preFilterSkills + enriched selectSkill + routing prompt** - `4af77dc` (feat)
2. **Task 2: DPEV sequence enforcement in debug.ts** - `24cd42c` (feat)

_Note: TDD tasks each include tests + implementation in single commit_

## Files Created/Modified
- `src/orchestrator/router.ts` - Added preFilterSkills(), updated selectSkill() with two-tier routing
- `src/orchestrator/context.ts` - Added ROUTING_CONSTITUTION, updated buildRoutingPrompt() for EnrichedSkillSummary[]
- `src/api/routes/debug.ts` - Added enforceDPEVSequence(), wired DPEV tracking into debug route handler
- `tests/orchestrator/router.test.ts` - 11 tests covering pre-filter, fallthrough, single-candidate, enriched prompt
- `tests/api/debug-dpev.test.ts` - 5 tests covering DPEV sequence validation

## Decisions Made
- preFilterSkills returns ALL skills when no candidates survive filtering (LLM safety net)
- Single-candidate shortcut returns immediately with "Single trigger match (pre-filter)" reasoning
- negative_triggers are Level 0 only -- they prune before LLM sees candidates; when_not_to_use goes to LLM via routing prompt
- DPEV enforcement is additive: validates at transitions but doesn't restructure the handler flow
- Discovery phase auto-completes after runDiscovery returns (whether commands exist or not)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Two-tier routing operational, ready for Plan 03 (integration testing / wiring validation)
- DPEV enforcement active in debug route for discovery -> diagnosis -> plan transitions
- 2 pre-existing nginx E2E failures remain out of scope (Docker networking environment issue)

---
*Phase: 12.5-intelligent-routing-and-framework-merge*
*Completed: 2026-03-14*
