---
phase: 12.5-intelligent-routing-and-framework-merge
task: post-execution
total_tasks: 3
status: verified_with_gap
last_updated: 2026-03-14T16:00:14.394Z
---

<current_state>
Phase 12.5 is EXECUTED and VERIFIED (14/14 must-haves). UAT passed 6/6 automated tests.
But LIVE testing revealed a critical gap: the fix-plan execution fails because local LLMs
(7B-70B) generate incorrect docker exec/chown syntax every single time — different wrong
variant each run (-it flags, $(id -u) expansion, -u 0 misplacement, invented usernames).

We added a regex-based command sanitizer (fixKnownCommandErrors) but realized this is
fundamentally the wrong approach. The RIGHT solution (agreed with user) is:
**Self-healing executor** — when a command fails, feed the error output back to the LLM
and let it generate a corrected command. Like a human reading `chown --help`.

This is exactly the Superpowers "systematic-debugging" pattern and the reason we saved
the GSD + Superpowers repos as inspiration for the next phase.
</current_state>

<completed_work>

## Phase 12.5 Execution (3/3 plans)
- Plan 01: Skill schema extension (negative_triggers, when_not_to_use, enriched registry.list()) ✓
- Plan 02: Two-tier routing engine (preFilterSkills + enriched LLM) + DPEV enforcement ✓
- Plan 03: Permission Trap remix (vault-processor-99) + dynamic E2E test ✓

## Post-Execution Fixes (8 commits)
- GROUND TRUTH injection into planner prompt
- Shell mode for complex discovery commands (for-loops, pipes)
- Error-only log filtering in discovery (2966 → 176 tokens)
- chown pattern docs in linux-expert skill
- Planning skill routed to strategic model (70B)
- Command templates in planning skill
- Comprehensive docker command sanitizer (fixKnownCommandErrors)
- Nested $(docker exec ...) shell expansion handling

## Tests
- 538 tests passing (up from 523)
- New: agnostic-discovery.test.ts (6 tests — random container + path)
- UAT: 6/6 passed
- Verification: 14/14 must-haves

## Key Discovery
- Routing + Discovery + Diagnosis: ALL WORK PERFECTLY with 7B model
- Fix plan GENERATION works but LLM generates wrong command syntax every time
- No amount of prompting/templates/regex fixes this — LLMs need to self-correct from errors
</completed_work>

<remaining_work>

## Next Phase: Self-Healing Executor + Superpowers/GSD Skill Depth

**Core problem:** Executor halts on first command failure. Should instead:
1. Capture error output (e.g., "chown: invalid option -- 'u'")
2. Feed error + original command back to LLM
3. LLM generates corrected command using error as learning signal
4. Retry with corrected command
5. Track correction history for learning

**Inspiration sources (local copies):**
- `/Users/nikolasretkovic/Desktop/InfraBrain/superpowers-main/` — 14 workflow-protocol skills
- `/Users/nikolasretkovic/Desktop/InfraBrain/get-shit-done-main/` — Structured agent design patterns

**Specific patterns to merge:**
- Superpowers `systematic-debugging`: 4-phase protocol, Iron Laws, Red Flags
- Superpowers `verification-before-completion`: Evidence before claims
- GSD `gsd-debugger`: Scientific method debugging with checkpoints
- GSD `gsd-planner`: Goal-backward must-haves, interface extraction, command templates

**Also needed:**
- Workflow-skills (steer HOW the model thinks, not just WHAT it knows)
- Skill-chaining (discovery → planning → execution → verification as enforced sequence)
- `<why_this_matters>` sections so downstream consumers understand output contracts
</remaining_work>

<decisions_made>

- Regex command sanitizer is a TEMPORARY band-aid, not the solution
- Self-healing executor (error → LLM correction → retry) is the correct architecture
- User confirmed: "chown --help ist ein Befehl — warum kann man die KI das nicht machen lassen?"
- GPU budget: max 4x 5090, money not an issue, realism is
- Planning skill routes to strategic model (70B) but even 70B makes syntax errors
- Superpowers + GSD patterns are the blueprint for enterprise-grade skill system
</decisions_made>

<blockers>
- None — clear path forward via Superpowers/GSD merge
</blockers>

<context>
User is frustrated with regex scripting approach (rightfully so). The breakthrough insight
is: instead of anticipating every possible LLM error variant, let the executor be
self-healing. When a command fails, show the LLM the error and let it fix itself.

This is exactly how Superpowers' systematic-debugging works — evidence gathering before
fixes, never guess. And GSD's debugger uses the same scientific method pattern.

The user wants to combine the best of GSD (structured agent design, goal-backward planning)
with Superpowers (workflow protocols, iron laws, self-correction) into InfraBrain's
skill system. This would make even weak LLMs (7B) effective because the SYSTEM
compensates for model limitations.

Local copies of both repos are at:
- /Users/nikolasretkovic/Desktop/InfraBrain/superpowers-main/
- /Users/nikolasretkovic/Desktop/InfraBrain/get-shit-done-main/
</context>

<next_action>
1. `/clear` for fresh context
2. `/gsd:resume-work` to restore this context
3. Plan the self-healing executor phase using Superpowers + GSD patterns as blueprint
4. Key design: on command failure → capture stderr → LLM generates corrected command → retry
5. Reference: superpowers-main/skills/systematic-debugging/SKILL.md for the 4-phase protocol
</next_action>
