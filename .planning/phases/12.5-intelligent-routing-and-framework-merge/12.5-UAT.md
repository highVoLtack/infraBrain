---
status: complete
phase: 12.5-intelligent-routing-and-framework-merge
source: [12.5-01-SUMMARY.md, 12.5-02-SUMMARY.md, 12.5-03-SUMMARY.md]
started: 2026-03-14T15:10:00Z
updated: 2026-03-14T15:10:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test -- Remix Scenario
expected: Kill any running permission-trap containers. Run `bash demo/permission-trap/reset-permission-trap-remix.sh`. Script exits 0. Container `vault-processor-99` is running. `docker logs vault-processor-99` shows "FATAL: Permission denied" for `/var/lib/internal/secrets/status.pid`.
result: pass

### 2. Skill Schema Parses Routing Metadata
expected: Run `npx vitest run tests/skills/types.test.ts`. All tests pass including tests for negative_triggers, when_not_to_use fields, and enriched list() returning routing metadata.
result: pass

### 3. Pre-Filter Routes "permission error" to linux-expert
expected: Run `npx vitest run tests/orchestrator/router.test.ts`. Tests confirm that "permission denied on /app/data" routes to linux-expert (not log-analysis) via negative trigger exclusion. Single-candidate shortcut skips LLM.
result: pass

### 4. DPEV Sequence Enforcement
expected: Run `npx vitest run tests/api/debug-dpev.test.ts`. All 5 tests pass -- out-of-order phase transitions are rejected, valid transitions are allowed.
result: pass

### 5. Dynamic E2E -- Permission Trap Remix (LIVE)
expected: Start server, send vague prompt like "mein Container kann nicht schreiben". Engine routes to linux-expert, discovers vault-processor-99, diagnoses ownership mismatch on /var/lib/internal/secrets, generates fix plan with REAL discovered values (not placeholders), executes fix successfully, app recovers.
result: pass

### 6. Full Test Suite No Regressions
expected: Run `npx vitest run`. 520+ tests pass. 2 nginx E2E failures are pre-existing (not from Phase 12.5). No new failures introduced.
result: pass

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0

## Gaps

- truth: "Fix plan uses discovered container name and data path from discovery phase, not placeholders"
  status: resolved
  reason: "Fixed: discoveryContext injected into planner GROUND TRUTH, shell mode for discovery commands, error-only log filtering, chown pattern docs"
  severity: blocker
  test: 5
