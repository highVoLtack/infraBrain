---
phase: 12.1-dynamic-command-rewriter
plan: 01
subsystem: execution
tags: [zod, tdd, command-rewriting, docker-exec, pure-function]

requires:
  - phase: 12-linux-filesystem-permission-trap
    provides: Proof that hardcoded SQL rewriter doesn't scale beyond Postgres
provides:
  - "dynamicRewrite() pure function for skill-driven command rewriting"
  - "RewriteRuleSchema Zod schema for declarative rewrite rules"
  - "RewriteRule type exported for skill frontmatter integration"
affects: [12.1-02-skill-frontmatter, 12.1-03-integration, debug-engine]

tech-stack:
  added: []
  patterns: [regex-match-first-wins, strip-then-wrap-then-exec, container-auto-resolution]

key-files:
  created:
    - src/execution/dynamic-rewriter.ts
    - tests/execution/dynamic-rewriter.test.ts
  modified: []

key-decisions:
  - "Wrapper {cmd} replaces with full stripped command -- wrapper is the entire executable line"
  - "Container resolution falls back to first container when specific name not in list"
  - "Empty containers list causes passthrough (no container = no docker exec)"
  - "Case-insensitive regex matching via 'i' flag on all rules"

patterns-established:
  - "RewriteRule schema: match/container/user/wrapper/risk/strip_flags"
  - "Strip-then-wrap pipeline: flags removed before wrapper applied"
  - "First-match-wins: rule order in skill frontmatter determines priority"

requirements-completed: [ENGN-03]

duration: 2min
completed: 2026-03-14
---

# Phase 12.1 Plan 01: Dynamic Command Rewriter Summary

**Pure function command rewriter with Zod schema -- regex-match pipeline replaces hardcoded SQL rewriting**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-14T09:13:39Z
- **Completed:** 2026-03-14T09:15:15Z
- **Tasks:** 1 feature (TDD RED-GREEN)
- **Files modified:** 2

## Accomplishments
- dynamicRewrite() pure function: match rules -> strip flags -> apply wrapper -> docker exec with optional -u
- RewriteRuleSchema with Zod validation: match, container, user, wrapper, risk, strip_flags
- Container auto-resolution from discovered containers list
- 21 unit tests covering all behavior cases from plan specification

## Task Commits

Each task was committed atomically:

1. **RED: Failing tests** - `3196a4b` (test)
2. **GREEN: Implementation** - `effd9f5` (feat)

## Files Created/Modified
- `src/execution/dynamic-rewriter.ts` - Pure function rewriter + RewriteRuleSchema + RewriteRule type
- `tests/execution/dynamic-rewriter.test.ts` - 21 unit tests for all rewrite behaviors

## Decisions Made
- Wrapper pattern replaces {cmd} with the full stripped command (not a sub-portion)
- Container resolution: "auto" -> first container, specific name -> verify + fallback to first
- Empty containers list -> passthrough (cannot wrap without a target)
- Case-insensitive regex matching by default (matches plan behavior examples)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- dynamicRewrite, RewriteRuleSchema, RewriteRule all exported and ready for skill frontmatter integration (Plan 02)
- Existing rewriteForContainer/stripHostFlag in runner.ts untouched -- migration happens in Plan 02/03
- All 26 existing command-rewriter tests still passing

---
*Phase: 12.1-dynamic-command-rewriter*
*Completed: 2026-03-14*
