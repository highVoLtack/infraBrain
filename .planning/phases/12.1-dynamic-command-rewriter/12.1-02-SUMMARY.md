---
phase: 12.1-dynamic-command-rewriter
plan: 02
subsystem: engine
tags: [zod, skill-frontmatter, rewrite-rules, yaml]

requires:
  - phase: 12-linux-filesystem-permission-trap
    provides: "Skill files with YAML frontmatter pattern"
provides:
  - "RewriteRuleSchema Zod validator in src/skills/types.ts"
  - "SkillFrontmatterSchema extended with rewrite_rules field"
  - "3 skill files with declarative rewrite_rules in frontmatter"
affects: [12.1-01-dynamic-rewriter-engine, 12.1-03-integration]

tech-stack:
  added: []
  patterns: ["Declarative rewrite rules in skill YAML frontmatter"]

key-files:
  created:
    - tests/skills/types.test.ts
  modified:
    - src/skills/types.ts
    - skills/postgres-troubleshoot.md
    - skills/linux-filesystem-troubleshoot.md
    - skills/docker-storage.md

key-decisions:
  - "Defined RewriteRuleSchema inline in types.ts since Plan 01 (dynamic-rewriter.ts) has not yet run"
  - "Used YAML single-quoted strings for regex patterns in skill frontmatter"

patterns-established:
  - "rewrite_rules frontmatter: skill files declare command rewrite rules as YAML arrays"
  - "RewriteRuleSchema: match (required), container (default auto), user/wrapper/risk/strip_flags (optional)"

requirements-completed: [ENGN-03]

duration: 1min
completed: 2026-03-14
---

# Phase 12.1 Plan 02: Skill Frontmatter Schema Extension Summary

**Extended SkillFrontmatterSchema with RewriteRuleSchema and migrated 3 skills to declare rewrite rules in YAML frontmatter**

## Performance

- **Duration:** 1 min
- **Started:** 2026-03-14T09:13:45Z
- **Completed:** 2026-03-14T09:15:03Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- RewriteRuleSchema with match, container, user, wrapper, risk, strip_flags validated by Zod
- SkillFrontmatterSchema backwards-compatible: skills without rewrite_rules default to []
- 3 skills migrated: postgres-troubleshoot (SQL wrapper + psql), linux-filesystem-troubleshoot (chown/chmod with root user), docker-storage (truncate/df/du)
- 4 new validation tests + all 24 existing skill tests pass

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend SkillFrontmatterSchema with rewrite_rules** - `1811106` (feat)
2. **Task 2: Migrate skill frontmatter with rewrite_rules** - `9391400` (feat)

## Files Created/Modified
- `src/skills/types.ts` - Added RewriteRuleSchema and rewrite_rules field to SkillFrontmatterSchema
- `tests/skills/types.test.ts` - 4 tests for rewrite_rules validation (defaults, valid array, missing match, minimal)
- `skills/postgres-troubleshoot.md` - SQL/psql rewrite rules with wrapper and strip_flags
- `skills/linux-filesystem-troubleshoot.md` - chown/chmod with user 0, read-only inspection commands
- `skills/docker-storage.md` - truncate write, df/du read rules

## Decisions Made
- Defined RewriteRuleSchema inline in types.ts because Plan 01 (dynamic-rewriter.ts) has not yet created the canonical module. Plan 03 integration will reconcile the import.
- Used YAML single-quoted strings for regex patterns to avoid escape issues in frontmatter.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Schema and skill data layer ready for dynamic rewriter engine (Plan 01)
- Plan 03 (integration) will wire RewriteRuleSchema import from dynamic-rewriter.ts once Plan 01 delivers

## Self-Check: PASSED

All 6 files verified present. Both task commits (1811106, 9391400) confirmed in git log.

---
*Phase: 12.1-dynamic-command-rewriter*
*Completed: 2026-03-14*
