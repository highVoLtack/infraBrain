---
phase: 20-audit-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/execution/audit-step-events.test.ts
  - .planning/phases/20-audit-integrity/20-RED-PROOF.txt
autonomous: true
requirements: [AUDIT-09]
must_haves:
  truths:
    - "The closing regression test exists and FAILS against the unmodified producer"
    - "The failing run's output is persisted as a committed file in the phase directory"
    - "A commit exists whose tree contains the failing test and no producer fix"
  artifacts:
    - tests/execution/audit-step-events.test.ts
    - .planning/phases/20-audit-integrity/20-RED-PROOF.txt
  key_links:
    - "executePlan (circuit-breaker path) -> capturing auditLogger -> buildReplayState"
    - "RED commit SHA recorded in SUMMARY -> D-4 evidentiary chain"
---

<objective>
Produce the D-4 red artifact **before any producer code changes exist**.

Write the criterion-9 closing regression test, run it against the current (broken) producer,
persist the failing output, and commit both. This plan deliberately lands a failing test in the
suite. That is the deliverable, not a defect.

Purpose: D-4 is LOCKED. "It was red first" that exists only in a summary is narration — in two
weeks it is indistinguishable from a test that was never red. This plan makes the guarantee
mechanical: a commit SHA plus a persisted run output.

Output: `tests/execution/audit-step-events.test.ts` (failing), `20-RED-PROOF.txt`, and a recorded
RED commit SHA.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-audit-integrity/20-CONTEXT.md
@.planning/phases/20-audit-integrity/20-BRIEF.md
</context>

<critical_notices>
**This plan intentionally leaves the test suite with one MORE failure than baseline.**

- Baseline at phase start: 1318 passed / 2 failed / 4 skipped. The 2 failures (`poc-nginx-502`,
  `poc-postgres-connleak`) are environmental — they need a live LLM backend, Docker, and the
  untracked `demo/` stack. They are NOT regressions.
- After this plan: 1318 passed / **3** failed / 4 skipped. The third failure is
  `tests/execution/audit-step-events.test.ts` and it is REQUIRED to be failing.
- Plans 20-03 through 20-07 will observe 3 failures. That is the expected state.
  **Do not "fix", skip, delete, or `.skip()` this test.** It goes green in plan 20-08.

**Do not modify any file under `src/` in this plan.** The red proof is only evidence if the tree
it was captured against contains no producer fix.
</critical_notices>

<tasks>

<task type="auto">
  <name>Task 1: Write the criterion-9 closing regression test</name>

  <read_first>
    - `tests/execution/executor.test.ts` lines 1-95 — the existing mock harness: module mocks for
      lock manager, snapshot, rollback, self-healer, persistence-verification and chalk, plus
      `makeDeps()` with its injectable `runner` and `auditLogger` spy. Reuse this shape.
    - `tests/execution/executor.test.ts` lines 164-186 and 405-426 — the exact recipe for driving a
      circuit-breaker failure with no LLM and no Docker.
    - `src/execution/executor.ts` lines 290-336 — the circuit-breaker branch. Note it `return`s at
      line 326 without ever reaching the `step_complete` emission at line 341.
    - `src/ui/App.tsx` lines 110-247 — `buildReplayState`. READ ONLY, frozen by criterion 8.
      Note the hard gate at line 204: an entry without a numeric `metadata.stepIndex` is skipped.
    - `tests/ui/app.test.tsx` lines 1-30 — shows `buildReplayState` already imported into tests.
  </read_first>

  <files>tests/execution/audit-step-events.test.ts</files>

  <action>
    Create the closing regression test as a producer-to-consumer round trip in a single test. The
    bug that opened this phase must be the test that closes it: drive a step to genuine failure,
    replay the resulting session, assert the reconstructed record shows failure plus the attempt
    history — not success.

    Build the test as follows.

    Mirror the module-mock preamble and `makeDeps()` helper from `tests/execution/executor.test.ts`
    lines 1-95. Do not import from that file; copy the harness so this file is self-contained and
    cannot be broken by unrelated edits to the executor suite.

    Supply a capturing audit logger that collects BOTH emitter doors into one ordered array of
    `{ eventType, metadata }` records:
    - `logExecution` — the general-purpose door that exists today.
    - `logStepEvent` — the typed step-event door that plan 20-03 will add. It does not exist yet
      and will never be called during this plan's run. Include it now, as a `vi.fn()` that pushes
      into the same array, so that this test file needs NO edit between its red state and its green
      state. A test that must be modified to go green is a weaker artifact than one that does not.
    - Cast the deps object with `as unknown as ExecutionDeps` (the existing suite already does this
      at `tests/execution/executor.test.ts:69-72`), which is what lets the not-yet-declared
      `logStepEvent` property compile today.

    Drive the circuit-breaker path — no LLM, no Docker, fully deterministic:
    - Build deps WITHOUT `correctionModel` and WITHOUT `skill`, so `hasSelfHealingDeps` is falsy
      and `executor.ts:290` takes the circuit-breaker branch.
    - Mock the runner's `run` to always resolve `{ stdout: '', stderr: 'nginx: configuration file
      test failed', exitCode: 1 }`.
    - Set `config.circuitBreaker` to `{ maxRetries: 3, retryDelayMs: 0 }` so the test is fast.
    - Use a single-step plan whose step has `risk: 'write'` and a real command string, and pass a
      target of `'nginx'` to `executePlan`.
    - Assert the run halts: `result.status` is `'halted'` and `result.reason` is `'circuit_breaker'`.
      These two assertions pass today and pin that the harness genuinely reaches the failure path —
      they must not be the assertions that fail.

    Shape the captured calls into `AuditEntry`-like records (`{ eventType, timestamp, metadata }`)
    with monotonically increasing ISO timestamps derived from capture order, then feed them to
    `buildReplayState('sess-red-proof', entries)` imported from `src/ui/App.tsx`.

    Assert the four things the phase exists to deliver. These are the assertions that must fail
    today:
    1. `state.executionSteps` has exactly one entry and its `status` is `'failed'`.
    2. That entry carries a non-empty `risk`, a defined `target`, a `total` equal to the plan's step
       count, and a `stderr` containing the runner's stderr text.
    3. The captured trail contains a terminal failure event for each of attempts 1 through 4 —
       one for the executor's own first execution plus three circuit-breaker retries — each
       carrying its own attempt number in `metadata`.
    4. Every captured step-family event carries an integer attempt number greater than or equal
       to 1 in its `metadata`.

    Name the test so its purpose survives a stranger reading it in six months, e.g.
    `a step that fails through the circuit breaker replays as failed, with its attempt history`.

    Add a file-head comment stating: this test is the D-4 red artifact for phase 20; it is expected
    to FAIL until the producer fix lands in wave 4; do not skip or delete it. Do not name any
    specific event-type string literal in that comment — describe the behaviour in prose. The
    assertions themselves carry the literals.

    Do NOT touch any file under `src/`.
  </action>

  <verify>
    <automated>npx vitest run tests/execution/audit-step-events.test.ts; test $? -ne 0 &amp;&amp; echo "RED CONFIRMED — test fails as required"</automated>
  </verify>

  <acceptance_criteria>
    - `tests/execution/audit-step-events.test.ts` exists and is a valid Vitest file (it is collected
      and executed, not skipped and not a collection error).
    - Running it exits non-zero.
    - The failure is an assertion failure on the replayed step state, NOT a module-resolution error,
      NOT a TypeScript error, and NOT a timeout. A test that errors before reaching its assertions
      proves nothing about the producer.
    - `git status --porcelain src/` reports no changes.
    - The `result.status === 'halted'` / `result.reason === 'circuit_breaker'` assertions pass,
      proving the harness reached the real circuit-breaker path.
  </acceptance_criteria>

  <done>
    The regression test exists, is genuinely executed by Vitest, reaches the circuit-breaker path,
    and fails on its replay assertions. No source file changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Persist the failing run as a committed artifact and record the RED commit</name>

  <read_first>
    - `.planning/phases/20-audit-integrity/20-CONTEXT.md` section D-4 — the locked requirement that
      the red proof be an artifact rather than a report.
    - `.planning/phases/20-audit-integrity/20-BRIEF.md` acceptance criterion 9.
  </read_first>

  <files>.planning/phases/20-audit-integrity/20-RED-PROOF.txt</files>

  <action>
    Capture the failing run verbatim into the phase directory, then commit the test and the proof
    together as the RED commit.

    Write the proof file with a short header block giving: the date, the git SHA of `HEAD` at
    capture time (the commit the test was run against, i.e. before this plan's own commit), the
    command that was run, and one sentence stating why the failure is expected. Follow the header
    with the complete unmodified stdout+stderr of the Vitest run.

    Capture command (run from the repo root):

        npx vitest run tests/execution/audit-step-events.test.ts > /tmp/red.txt 2>&1; echo "exit=$?" >> /tmp/red.txt

    Then assemble `20-RED-PROOF.txt` from the header plus `/tmp/red.txt`. The recorded exit code
    must be non-zero and must appear in the file.

    Review the captured output for anything secret-shaped before committing — the gitleaks
    pre-commit hook (`core.hooksPath=.githooks`) scans every commit. The output should contain only
    test names, assertion diffs and file paths, but check rather than assume.

    Commit the test file and the proof file together in one commit. Use a message that makes the
    intent unmistakable to anyone bisecting later, e.g.
    `test(20-01): RED — failing step replays as success (D-4 red proof)`.

    Record the resulting commit SHA in the plan SUMMARY under a heading `D-4 RED commit`. That SHA
    is the evidentiary anchor plan 20-08 will reference when it turns the test green.

    Do NOT use `git add -A` or `git add .` — the working tree carries unrelated untracked files.
    Stage exactly the two files this plan owns.
  </action>

  <verify>
    <automated>test -s .planning/phases/20-audit-integrity/20-RED-PROOF.txt &amp;&amp; grep -qE 'exit=[1-9]' .planning/phases/20-audit-integrity/20-RED-PROOF.txt &amp;&amp; git log -1 --format=%H -- tests/execution/audit-step-events.test.ts &amp;&amp; echo "RED ARTIFACT COMMITTED"</automated>
  </verify>

  <acceptance_criteria>
    - `20-RED-PROOF.txt` is non-empty, is tracked by git, and contains a non-zero exit code.
    - The proof file names the git SHA the run was captured against.
    - `git log --oneline -- tests/execution/audit-step-events.test.ts` returns at least one commit.
    - The commit containing the test contains no file under `src/`
      (verify: `git show --stat --name-only HEAD -- src/` is empty).
    - The RED commit SHA is written into the plan SUMMARY.
  </acceptance_criteria>

  <done>
    A committed, non-empty red proof exists in the phase directory, and a commit whose tree carries
    the failing test and zero producer changes exists in git history with its SHA recorded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| execution engine -> audit record | The record is the compliance artifact; anything lost here is unprovable afterwards |
| audit record -> auditor / pilot engineer | The reader trusts the record to describe what actually happened |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Repudiation | phase 20 regression test | high | mitigate | The red proof is committed as a file plus a commit SHA, so "the test was red first" is verifiable from git rather than trusted from a summary (D-4) |
| T-20-02 | Tampering | `20-RED-PROOF.txt` | low | accept | The proof is a plain committed file; its integrity rests on git history, which is sufficient for an internal evidentiary artifact |
| T-20-03 | Information disclosure | committed test output | medium | mitigate | Review captured Vitest output for host names, paths and secret-shaped strings before committing; the gitleaks pre-commit hook provides a second gate |
</threat_model>

<artifacts_produced>
## Artifacts this plan produces

**New files**
- `tests/execution/audit-step-events.test.ts` — the criterion-9 closing regression test (RED)
- `.planning/phases/20-audit-integrity/20-RED-PROOF.txt` — persisted failing run output

**New symbols**
- Test-local helper `makeCapturingAuditLogger()` — collects `logExecution` and `logStepEvent` calls
  into one ordered array
- Test-local helper `makeDeps()` — circuit-breaker-path executor deps (no `correctionModel`, no
  `skill`)

**Recorded values**
- D-4 RED commit SHA (written to the plan SUMMARY)
</artifacts_produced>

<verification>
1. `npx vitest run tests/execution/audit-step-events.test.ts` exits non-zero.
2. The failure is an assertion failure, not an import/type/timeout error.
3. `git status --porcelain src/` is empty — no producer file was touched.
4. `20-RED-PROOF.txt` is committed, non-empty, and records a non-zero exit code.
5. Full suite reports exactly 3 failures: the 2 known environmental POCs plus this test.
   Run: `npm test 2>&1 | tail -20`. Any fourth failure is a real regression and must be fixed.
</verification>

<success_criteria>
- The criterion-9 regression test exists and fails against the unmodified producer.
- The failure output is persisted into the phase directory and committed.
- A RED commit exists containing the failing test and no `src/` change, with its SHA recorded.
- No file under `src/` was modified by this plan.
- Test suite failure count is exactly 3 (2 environmental + 1 intentional RED).
</success_criteria>

<output>
Create `.planning/phases/20-audit-integrity/20-01-SUMMARY.md` when done.
Include a `## D-4 RED commit` section giving the SHA and the assertion that failed.
</output>
