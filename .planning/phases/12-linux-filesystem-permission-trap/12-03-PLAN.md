---
phase: 12-linux-filesystem-permission-trap
plan: 03
type: execute
wave: 2
depends_on: [12-01, 12-02]
files_modified:
  - tests/e2e/poc-permission-trap.test.ts
autonomous: true
requirements: [SCEN-07]

must_haves:
  truths:
    - "E2E test starts permission-trap Docker environment and verifies broken state (container running + Permission Denied in logs)"
    - "E2E test exercises full DPEV loop: /debug with mocked LLM diagnoses permission mismatch and generates fix plan"
    - "E2E test executes fix plan (chown + restart) and verifies recovery (PID file written successfully)"
    - "E2E test verifies audit trail contains complete DPEV sequence"
    - "No DB-specific logic used -- pure OS-level troubleshooting"
  artifacts:
    - path: "tests/e2e/poc-permission-trap.test.ts"
      provides: "Full DPEV loop E2E test for permission trap scenario"
      contains: "permission-trap"
      min_lines: 150
  key_links:
    - from: "tests/e2e/poc-permission-trap.test.ts"
      to: "demo/permission-trap/reset-permission-trap.sh"
      via: "execSync in beforeAll"
      pattern: "reset-permission-trap"
    - from: "tests/e2e/poc-permission-trap.test.ts"
      to: "skills/linux-filesystem-troubleshoot.md"
      via: "SkillRegistry.get('linux-filesystem-troubleshoot')"
      pattern: "linux-filesystem-troubleshoot"
    - from: "tests/e2e/poc-permission-trap.test.ts"
      to: "/debug endpoint"
      via: "supertest POST /debug"
      pattern: "post.*debug"
---

<objective>
Create the full DPEV loop E2E test for the permission trap scenario: mock LLM diagnoses ownership mismatch, generates fix plan with chown + restart, executor applies fix, and verification confirms recovery.

Purpose: Proves InfraBrain can handle OS-level troubleshooting end-to-end without any DB-specific logic.
Output: tests/e2e/poc-permission-trap.test.ts with 4 sequential tests covering broken state, diagnosis, fix execution, and audit trail.
</objective>

<execution_context>
@/Users/nikolasretkovic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nikolasretkovic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-linux-filesystem-permission-trap/12-CONTEXT.md
@.planning/phases/12-linux-filesystem-permission-trap/12-RESEARCH.md
@.planning/phases/12-linux-filesystem-permission-trap/12-01-SUMMARY.md
@.planning/phases/12-linux-filesystem-permission-trap/12-02-SUMMARY.md

# Direct template -- copy this structure exactly
@tests/e2e/poc-docker-storage.test.ts
@tests/e2e/helpers/index.ts

<interfaces>
<!-- From tests/e2e/poc-docker-storage.test.ts: -->
<!-- Pattern: vi.mock orchestrator modules -> beforeAll (reset script + broken state verify + temp dir + mocks) -> sequential tests -->
<!-- Mocked: selectSkill, generateFixPlan, buildMessages -->
<!-- Real: Docker commands, Express server, SQLite, audit logger, safety classifier -->
<!-- Tests: 1) broken env, 2) diagnose + plan, 3) execute + verify recovery, 4) audit trail -->

<!-- From tests/e2e/helpers/index.ts: -->
<!-- assertDPEVSequence(entries): validates S -> D -> E -> V ordering -->
<!-- createMockLLMProvider(response: string): returns mock Ollama provider -->

<!-- From Plan 01 output: -->
<!-- Container: permission-app (running, logs show "Permission denied") -->
<!-- Reset: bash demo/permission-trap/reset-permission-trap.sh -->
<!-- Success signal: docker logs show "PID written successfully" after fix -->

<!-- From Plan 02 output: -->
<!-- Skill: linux-filesystem-troubleshoot (preferred_model: default) -->
<!-- Discovery: DISCOVERY_COMMANDS['linux-filesystem-troubleshoot'] with 4 commands -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create full DPEV E2E test for permission trap scenario</name>
  <files>tests/e2e/poc-permission-trap.test.ts</files>
  <action>
Create `tests/e2e/poc-permission-trap.test.ts` by copying the structure of `tests/e2e/poc-docker-storage.test.ts` exactly and adapting for the permission trap scenario.

**Same mock setup pattern:**
- `vi.mock` for selectSkill, generateFixPlan, buildMessages (identical)
- Same imports: vitest, execSync, mkdtempSync, supertest, uuid, createServer, initDatabase, WriteThrough, AuditLogger, SkillRegistry, classifyCommand, validateCommand, InfraBrainConfigSchema, assertDPEVSequence, createMockLLMProvider

**Canned fix plan (per RESEARCH.md):**
```typescript
const cannedFixPlan: FixPlan = {
  summary: 'Fix directory ownership to match app user and restart',
  complexity: 'simple',
  steps: [
    {
      command: 'docker exec permission-app ls -ld /app/data',
      description: 'Verify directory ownership and permissions',
      risk: 'read',
      rollback: 'N/A',
    },
    {
      command: 'docker exec -u 0 permission-app chown 1000:1000 /app/data',
      description: 'Change directory ownership to app user (UID 1000)',
      risk: 'write',
      rollback: 'docker exec -u 0 permission-app chown root:root /app/data',
    },
    {
      command: 'docker restart permission-app',
      description: 'Restart app to retry PID file write',
      risk: 'write',
      rollback: 'docker stop permission-app',
    },
    {
      command: 'docker logs permission-app --tail 5',
      description: 'Verify app started successfully after fix',
      risk: 'read',
      rollback: 'N/A',
    },
  ],
};
```

**beforeAll (timeout 120_000):**
1. Run reset script: `execSync('bash demo/permission-trap/reset-permission-trap.sh', { cwd: PROJECT_ROOT, timeout: 60_000, stdio: 'pipe' })`
2. Verify broken state -- poll (max 15 attempts, 2s apart):
   - Container running: `docker inspect --format '{{.State.Status}}' permission-app` = "running"
   - Logs show error: `docker logs permission-app 2>&1` contains "Permission denied"
   - Both true = broken state confirmed
3. Create temp dir for session storage (same pattern as docker-storage test)
4. Initialize SQLite + WriteThrough store
5. Create mock LLM provider with permission-focused diagnostic reasoning:
```
createMockLLMProvider([
  'Diagnostic Ladder Investigation:',
  '',
  'Step 0 - Container Discovery: Found permission-app container in running state. Logs show Permission Denied error.',
  'Step 1 - Log Analysis: FATAL: Permission denied writing to /app/data/status.pid. App cannot write PID file.',
  'Step 2 - Permission Inspection: ls -ld /app/data shows drwx------ root root. Directory is mode 700, owned by root:root.',
  'Step 3 - User Identity Check: id shows uid=1000. App runs as UID 1000 but directory owned by root with 700 permissions.',
  'Step 4 - Correlation: Owner mismatch -- directory owned by root:root (mode 700 = rwx------), process runs as UID 1000. Non-root user cannot read, write, or enter the directory.',
  '',
  'Root Cause: /app/data directory owned by root:root with mode 700. App runs as UID 1000 and cannot access the directory.',
  'Fix: chown 1000:1000 /app/data to transfer ownership to app user, then restart container.',
].join('\n'))
```
6. Set up mocks: selectSkill returns `linux-filesystem-troubleshoot` skill, generateFixPlan returns cannedFixPlan
7. Create audit logger, config, Express app (identical pattern)

**afterAll:**
- Tear down: `docker compose -f demo/permission-trap/docker-compose.yml down --remove-orphans`
- Clean up temp dir

**Test 1: 'broken environment shows permission denied and running container'**
- Verify container running: `docker inspect --format '{{.State.Status}}' permission-app` = "running"
- Verify logs contain "Permission denied": `docker logs permission-app 2>&1`
- Verify directory ownership: `docker exec permission-app ls -ld /app/data` contains "root root"
- Verify user identity: `docker exec permission-app id` contains "uid=1000"

**Test 2: 'diagnoses permission mismatch and generates fix plan'**
- POST /debug with prompt: "Container permission-app has Permission Denied errors writing to /app/data"
- Assert: status 200, diagnosis contains "permission" or "ownership" or "mismatch"
- Assert: fixPlan defined with >= 3 steps
- Assert: skillMessage contains "linux-filesystem-troubleshoot"
- Store sessionId and fixPlan for next test

**Test 3: 'executes fix plan and verifies recovery'**
- POST /execute with sessionId, fixPlan, target: 'linux-filesystem-troubleshoot', adminName: 'e2e-test'
- Assert: status 200, status: 'completed', stepResults defined
- Verify recovery: `docker logs permission-app --tail 10` contains "PID" and "written successfully"
- Verify fix: `docker exec permission-app ls -ld /app/data` shows UID 1000 ownership (not root)

**Test 4: 'audit trail contains full DPEV evidence with correct ordering'**
- Query audit log for test session
- assertDPEVSequence(entries)
- Verify at least one entry has reasoning

**describe timeout: 120_000** (same as docker-storage test)
  </action>
  <verify>
    <automated>cd /Users/nikolasretkovic/Desktop/InfraBrain && npx vitest run tests/e2e/poc-permission-trap.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 E2E tests pass: broken state verified, diagnosis generates fix plan via mocked LLM, fix execution recovers the app (PID written successfully), audit trail contains complete DPEV sequence. No DB-specific logic used.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/e2e/poc-permission-trap.test.ts` -- all 4 tests pass
- `npx vitest run` -- full suite green (no regressions)
- Test exercises real Docker commands against permission-trap demo environment
- Mocked LLM produces permission-focused diagnostic reasoning (no DB logic)
- Fix plan uses chown (not chmod 777) as the proper fix
- Recovery verified by checking app logs for "PID written successfully"
</verification>

<success_criteria>
- E2E test starts Docker environment and verifies broken state
- Full DPEV loop exercised with mocked LLM and real Docker commands
- Fix applied via chown + restart, recovery verified
- Audit trail complete with DPEV sequence
- No DB-specific logic in any test assertion or mock response
- Full test suite passes without regressions
</success_criteria>

<output>
After completion, create `.planning/phases/12-linux-filesystem-permission-trap/12-03-SUMMARY.md`
</output>
