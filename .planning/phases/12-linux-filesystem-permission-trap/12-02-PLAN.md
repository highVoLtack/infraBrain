---
phase: 12-linux-filesystem-permission-trap
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/linux-filesystem-troubleshoot.md
  - src/api/routes/debug.ts
  - src/safety/rules.ts
autonomous: true
requirements: [SCEN-07]

must_haves:
  truths:
    - "linux-filesystem-troubleshoot skill is auto-loaded by SkillRegistry from skills/ directory"
    - "Skill triggers match filesystem permission problem descriptions (permission denied, chown, chmod, ownership, filesystem)"
    - "Skill system prompt instructs 4-step Diagnostic Ladder focused on permission investigation"
    - "4 discovery commands in debug.ts pre-inject ground truth for linux-filesystem-troubleshoot skill"
    - "Safety rules classify chown and chmod as WRITE, id and stat as READ"
  artifacts:
    - path: "skills/linux-filesystem-troubleshoot.md"
      provides: "4-step Diagnostic Ladder skill for Linux filesystem permission issues"
      contains: "Diagnostic Ladder"
    - path: "src/api/routes/debug.ts"
      provides: "DISCOVERY_COMMANDS['linux-filesystem-troubleshoot'] with 4 commands"
      contains: "linux-filesystem-troubleshoot"
    - path: "src/safety/rules.ts"
      provides: "Safety classification for chown, chmod (WRITE) and id, stat (READ)"
      contains: "chown"
  key_links:
    - from: "src/api/routes/debug.ts"
      to: "skills/linux-filesystem-troubleshoot.md"
      via: "DISCOVERY_COMMANDS keyed by skill name"
      pattern: "linux-filesystem-troubleshoot.*command"
    - from: "skills/linux-filesystem-troubleshoot.md"
      to: "container permission-app"
      via: "diagnostic ladder referencing container names from discovery"
      pattern: "permission-app"
---

<objective>
Create the linux-filesystem-troubleshoot diagnostic skill, register its discovery commands, and add safety classifications for filesystem commands (chown, chmod, id, stat).

Purpose: Enables InfraBrain to autonomously diagnose Linux filesystem permission issues -- ownership mismatches, mode restrictions, and UID mapping problems.
Output: skills/linux-filesystem-troubleshoot.md skill file + DISCOVERY_COMMANDS entry in debug.ts + safety rule updates in rules.ts.
</objective>

<execution_context>
@/Users/nikolasretkovic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nikolasretkovic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-linux-filesystem-permission-trap/12-CONTEXT.md
@.planning/phases/12-linux-filesystem-permission-trap/12-RESEARCH.md

# Direct templates
@skills/docker-storage.md
@skills/postgres-troubleshoot.md
@src/api/routes/debug.ts
@src/safety/rules.ts

<interfaces>
<!-- From src/api/routes/debug.ts lines 131-174: -->
<!-- DISCOVERY_COMMANDS is a Record<string, { command: string; label: string }[]> -->
<!-- Keyed by skill frontmatter name -- runDiscovery(skillName) looks up commands -->

<!-- From skills/postgres-troubleshoot.md / docker-storage.md: -->
<!-- Skill frontmatter: name, description, triggers, tools, preferred_model, priority -->
<!-- Body: System Prompt with STRICT RULES + Diagnostic Ladder steps + Tools + Examples -->

<!-- From src/safety/rules.ts: -->
<!-- DEFAULT_RULES: ClassificationRule[] with pattern (RegExp) and level (RiskLevel) -->
<!-- RiskLevel: READ, WRITE, DESTRUCTIVE -->
<!-- BLOCKED_PATTERNS: RegExp[] for non-overridable safety blocks -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create linux-filesystem-troubleshoot diagnostic skill</name>
  <files>skills/linux-filesystem-troubleshoot.md</files>
  <action>
Create `skills/linux-filesystem-troubleshoot.md` following the exact structure of `skills/docker-storage.md` and `skills/postgres-troubleshoot.md`.

**Frontmatter (per RESEARCH.md):**
```yaml
name: linux-filesystem-troubleshoot
description: "Diagnoses Linux filesystem permission issues in Docker containers -- ownership mismatches, mode restrictions, and UID mapping problems"
triggers:
  - permission denied
  - permission
  - chown
  - chmod
  - access denied
  - filesystem
  - ownership
tools:
  - docker
  - ls
  - id
  - stat
  - chown
  - chmod
  - cat
  - whoami
preferred_model: default
priority: 10
```

**System Prompt** -- follow the same STRICT RULES pattern from existing skills:

Role: "You are a surgical Linux filesystem permission analyst. You diagnose permission denied errors by correlating file ownership, directory modes, and process UID. You are a production execution engine, not a tutor."

**STRICT RULES** (same 6 rules as docker-storage.md and postgres-troubleshoot.md):
1. NEVER invent container names -- use ONLY names from discovery/docker ps
2. Execute one command at a time, no pipes or semicolons
3. Gather evidence BEFORE proposing fixes
4. Every discovery must use GROUND TRUTH from previous steps
5. Use TOON encoding format for structured data
6. Follow Diagnostic Ladder -- do NOT skip steps

**Diagnostic Ladder (4 steps per CONTEXT.md):**

**Step 0: Container Discovery (MANDATORY)**
- Run: `docker ps -a --format "{{.Names}} {{.Status}}"` and `docker logs <container>`
- Purpose: Discover container names, status, and crash logs. Use `docker ps -a` to include exited containers.
- Output: Container list with status, identify which container has errors.

**Step 1: Log Analysis**
- Run: `docker logs <container> --tail 50`
- Purpose: Find the Permission Denied error message, identify which path failed.
- Output: Error message with file path.

**Step 2: Permission Inspection**
- Run: `docker exec <container> ls -ld <path>`
- Purpose: Check directory ownership (user:group) and mode bits.
- Output: Permission string, owner, group for the target directory.
- Note: `docker exec` runs as root by default, so this works even on 700 directories.

**Step 3: User Identity Check**
- Run: `docker exec <container> id`
- Purpose: Determine which UID/GID the process runs as.
- Output: uid=1000(?) gid=1000(?) groups=...

**Step 4: Correlation and Fix**
- Purpose: Correlate: directory owned by root:root mode 700, process runs as UID 1000.
- Fix: `docker exec -u 0 <container> chown 1000:1000 <path>` -- use `-u 0` to run as root explicitly.
- Then restart: `docker restart <container>` so the app retries the write.
- Note: Prefer `chown` over `chmod 777` -- changing ownership is the correct fix, not opening permissions to everyone.

**Important Rules section:**
- Use `docker ps -a` (not `docker ps`) to see exited/crashed containers
- `docker exec` runs as root by default -- this is how `chown` works even on restricted directories
- Prefer `chown` over `chmod 777`
- After fix, the app needs restart: `docker restart <container>`

**Tools section:** docker, ls, id, stat, chown, chmod, cat, whoami with brief descriptions.

**Examples section:** One complete example showing the Permission Trap scenario with all steps and the fix plan. Use container name `permission-app`. Show the ownership mismatch reasoning explicitly.
  </action>
  <verify>
    <automated>cd /Users/nikolasretkovic/Desktop/InfraBrain && node -e "const {SkillRegistry} = await import('./src/skills/registry.js'); const r = new SkillRegistry(); r.populate('skills'); const s = r.get('linux-filesystem-troubleshoot'); if (!s) throw new Error('skill not found'); console.log('OK:', s.frontmatter.name, 'tools:', s.frontmatter.tools.join(','), 'model:', s.frontmatter.preferred_model);"</automated>
  </verify>
  <done>SkillRegistry loads linux-filesystem-troubleshoot skill, frontmatter has correct name/triggers/tools/preferred_model=default, system prompt contains 4-step Diagnostic Ladder with permission correlation</done>
</task>

<task type="auto">
  <name>Task 2: Register discovery commands and update safety rules</name>
  <files>src/api/routes/debug.ts, src/safety/rules.ts</files>
  <action>
**Part A: Discovery commands in debug.ts**

Add a `'linux-filesystem-troubleshoot'` entry to the `DISCOVERY_COMMANDS` record in `src/api/routes/debug.ts` (after the existing `'docker-storage'` entry, around line 173).

```typescript
'linux-filesystem-troubleshoot': [
  { command: 'docker ps -a --format "{{.Names}} {{.Status}}"', label: 'All containers with status' },
  { command: 'docker logs permission-app --tail 50', label: 'App crash logs' },
  {
    command: 'docker exec permission-app ls -ld /app/data',
    label: 'Target directory permissions',
  },
  {
    command: 'docker exec permission-app id',
    label: 'App user identity',
  },
],
```

Note: Uses `docker ps -a` (not just `docker ps`) because the container might be exited. Uses `docker exec` because the app stays alive (sleep loop). These commands work because Plan 01 keeps the container running.

**Part B: Safety rules in rules.ts**

Add to DEFAULT_RULES in `src/safety/rules.ts`:

Read-only (add after existing READ rules, before the WRITE section):
```typescript
{ pattern: /^id\b/, level: RiskLevel.READ },
{ pattern: /^stat\s+/, level: RiskLevel.READ },
```

Write (add after existing WRITE rules, before the DESTRUCTIVE section):
```typescript
{ pattern: /^chown\s+/, level: RiskLevel.WRITE },
{ pattern: /^chmod\s+/, level: RiskLevel.WRITE },
```

Also add `docker exec` classification. Currently `docker exec` is NOT classified in DEFAULT_RULES. The skill allowlist handles tool-level access (skill allows `docker`), but the safety classifier needs to handle `docker exec` commands for risk classification. Add:
```typescript
{ pattern: /^docker\s+exec\s+/, level: RiskLevel.WRITE },
```

This classifies `docker exec` as WRITE (conservative default -- it can run arbitrary commands inside containers). Add in the WRITE section alongside `docker restart`.

Do NOT modify any other part of these files.
  </action>
  <verify>
    <automated>cd /Users/nikolasretkovic/Desktop/InfraBrain && npx tsc --noEmit 2>&1 | head -20 && npx vitest run tests/unit/safety-classifier.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>TypeScript compiles without errors, DISCOVERY_COMMANDS contains 'linux-filesystem-troubleshoot' key with 4 entries, DEFAULT_RULES includes chown/chmod as WRITE and id/stat as READ, existing safety classifier tests pass</done>
</task>

</tasks>

<verification>
- SkillRegistry loads linux-filesystem-troubleshoot skill from skills/ directory
- Skill frontmatter: name=linux-filesystem-troubleshoot, preferred_model=default, tools include docker/ls/id/stat/chown/chmod
- Skill body contains 4-step Diagnostic Ladder with permission correlation
- debug.ts compiles, DISCOVERY_COMMANDS['linux-filesystem-troubleshoot'] has 4 entries
- rules.ts compiles, chown/chmod classified as WRITE, id/stat as READ
- Existing tests still pass: `npx vitest run --exclude 'tests/e2e/**'`
</verification>

<success_criteria>
- linux-filesystem-troubleshoot.md skill auto-loaded by registry with correct frontmatter
- 4-step Diagnostic Ladder follows established pattern with OS-level focus
- Skill prefers chown over chmod 777 as the fix
- 4 discovery commands registered in debug.ts for ground truth injection
- Safety rules updated for filesystem commands (chown, chmod, id, stat, docker exec)
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/12-linux-filesystem-permission-trap/12-02-SUMMARY.md`
</output>
