---
phase: 12.4-agnostic-skill-redesign
plan: 01
subsystem: engine
tags: [zod, schema, rewriter, allowlist, tool-map, backward-compat]

# Dependency graph
requires:
  - phase: 12.1-dynamic-rewriter
    provides: dynamicRewrite() and RewriteRuleSchema
  - phase: 12.3-engine-proof-rewriter
    provides: docker-exec-aware rewriting, parseDockerExec
provides:
  - ToolDeclarationSchema with risk/user/wrapper/strip_flags/container
  - SkillFrontmatterSchema union tools field (string[] | Record<string, ToolDeclaration>)
  - toolsToRewriteRules() adapter converting tool map to RewriteRule[]
  - generateToolList() auto-injecting YOUR TOOLS into system prompt
  - Backward-compatible allowlist handling both tool formats
affects: [12.4-02-skill-migration, 12.4-03-cleanup]

# Tech tracking
tech-stack:
  added: []
  patterns: [union-schema-with-default, adapter-pattern-for-backward-compat, auto-generated-system-prompt-sections]

key-files:
  created: []
  modified:
    - src/skills/types.ts
    - src/execution/dynamic-rewriter.ts
    - src/skills/allowlist.ts
    - src/api/routes/debug.ts
    - src/orchestrator/context.ts
    - tests/skills/types.test.ts
    - tests/execution/dynamic-rewriter.test.ts
    - tests/orchestrator/context.test.ts

key-decisions:
  - "ToolDeclarationSchema requires risk field (read/write/destructive), all others optional"
  - "tools field defaults to {} (empty map) not [] (empty array) for new-format-first design"
  - "z.union([string[], Record]) with .default({}) enables gradual migration"
  - "toolsToRewriteRules uses ^toolName\\b regex for each tool name"
  - "Removed unused version and author fields from SkillFrontmatterSchema"
  - "generateToolList injected only for map-format skills, not legacy string[] (clean separation)"

patterns-established:
  - "Union schema with default: z.union([legacy, new]).default(newDefault) for backward-compat migration"
  - "Adapter pattern: toolsToRewriteRules() converts new format to existing engine input"
  - "Format detection via Array.isArray() for dual-format handling across engine files"

requirements-completed: [ENGN-06]

# Metrics
duration: 3min
completed: 2026-03-14
---

# Phase 12.4 Plan 01: Engine Type Foundation Summary

**ToolDeclarationSchema + toolsToRewriteRules() adapter with union tools field, allowlist/debug/context wiring, and auto-generated YOUR TOOLS system prompt injection**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-14T12:29:45Z
- **Completed:** 2026-03-14T12:33:07Z
- **Tasks:** 2
- **Files modified:** 8

## Accomplishments
- ToolDeclarationSchema exported from types.ts with risk (required), user, wrapper, strip_flags, container fields
- SkillFrontmatterSchema accepts BOTH string[] (legacy) and Record<string, ToolDeclaration> (new map) via z.union()
- toolsToRewriteRules() converts unified tool map to RewriteRule[] for dynamicRewrite() compatibility
- allowlist.ts, debug.ts, and context.ts all handle both tool formats transparently
- context.ts auto-injects YOUR TOOLS section into LLM system prompt for map-format skills
- 489 tests passing -- zero regressions, full backward compatibility proven

## Task Commits

Each task was committed atomically:

1. **Task 1: ToolDeclarationSchema + toolsToRewriteRules() adapter + union schema**
   - `5b0321e` (test: failing tests - TDD RED)
   - `7822c13` (feat: implementation - TDD GREEN)
2. **Task 2: Wire engine files -- allowlist, debug.ts, context.ts** - `0c6ca50` (feat)

## Files Created/Modified
- `src/skills/types.ts` - Added ToolDeclarationSchema, ToolDeclaration type, union tools field in SkillFrontmatterSchema
- `src/execution/dynamic-rewriter.ts` - Added toolsToRewriteRules() adapter function
- `src/skills/allowlist.ts` - Updated enforceSkillAllowlist to handle both string[] and Record tools
- `src/api/routes/debug.ts` - Derives rewrite rules from tool map for new-format skills
- `src/orchestrator/context.ts` - Added generateToolList() and YOUR TOOLS injection in buildMessages()
- `tests/skills/types.test.ts` - ToolDeclarationSchema + union tools tests (9 new)
- `tests/execution/dynamic-rewriter.test.ts` - toolsToRewriteRules tests (5 new)
- `tests/orchestrator/context.test.ts` - YOUR TOOLS injection tests (2 new)

## Decisions Made
- ToolDeclarationSchema requires `risk` field -- matches RewriteRuleSchema's optional risk but makes it mandatory for tool declarations (tools must declare their risk level)
- Default tools to `{}` (empty map) not `[]` -- new-format-first design, legacy skills explicitly set string[]
- Removed `version` and `author` fields from SkillFrontmatterSchema -- unused across entire codebase
- generateToolList only injected for map-format skills -- clean separation means legacy skills behave identically to before
- toolsToRewriteRules escapes special regex chars in tool names for safety

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Engine fully supports both legacy and new tool formats
- Plan 02 (skill migration) can proceed: convert skill YAML frontmatter from string[] tools + rewrite_rules to unified tool map
- Plan 03 (cleanup) can proceed after migration: remove rewrite_rules field and legacy code paths

## Self-Check: PASSED

- All 8 modified files exist on disk
- All 3 task commits verified (5b0321e, 7822c13, 0c6ca50)
- 489 tests passing, zero regressions

---
*Phase: 12.4-agnostic-skill-redesign*
*Completed: 2026-03-14*
