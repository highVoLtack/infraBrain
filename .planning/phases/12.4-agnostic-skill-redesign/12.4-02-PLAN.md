---
phase: 12.4-agnostic-skill-redesign
plan: 02
type: execute
wave: 2
depends_on: ["12.4-01"]
files_modified:
  - skills/linux-expert.md
  - skills/postgres-expert.md
  - skills/network-expert.md
  - skills/linux-filesystem-troubleshoot.md
  - skills/docker-storage.md
  - skills/postgres-troubleshoot.md
  - skills/nginx-troubleshoot.md
  - skills/log-analysis.md
autonomous: true
requirements: [ENGN-06]

must_haves:
  truths:
    - "linux-expert.md covers both permission and disk pressure domains with unified tool map"
    - "postgres-expert.md covers connection leak diagnosis with SQL wrapper in tool map"
    - "network-expert.md covers Nginx 502 and connectivity diagnosis"
    - "All 3 new skills use Domain Knowledge sections instead of Diagnostic Ladders"
    - "No skill contains hardcoded container names in prompts or examples"
    - "Discovery commands are generic (docker ps -a, docker stats) not scenario-specific"
    - "Old skill files are deleted (not deprecated) to avoid registry confusion"
    - "log-analysis.md updated to use new tool map format"
  artifacts:
    - path: "skills/linux-expert.md"
      provides: "Universal Linux expert skill covering permissions + disk pressure"
      contains: "DOMAIN KNOWLEDGE"
    - path: "skills/postgres-expert.md"
      provides: "Universal Postgres expert skill"
      contains: "DOMAIN KNOWLEDGE"
    - path: "skills/network-expert.md"
      provides: "Universal network expert skill"
      contains: "DOMAIN KNOWLEDGE"
  key_links:
    - from: "skills/linux-expert.md"
      to: "src/skills/types.ts"
      via: "YAML frontmatter parsed by SkillFrontmatterSchema"
      pattern: "tools:"
    - from: "skills/postgres-expert.md"
      to: "src/execution/dynamic-rewriter.ts"
      via: "tool map converted by toolsToRewriteRules()"
      pattern: "wrapper:"
---

<objective>
Create 3 universal expert skills (linux-expert, postgres-expert, network-expert) that replace the 4 scenario-specific skills. Convert from Diagnostic Ladders to Domain Knowledge sections. Use new unified tool map format. Remove all hardcoded container names. Delete old skill files.

Purpose: This completes the "skills as domain knowledge" transformation. Skills become universal -- they work for ANY instance, not just demo scenarios.
Output: 3 new skill files in skills/, old skills deleted, log-analysis.md updated.
</objective>

<execution_context>
@/Users/nikolasretkovic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nikolasretkovic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.4-agnostic-skill-redesign/12.4-CONTEXT.md
@.planning/phases/12.4-agnostic-skill-redesign/12.4-RESEARCH.md
@.planning/phases/12.4-agnostic-skill-redesign/12.4-01-SUMMARY.md

<!-- Read all current skills to understand what domain knowledge to preserve -->
@skills/linux-filesystem-troubleshoot.md
@skills/docker-storage.md
@skills/postgres-troubleshoot.md
@skills/nginx-troubleshoot.md
@skills/log-analysis.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 3 universal expert skills with new tool map format</name>
  <files>skills/linux-expert.md, skills/postgres-expert.md, skills/network-expert.md</files>
  <action>
    Create 3 new skill files using the new tool map format (Record, not string[]) and Domain Knowledge sections (not Diagnostic Ladders).

    **skills/linux-expert.md** (merges linux-filesystem-troubleshoot + docker-storage):
    - `name: linux-expert`
    - `description: "Universal Linux specialist for filesystem permissions, disk pressure, process diagnostics, and OOM analysis in Docker environments"`
    - `triggers:` broad -- permission, crash, error, slow, disk, storage, volume, OOM, killed, exit, no space, truncate, chown, chmod, access denied, filesystem, ownership, df, du
    - `tools:` map format with:
      - `ls: { risk: read }`, `id: { risk: read }`, `stat: { risk: read }`, `cat: { risk: read }`, `whoami: { risk: read }`
      - `chown: { risk: write, user: "0" }`, `chmod: { risk: write, user: "0" }`
      - `df: { risk: read }`, `du: { risk: read }`, `truncate: { risk: write }`
      - `docker: { risk: read }`
    - `preferred_model: default`, `priority: 10`
    - `discovery:` generic only -- `docker ps -a --format "{{.Names}} {{.Status}}"` (label: Container Inventory), `docker stats --no-stream` (label: Resource Usage)
    - System Prompt: "You are a Senior Linux Systems Engineer with deep expertise..."
    - DOMAIN KNOWLEDGE: PERMISSIONS section (from research doc section 12): UID correlation, chown over chmod 777, Docker volume ownership, restart after fix
    - DOMAIN KNOWLEDGE: DISK PRESSURE section: df -h, du -sh, truncate -s 0 not rm, classify before acting (log vs state)
    - DOMAIN KNOWLEDGE: PROCESS AND OOM section: OOM killed check, docker stats, memory limits
    - EXECUTION PROTOCOL section: 5-step reasoning process (analyze ground truth, identify failing container, gather evidence, correlate, propose fix)
    - NO Diagnostic Ladder, NO COMMAND-ONLY MODE instructions (the engine + YOUR TOOLS injection handles this), NO hardcoded container names
    - Keep STRICT RULES section with: ZERO HYPOTHETICAL REASONING, ZERO PLACEHOLDERS, DISCOVERY IS GROUND TRUTH, FRESH DATA FOR MUTATIONS, ONE COMMAND PER STEP, EVIDENCE BEFORE ACTION

    **skills/postgres-expert.md** (replaces postgres-troubleshoot):
    - `name: postgres-expert`
    - `description: "Universal PostgreSQL specialist for connection saturation, deadlocks, slow queries, and replication diagnostics"`
    - `triggers:` postgres, connection, max_connections, pg_stat_activity, too many connections, connection limit, deadlock, slow query, replication, vacuum
    - `tools:` map format:
      - `psql: { risk: read, wrapper: 'psql -U postgres -c "{cmd}"', strip_flags: ["-h", "--host"] }`
      - `docker: { risk: read }`
    - `preferred_model: forensic`, `priority: 10`
    - `discovery:` generic -- `docker ps --format "{{.Names}}"` (Running containers), `docker network ls --format "{{.Name}}"` (Docker networks)
    - Note: The scenario-specific discovery commands (SELECT count from pg_stat_activity, SHOW max_connections) are REMOVED from discovery. The LLM will gather this evidence itself using its SQL tools, guided by domain knowledge.
    - System Prompt: "You are a surgical PostgreSQL diagnostics engineer. You output ONLY valid JSON matching the StructuredDiagnosis schema."
    - DOMAIN KNOWLEDGE: CONNECTION SATURATION section: pg_stat_activity queries, idle connection identification, client_addr correlation, pg_terminate_backend
    - DOMAIN KNOWLEDGE: MANDATORY FIX CONSTRAINT: ONLY pg_terminate_backend allowed. FORBIDDEN: container restarts, pooler installation, config changes, package installation.
    - Keep SQL-ONLY MODE note: "Write ONLY raw SQL queries. The engine wraps in docker exec + psql automatically. NEVER use -h flag."
    - EXECUTION PROTOCOL: analyze ground truth, query pg_stat_activity, correlate, terminate leakers
    - NO hardcoded container names (no "postgres-demo"), NO Diagnostic Ladder steps

    **skills/network-expert.md** (replaces nginx-troubleshoot):
    - `name: network-expert`
    - `description: "Universal network diagnostics specialist for HTTP errors, reverse proxy issues, DNS resolution, and Docker network connectivity"`
    - `triggers:` nginx, 502, bad gateway, proxy, upstream, gateway, connection refused, dns, network, connectivity, timeout
    - `tools:` map format:
      - `docker: { risk: read }`, `curl: { risk: read }`, `nginx: { risk: read }`, `cat: { risk: read }`, `grep: { risk: read }`, `ss: { risk: read }`
    - `priority: 10` (no preferred_model -- uses default)
    - `discovery:` generic -- `docker ps --format "{{.Names}}"` (Running containers), `docker network ls --format "{{.Name}}"` (Docker networks)
    - System Prompt: "You are a surgical network infrastructure engineer..."
    - DOMAIN KNOWLEDGE: HTTP DIAGNOSTICS: curl status codes, response headers, upstream errors
    - DOMAIN KNOWLEDGE: REVERSE PROXY: Nginx error log patterns, upstream host correlation, docker network inspect
    - DOMAIN KNOWLEDGE: DOCKER NETWORKING: network inspect, container connectivity, DNS resolution
    - EXECUTION PROTOCOL: HTTP check -> log analysis -> network topology -> cross-layer correlation -> fix
    - Keep STRICT RULES section
    - NO hardcoded container names, NO Diagnostic Ladder

    For ALL three skills:
    - Do NOT include a `## Tools` markdown section in the body (the engine auto-generates YOUR TOOLS from the frontmatter tool map via context.ts)
    - Do NOT include a `## Examples` section with hardcoded container names
    - Do NOT include `rewrite_rules:` in frontmatter (merged into tools map)
  </action>
  <verify>
    <automated>cd /Users/nikolasretkovic/Desktop/InfraBrain && node -e "
      const { loadSkillFile } = await import('./src/skills/loader.js');
      for (const f of ['skills/linux-expert.md', 'skills/postgres-expert.md', 'skills/network-expert.md']) {
        const s = loadSkillFile(f);
        const t = s.frontmatter.tools;
        console.log(f, ':', typeof t === 'object' && !Array.isArray(t) ? 'MAP format OK' : 'FAIL - not a map');
        console.log('  tools:', Object.keys(t).join(', '));
        console.log('  discovery:', s.frontmatter.discovery.length, 'commands');
      }
    " 2>&1</automated>
  </verify>
  <done>3 new universal expert skills created with map-format tools, Domain Knowledge sections, generic discovery, and zero hardcoded container names. All parse successfully via SkillFrontmatterSchema.</done>
</task>

<task type="auto">
  <name>Task 2: Delete old skills + update log-analysis.md to new format</name>
  <files>skills/linux-filesystem-troubleshoot.md, skills/docker-storage.md, skills/postgres-troubleshoot.md, skills/nginx-troubleshoot.md, skills/log-analysis.md</files>
  <action>
    1. Delete the 4 old scenario-specific skill files:
       - `rm skills/linux-filesystem-troubleshoot.md`
       - `rm skills/docker-storage.md`
       - `rm skills/postgres-troubleshoot.md`
       - `rm skills/nginx-troubleshoot.md`

    2. Update `skills/log-analysis.md` to use new tool map format:
       - Change `tools:` from string array to map:
         ```yaml
         tools:
           grep: { risk: read }
           journalctl: { risk: read }
           docker: { risk: read }
           tail: { risk: read }
           cat: { risk: read }
           head: { risk: read }
           zcat: { risk: read }
           less: { risk: read }
         ```
       - Keep existing system prompt, examples, and sections (log-analysis is already a utility skill, not scenario-specific)
       - Remove `rewrite_rules` if present (it's not, but check)

    3. Verify planning.md and verification.md still parse (they are utility skills, should be unaffected since they likely use the old tools: [] format which defaults to empty):
       - These skills have no tools listed, so they default to {} which is fine

    4. Verify final skills directory contains exactly: linux-expert.md, postgres-expert.md, network-expert.md, log-analysis.md, planning.md, verification.md
  </action>
  <verify>
    <automated>cd /Users/nikolasretkovic/Desktop/InfraBrain && ls skills/*.md | sort && echo "---" && node -e "
      const { loadSkillDirectory } = await import('./src/skills/loader.js');
      const { skills, errors } = loadSkillDirectory('skills');
      console.log('Loaded:', skills.length, 'skills');
      console.log('Errors:', errors.length);
      errors.forEach(e => console.log('  ERROR:', e.file, e.error));
      skills.forEach(s => console.log(' ', s.frontmatter.name));
    " 2>&1</automated>
  </verify>
  <done>4 old scenario skills deleted. log-analysis.md converted to map format. All remaining skills (6 total) load without errors. Registry contains: linux-expert, postgres-expert, network-expert, log-analysis, planning, verification.</done>
</task>

</tasks>

<verification>
1. All 6 skill files parse without errors via loadSkillDirectory()
2. No skill file contains hardcoded container names (permission-app, storage-logger, storage-redis, postgres-demo) in prompts
3. All 3 new expert skills use map-format tools (not string[])
4. Non-E2E tests still pass (loader tests, registry tests):
```bash
cd /Users/nikolasretkovic/Desktop/InfraBrain && npx vitest run tests/skills/ --reporter=verbose 2>&1 | tail -30
```
</verification>

<success_criteria>
1. skills/ contains exactly 6 files: linux-expert, postgres-expert, network-expert, log-analysis, planning, verification
2. All 3 expert skills use Domain Knowledge sections, not Diagnostic Ladders
3. All 3 expert skills use map-format tools with risk classification
4. Zero hardcoded container names in any skill prompt/example
5. Discovery commands are generic (docker ps, docker stats) not scenario-specific
6. All skill files load without parse errors
</success_criteria>

<output>
After completion, create `.planning/phases/12.4-agnostic-skill-redesign/12.4-02-SUMMARY.md`
</output>
