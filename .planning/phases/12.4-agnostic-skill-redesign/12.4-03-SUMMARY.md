---
phase: 12.4-agnostic-skill-redesign
plan: 03
subsystem: testing
tags: [vitest, e2e, skills, registry, universal-experts]

# Dependency graph
requires:
  - phase: 12.4-agnostic-skill-redesign (plan 01)
    provides: ToolDeclarationSchema, map-format tools, toolsToRewriteRules adapter
  - phase: 12.4-agnostic-skill-redesign (plan 02)
    provides: 3 universal expert skills (linux-expert, postgres-expert, network-expert), old skills deleted
provides:
  - All E2E tests updated for universal expert skill names
  - Map-format tools loader test validating Record structure
  - Real skills directory integration test (6 skills)
  - Full test suite verification (506 passing)
affects: [future-phases, skill-system]

# Tech tracking
tech-stack:
  added: []
  patterns: [universal-expert-skill-naming-in-tests]

key-files:
  modified:
    - tests/e2e/poc-permission-trap.test.ts
    - tests/e2e/poc-docker-storage.test.ts
    - tests/e2e/poc-nginx-502.test.ts
    - tests/e2e/poc-postgres-connleak.test.ts
    - tests/skills/loader.test.ts

key-decisions:
  - "Mock skill objects in unit tests (routes, loop-guard, history) kept with old names since they are self-contained fixtures that do not use registry.get()"
  - "2 pre-existing nginx E2E failures (Docker networking) remain out of scope -- not related to skill naming"

patterns-established:
  - "E2E tests use universal expert skill names via registry.get('linux-expert'), registry.get('postgres-expert'), registry.get('network-expert')"

requirements-completed: [ENGN-06]

# Metrics
duration: 5min
completed: 2026-03-14
---

# Phase 12.4 Plan 03: Test Migration Summary

**All E2E and unit tests migrated to universal expert skills -- 506 tests passing with new skill names**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-14T12:41:49Z
- **Completed:** 2026-03-14T12:47:25Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- All 4 E2E tests updated from old scenario-specific skill names to new universal expert names
- New loader test validates map-format tools are parsed as Record (not array)
- New integration test confirms all 6 real skills load from skills/ directory
- Full test suite: 506 passing, 2 failed (pre-existing nginx Docker networking issue), 4 skipped

## Task Commits

Each task was committed atomically:

1. **Task 1: Update E2E tests for new skill names** - `1a1ff42` (feat)
2. **Task 2: Update skill unit tests + run full suite verification** - `74d4c7f` (test)

## Files Created/Modified
- `tests/e2e/poc-permission-trap.test.ts` - linux-filesystem-troubleshoot -> linux-expert
- `tests/e2e/poc-docker-storage.test.ts` - docker-storage -> linux-expert
- `tests/e2e/poc-nginx-502.test.ts` - nginx-troubleshoot -> network-expert
- `tests/e2e/poc-postgres-connleak.test.ts` - postgres-troubleshoot -> postgres-expert
- `tests/skills/loader.test.ts` - Added map-format tools test + real skills directory test

## Decisions Made
- Mock skill objects in unit tests (routes.test.ts, postgres-loop-guard.test.ts, history.test.ts) kept with old names since they are self-contained fixtures that do not use registry.get() -- they create inline mock objects and all pass
- 2 pre-existing nginx E2E failures (execution halts due to Docker networking environment) remain out of scope -- confirmed identical failure on pre-change code

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 12.4 complete: all 3 plans (tool schema, universal skills, test migration) delivered
- Full test suite green (minus 2 pre-existing nginx Docker networking issues)
- Ready for next milestone phase

---
*Phase: 12.4-agnostic-skill-redesign*
*Completed: 2026-03-14*
