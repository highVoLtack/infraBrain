---
phase: 16-qdrant-fix-caching
plan: 04
type: execute
wave: 1
depends_on: ["16-01", "16-02", "16-03"]
files_modified:
  - src/cache/lance-store.ts
  - src/cache/cache-lookup.ts
  - tests/cache/cache-lookup.test.ts
autonomous: true
gap_closure: true
requirements:
  - CACHE-06

must_haves:
  truths:
    - "Calling recordFixOutcome(store, id, true) twice results in success_count = 2"
    - "Calling recordFixOutcome(store, id, false) twice results in fail_count = 2"
    - "Confidence scoring receives accurate success_count / hit_count ratio after multiple cache uses"
  artifacts:
    - path: "src/cache/lance-store.ts"
      provides: "getById method for reading single entry by ID"
      contains: "async getById"
    - path: "src/cache/cache-lookup.ts"
      provides: "recordFixOutcome with read-then-increment logic"
    - path: "tests/cache/cache-lookup.test.ts"
      provides: "Tests asserting exact increment values, not expect.any(Number)"
  key_links:
    - from: "src/cache/cache-lookup.ts"
      to: "src/cache/lance-store.ts"
      via: "store.getById(entryId) to read current counts before incrementing"
      pattern: "store\\.getById"
---

<objective>
Fix recordFixOutcome to increment success_count/fail_count instead of overwriting with absolute value 1.

Purpose: CACHE-06 confidence scoring uses successRate = successCount / totalUses. The current absolute-set bug means success_count stays at 1 regardless of how many times a cached fix succeeds, producing incorrect confidence scores over time.

Output: Correct increment behavior in recordFixOutcome, backed by tests that assert exact count values.
</objective>

<execution_context>
@/Users/nikolasretkovic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nikolasretkovic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-qdrant-fix-caching/16-VERIFICATION.md

<interfaces>
<!-- From src/cache/lance-store.ts — CacheStore class methods relevant to this fix -->

```typescript
export class CacheStore {
  async listAll(): Promise<Array<Record<string, unknown>>>;
  async updateStats(
    id: string,
    updates: Partial<Pick<CacheEntry, 'hit_count' | 'success_count' | 'fail_count' | 'last_used'>>,
  ): Promise<boolean>;
  // NOTE: No getById method exists yet — Task 1 adds it
}
```

<!-- From src/cache/cache-lookup.ts — function to fix -->

```typescript
export async function recordFixOutcome(
  store: CacheStore | null,
  entryId: string,
  succeeded: boolean,
): Promise<void>;
```

<!-- From src/cache/types.ts — CacheEntry fields -->

```typescript
interface CacheEntry {
  id: string;
  hit_count: number;
  success_count: number;
  fail_count: number;
  last_used: string;
  // ... other fields
}
```

<!-- Existing pattern: checkCache reads entry then passes incremented value -->
<!-- src/cache/cache-lookup.ts line 117: hit_count: entry.hit_count + 1 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add getById to CacheStore and fix recordFixOutcome increment logic</name>
  <files>src/cache/lance-store.ts, src/cache/cache-lookup.ts, tests/cache/cache-lookup.test.ts</files>
  <behavior>
    - Test: recordFixOutcome called once with succeeded=true results in updateStats receiving success_count = 1 (from 0 + 1)
    - Test: recordFixOutcome called twice with succeeded=true results in updateStats receiving success_count = 2 on the second call
    - Test: recordFixOutcome called once with succeeded=false results in updateStats receiving fail_count = 1
    - Test: recordFixOutcome called twice with succeeded=false results in updateStats receiving fail_count = 2 on the second call
    - Test: recordFixOutcome with null store still does nothing (existing test, keep it)
    - Test: recordFixOutcome gracefully handles getById returning null (entry deleted between calls)
  </behavior>
  <action>
    1. In `src/cache/lance-store.ts`, add a `getById(id: string)` method to CacheStore:
       - Query the table with `where: id = '{id}'`, limit 1
       - Return the row as `Record<string, unknown> | null`
       - Wrap in try-catch, return null on failure (graceful degradation pattern)
       - Place it after the existing `listAll()` method for logical grouping

    2. In `src/cache/cache-lookup.ts`, fix `recordFixOutcome`:
       - After the `if (!store) return` guard, call `const existing = await store.getById(entryId)`
       - If `existing` is null, return early (entry was deleted or not found — graceful degradation)
       - Read current counts: `const currentSuccess = Number(existing.success_count ?? 0)` and `const currentFail = Number(existing.fail_count ?? 0)`
       - On success: set `updates.success_count = currentSuccess + 1`
       - On failure: set `updates.fail_count = currentFail + 1`
       - Remove the comment about "signal intent" — this is now the real implementation

    3. In `tests/cache/cache-lookup.test.ts`, update the `recordFixOutcome` describe block:
       - Add `getById` to `makeMockStore` returning a mock entry with `{ success_count: 0, fail_count: 0 }` by default
       - Replace `expect.any(Number)` assertions with exact values: `success_count: 1`
       - Add test "increments success_count across two calls": configure mock so first `getById` returns `{ success_count: 0 }`, second returns `{ success_count: 1 }`. Call `recordFixOutcome` twice. Assert second `updateStats` call has `success_count: 2`.
       - Add test "increments fail_count across two calls": same pattern with fail_count.
       - Add test "handles missing entry gracefully": mock `getById` returning null, assert `updateStats` is NOT called, no error thrown.
  </action>
  <verify>
    <automated>cd /Users/nikolasretkovic/Desktop/InfraBrain && npx vitest run tests/cache/cache-lookup.test.ts</automated>
  </verify>
  <done>
    - recordFixOutcome reads current counts via store.getById before incrementing
    - Two successive success outcomes produce success_count = 2 (verified by test)
    - Two successive failure outcomes produce fail_count = 2 (verified by test)
    - Null store and missing entry cases handled gracefully
    - No expect.any(Number) remaining in recordFixOutcome tests
    - All existing cache-lookup tests still pass
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/cache/cache-lookup.test.ts` — all tests pass including new increment assertions
2. `npx vitest run tests/cache/` — full cache test suite passes (no regressions)
3. No `expect.any(Number)` in recordFixOutcome test assertions (grep check)
4. `store.getById` is called before `store.updateStats` in recordFixOutcome (code review)
</verification>

<success_criteria>
- recordFixOutcome correctly increments (not overwrites) success_count and fail_count
- CACHE-06 confidence scoring receives accurate successRate data after multiple cache uses
- All 58+ cache tests pass with no regressions
- The verification gap from 16-VERIFICATION.md is closed
</success_criteria>

<output>
After completion, create `.planning/phases/16-qdrant-fix-caching/16-04-SUMMARY.md`
</output>
